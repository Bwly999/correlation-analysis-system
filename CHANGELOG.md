# 更新日志 (Changelog)

## [Unreleased]

### 新增 (Added)

- **新增统一相关性分析节点**:
  - 新增 `correlation-analysis` 终点节点，可在一个节点内选择 Pearson、Spearman、Kendall 多种相关性方法。
  - 多方法结果会合并为同一份报告，分别展示各方法的热力图、排行、可信提示与明细，便于对比线性、单调和排序一致性关系。
  - 同步补充节点帮助与工作流节点说明，作为后续替代独立 Pearson / Spearman / Kendall 节点的新入口。

- **隐藏拆分相关性旧节点的新建入口**:
  - `pearson`、`spearman`、`kendall` 标记为兼容节点，不再出现在新建节点列表中，仅保留历史流程执行兼容能力。
  - 节点帮助中的推荐链路改为指向新的 `correlation-analysis`，避免继续引导用户创建旧节点。

- **报告区块支持说明 tooltip**:
  - `report.sections[]` 新增可选 `help` 协议，可描述区块含义、阅读方式和注意事项。
  - `report-viewer` 在报告区块标题旁显示说明图标，并通过 tooltip 展示说明内容，后续算法结果可逐步补齐解释文案。

- **拆分数据清洗节点并收敛职责边界**:
  - 新增 `去重`、`缺失/异常值处理`、`编码/缩放` 三个 Action 节点，替代单一“大而全”的数据清洗入口。
  - 三个新节点中字段选择参数统一从 `tags` 切换为 `multi-options`，保持可从上游字段选择并支持手动输入。
  - 旧 `data-cleaning` 节点保留为兼容节点用于历史流程执行，新建入口与 AI 编排目录默认使用拆分后的新节点。

- **缺失/异常值处理新增手动区间规则模式**:
  - `outlierMethod` 新增 `manual_range`，支持按多组规则配置“多字段 + 上下限”过滤，组间与组内字段均按 AND 生效。
  - 每组规则支持手动多选字段并分别配置上下限；上下限留空时表示不设限，区间判断为 `> 下限` 且 `< 上限`。
  - 同步更新节点帮助文档、AI 节点目录与推断配置逻辑，支持从自然语言识别区间/阈值意图。

- **隐藏数据体检节点的新建入口**:
  - `data-profiling` 标记为兼容节点，不再出现在节点侧边栏的新建列表中，仅保留历史流程执行兼容能力。
  - 同步清理节点帮助中的默认推荐链路，避免继续引导到已隐藏的体检节点。
  - AI 节点目录不再暴露 `data-profiling`，内嵌 JSON 快速规划在缺少数值字段时改为回退到最小预处理流程提示。

- **新增上游字段质量筛选助手**:
  - `multi-options` 字段选择器在上游字段具备质量指标时新增“字段质量筛选”入口，可按完整率或缺失率阈值快速缩小候选字段范围。
  - 字段质量筛选与原有文本 / 正则筛选独立叠加，支持先按完整率 / 缺失率筛选，再在命中字段内继续搜索和全选。
  - 节点配置弹窗会为上游字段补充缺失率与完整率指标，便于字段选择、Pearson 分析等节点复用数据透视结果。

- **增强结果弹窗数据透视能力**:
  - `table-chart-combo-viewer` 在“图表 / 表格 / 分屏”之外新增“数据透视”模式，可在结果详情中快速查看行数、字段数、缺少字段数、最高缺少率与数值字段数。
  - 新增数据质量统计层，支持按字段计算缺少数、缺少率、非缺少数、最小值、最大值与均值，并按 `0%`、`0-10%`、`10-30%`、`30-60%`、`60%+` 展示缺少率分布。
  - 数据透视支持按给定缺少率阈值展示命中字段全集，便于在数据清洗前快速定位需要处理的字段。

- **增强看板默认 mock 测试数据**:
  - 默认看板桥接的因子全集扩充为覆盖涂布、辊压、分切、卷绕、装配、注液、化成、分容、PACK 的 40 个因子。
  - 工序运行时输入在默认 mock 桥接下支持未选择产品名时先展示本地候选，方便本地快速测试启动参数弹窗。

- **升级看板 SN 列表导入体验**:
  - `SN 列表`输入改为默认使用大文本编辑器，并增加 CSV / Excel 文件导入入口。
  - 导入弹窗支持选择 SN 列、默认跳过空值并去重，右侧自动展示虚拟化 SN 预览。
  - 保持看板节点执行协议不变，确认导入后仍回填为一行一个 SN 的文本配置。

- **新增 Node API JWT 鉴权接入**:
  - 新增独立 `jwtAuth` 鉴权模块，并基于 `jose` 标准库校验 HS256 Bearer JWT；配置 `WORKFLOW_JWT_SECRET` 后会保护所有非 `OPTIONS` Node API。
  - JWT payload 默认通过 `w3Account` / `cnName` 解析当前用户，并覆盖客户端用户头，服务端存储继续按解析用户隔离数据。
  - 新增前端 API token 适配入口，统一为存储、分析、AI 编排和 Agent 请求注入 `Authorization`，并补充 `jose` 交叉验证测试与部署/工作流文档。

- **新增二分类 / 多分类因子分析能力**:
  - 前端新增单因素筛查节点，支持数值型、分类型因子与二分类 / 多分类标签的常见检验与排序展示。
  - 后端新增逻辑回归分类节点，支持分类建模、混淆矩阵、ROC 与系数结果展示。
  - 同步更新节点帮助、工作流说明与 `xgboost-shap` 文案，明确职责边界。

### 重构 (Refactor)

- **收敛 Node 后端装配层、存储服务与 MCP 依赖边界**:
  - 新增 `storageService`、`storageCompositionRoot`、`serverDependencies` 与共享存储 DTO，统一服务端存储读写、用户解析和仓储装配，避免路由与运行时直接依赖隐式全局实现。
  - 将 `app.ts` 拆回轻量装配入口，新增 `http/` 与 `modules/` 分层，统一请求上下文、响应、错误处理与各业务域路由组织。
  - 统一 `x-workflow-user-id`、`x-workflow-user-name`、`x-workflow-session-id` 请求头，补齐 MCP runtime 注入与后端相关测试，并更新 Node 后端架构整改清单。

### 修复 (Fix)

- **修复工作流 server 存储中文用户头导致保存失败的问题**:
  - 前端 server 存储请求在写入 `x-workflow-user-id` 与 `x-workflow-user-name` 前统一做百分号编码，避免中文用户名触发浏览器 `fetch` 的 header 字符集限制。
  - 服务端存储用户解析链路统一解码上述请求头，保持工作流列表、保存、版本、回滚与历史接口的用户隔离语义不变。
  - 补充 server 存储客户端与服务端用户解析回归测试，并同步更新工作流系统文档。

- **修复图表分析因子搜索后全选会覆盖已选项的问题，并新增一键清空能力**:
  - 新增通用组件 `SearchAppendMultiSelect`，在搜索态触发全选时改为与当前选中项合并，避免覆盖既有选择。
  - `DataChart` 的“分析因子”选择器切换为通用组件，并提供清空图标按钮用于一键取消全部选中项。
  - 补充 `DataChart` 相关单测，覆盖“搜索全选追加”与“清空全部选中”行为。

- **将 MySQL 工作流存储重构为 Drizzle 管理并隔离集成测试库**:
  - 将 MySQL storage 从集中式手写 SQL 重构为 `schema`、`client`、`serialization`、`repositories`、`migrator` 分层结构，引入 Drizzle schema 与迁移文件管理表结构，服务启动时仅校验 schema 是否已就绪。
  - 新增 `pnpm db:mysql:generate` 与 `pnpm db:mysql:migrate`，部署文档与工作流系统文档同步改为“先建库、再执行迁移”的使用方式。
  - 新增 MySQL 序列化与路由持久化测试，并将 MySQL 集成测试默认保持显式开启；测试库默认自动映射到 `*_test`，避免再清理开发库数据。

### 新增 (Added)

- **新增 pm2 统一进程管理配置**:
  - 新增 `ecosystem.config.js`，支持通过 pm2 统一管理 Node 和 Python 两个服务，自动检测平台选择正确的 venv 可执行文件路径。
  - 新增 `deploy/scripts/start_pm2.sh` 和 `deploy/scripts/stop_pm2.sh` 辅助脚本。
  - pm2 日志输出到 `deploy/logs/` 目录。

### 修复 (Fix)

- **跨平台 venv 路径支持**:
  - `start_python.sh` 和 `setup_python_env.sh` 新增操作系统检测，在 Windows (Git Bash) 上使用 `Scripts/` 目录，在 Linux/macOS 上使用 `bin/` 目录，修复 Windows 环境下虚拟环境启动失败的问题。

- **系统性修复结果看板标准网格下图表预览被卡片高度遮挡的问题**:
  - 结果看板标准网格改为分层最小高度策略，普通结果卡片至少保留 `360px`，`chart` / `table` / `tableCollection` 等图表优先型结果卡片至少保留 `420px`，避免头部、摘要与工具栏挤压后图表绘图区塌陷。
  - `chart-viewer`、`table-chart-combo-viewer`、`table-collection-preview` 与 `json-viewer` 收敛为统一的全高嵌入布局，移除重复消耗空间的外层 padding/card 壳体，稳定结果看板与详情弹窗中的高度传导链路。
  - 新增 `ChartViewer`、组合预览与结果看板回归测试，并在 `工作流系统.md` 中补充 viewer 嵌入布局约束，防止后续新增 viewer 再次引入同类遮挡问题。

- **修复结果看板全屏模式下 viewer 内浮层不可见的问题**:
  - 新增工作流级 `overlay host` 上下文，统一为结果看板全屏容器内的 PrimeVue 下拉、弹窗和自定义 `Teleport` 浮层提供宿主目标，避免 overlay 落到全屏树外后实际打开但不可见。
  - `DataChart`、`TableViewer` 与 `ReportViewer` 改为消费统一宿主，覆盖分析因子下拉、图表类型切换、表格列菜单、批量列宽多选和报告整图预览弹窗。
  - 补充相关回归测试，并在 `工作流系统.md` 中新增全屏浮层宿主管理约束，防止后续 viewer 再次直接挂载到 `body`。

- **优化 `options` 与 `multi-options` 在大选项集下的卡顿问题**:
  - 为单选 `Select` 与多选 `MultiSelect` 启用 PrimeVue 原生虚拟滚动，降低大列表下拉展开时的 DOM 渲染开销。
  - 将多选字段的筛选全选状态计算改为基于单次扫描与 `Set` 查找，并在闭合态改用“已选数量”汇总文案，减少大量已选标签带来的渲染负担。
  - `usePropertyFieldOptions` 改为按需克隆选项对象，仅在需要禁用提示或补齐手工输入值时创建新对象，降低大数组响应式复制成本。
  - 补充 `PropertyField` 与 `usePropertyFieldOptions` 回归测试，覆盖虚拟滚动参数、多选汇总文案与增量补齐手工值行为。

- **修复本地文件导入后台解析时 worker 参数克隆失败的问题**:
  - `file-import` 后台解析任务在发送给 Web Worker 前，改为先将解析选项归一化为可结构化克隆的普通对象，避免 `excludeFields` 等响应式数组触发 `postMessage` 克隆异常。
  - 补充文件导入后台任务回归测试，覆盖响应式 `excludeFields` 传入时仍可正常启动 worker 解析的场景。

- **结果预览功能设置改为按节点记忆并统一基于本地存储恢复**:
  - 为结果预览弹窗、组合预览、交互式图表、表格预览和分组表格补充按节点隔离的 `useLocalStorage` 持久化，恢复侧栏折叠、预览条数、图表筛选/归一化/预设、组合模式以及表格列布局、排序和筛选等功能设置。
  - 保持面板展开态、快速搜索词、列搜索词和预设命名输入框等瞬时状态不做持久化，并补充对应回归测试与 `工作流系统.md` 说明。

- **修复结果预览图表多选数值筛选只按任一字段命中的问题**:
  - `DataChart` 的上下限过滤改为对所有已选数值字段同时生效，避免多选对比时仍显示超出范围的值。
  - 补充多字段数值筛选回归测试，并同步调整归一化过滤测试，使其与新的筛选语义保持一致。

- **修复数据预览表格列头三点菜单在弹窗内被遮挡的问题**:
  - 为列头菜单浮层补充高于 `Dialog` 的显式层级，恢复在节点配置与结果预览弹窗中的可见性。
  - 新增三点菜单浮层层级回归测试，并同步修正 `TableViewer` 测试桩，使其覆盖当前 AG Grid 表头按钮的真实触发链路。

- **将结果预览表格重构为 AG Grid 高性能虚拟滚动内核**:
  - `table-preview` 结果 viewer 从 PrimeVue `DataTable` 切换为 `AG Grid Community`，改为整表虚拟滚动预览，显著降低宽表在打开、滚动和关闭阶段的卡顿。
  - 新增表格 grid 模型适配层与 AG Grid 预览组件，保留只读 tooltip 与列宽拖拽，并移除旧分页式 DOM 表格主路径。
  - 重写 `TableViewer` 回归测试并同步更新 `工作流系统.md` 中的 viewer 实现说明。

### 新增 (Feat)

- **为节点悬浮工具栏新增结果预览快捷入口**:
  - 节点 hover toolbox 新增“结果预览”按钮，节点已有输出时可直接打开独立结果预览弹窗，无需先进入节点设置。
  - 工作流画布新增独立 `DataAnalysisModal` 承接节点结果预览，并通过 store 维护当前激活的预览节点状态。
  - 补充 `BaseNode` 与 `WorkflowCanvas` 回归测试，并同步更新 `工作流系统.md` 的结果预览链路说明。

### 修复 (Fix)

- **统一相关性结果预览的正负配色语义**:
  - 将 Pearson / Spearman / Kendall 相关性排行柱状图改为正相关使用深红 `#991b1b`、负相关使用浅蓝 `#60a5fa`，与热力图保持“正红负蓝”一致。
  - 提取相关性热力图与排行图共用的色板常量，避免后续两处颜色定义再次分叉。
  - 补充相关性节点回归测试，覆盖三类相关性报告中的正负条形颜色输出。

- **修复本地开发态服务端模块加载与分析错误提示不清晰的问题**:
  - Vite 开发中间件改为通过 `ssrLoadModule('/src/server/app.ts')` 加载服务端入口，避免源码直载时 `*.js` 导入无法映射到 `*.ts`，修复 `workflowAi/profiles.js` 缺失导致的本地启动报错。
  - Node 分析代理在 Python 算法后端不可达时返回明确的 `502` 中文错误与目标地址诊断，前端分析服务同步透传网络失败和纯文本后端错误，避免页面只显示 `fetch failed`。
  - 补充开发中间件、分析代理和分析服务层回归测试，覆盖 dev 加载链路、后端不可达和错误透传场景。

## [v1.3.0] - 2026-04-16

### 新增 (Feat)

- **补齐 workflow MCP 的节点发现、工作流搭建与调试工具链**:
  - 新增 `workflowMcpRuntime` 与 `planExecution`，支持节点搜索、节点属性选项解析、增量/整包工作流写入、单节点调试、完整执行记录查询与基于会话数据源的计划执行。
  - `workflowMcpServer` 扩展 `workflow_*` 工具集合，并补充执行结果、tool trace、raw message 与 parse failure 调试留痕，便于追踪模型是否真实调用 MCP。
  - 服务端安全节点目录补充 `data-merge` 等关键节点元数据，支持 MCP 侧节点搜索、运行时要求检查与属性选项探索。

### 修复 (Fix)

- **修复 opencode agent 与 workflow MCP 的服务端构建阻塞问题**:
  - `workflowMcpRuntime` 改为依赖服务端安全节点目录，避免将整套前端节点定义卷入 `type-check:server` / `build:server` 的 NodeNext 编译。
  - 修正 gateway 的 workflow MCP 权限放行顺序，并为 `createOpencodeServer` 显式增加启动超时，降低 Windows 环境下的本地 `opencode serve` 启动超时风险。
  - 修复离线 HTML 报告导出、结果面板测试与导出 viewer 的类型问题，恢复 `pnpm build` 全链路通过。

### 测试 (Test)

- **补充 workflow MCP 与服务端安全节点目录回归测试**:
  - 新增节点目录静态选项、上游字段推导与 `data-merge` 暴露断言。
  - 覆盖 workflow MCP 节点发现、节点属性选项解析、单节点调试与 gateway 启动参数回归场景。

### 文档 (Docs)

- **新增 workflow MCP 评测题集并同步系统文档**:
  - 新增 `docs/evaluations/workflow-mcp-evaluation.xml` 作为 MCP 能力评测题库。
  - 同步更新 `工作流系统.md` 中的 workflow MCP 能力说明与调试链路描述。

### 优化 (Improve)

- **优化树形字段与参数面板的嵌套滚动体验**:
  - `PropertyFieldTreeInput` 新增智能滚轮接管：树内可滚时优先滚树，到顶/到底后再把剩余滚动交给外层参数区。
  - 当树内容不足以产生内部滚动条时，鼠标停留在树区域也可直接滚动外层容器，避免“滚不动参数区”。
  - `neighbor-system` 的“场景选择”树改为更紧凑的视口高度，保留“因子全集”作为主操作区。

### 新增 (Feat)

- **为树形字段搜索框新增正则搜索开关**:
  - `tree` 字段搜索框新增 `.*` 图标开关，允许在普通包含搜索与正则搜索之间切换。
  - 正则模式下按表达式匹配树节点搜索文本，并保持现有展开与筛选选择行为不变。
  - 当正则表达式无效时，界面会显示中文错误提示，并返回空结果。

### 优化 (Improve)

- **优化结果视图首开加载性能**:
  - 将工作流结果 viewer 注册改为同步静态导入，减少结果详情弹窗与结果看板首次打开时的异步等待。
  - 移除 `DataAnalysisModal` 中“正在加载结果视图”的按需加载占位，直接渲染命中的结果 viewer。
  - 补充结果详情弹窗与结果看板的同步渲染回归测试，并同步更新 viewer 注册与加载策略文档。

### 修复 (Fix)

- **重构分析报告导出链路并移除 PDF 导出实现**:
  - 将分析报告导出统一切换为单文件离线 HTML，避免旧版 PDF 导出对页面截图、颜色函数兼容和主线程阻塞的依赖。
  - `ReportViewer` 与 `file-viewer` 改为直接生成可离线打开的 HTML 报告，并同步更新节点说明、帮助文案与回归测试。
  - 移除 `html2canvas`、`html2pdf.js` 及相关构建分包配置，减少导出链路依赖与前端打包残留。

- **修复 multi-options 与树形字段在筛选状态下全选/反选会覆盖既有选择的问题**:
  - `multi-options` 的筛选全选改为仅对当前命中的可选项做增量选中或局部反选，不再覆盖未命中的已选项。
  - `tree` 字段在筛选状态下勾选时会保留隐藏叶子节点的既有选择，仅更新当前可见叶子的勾选结果。
  - 补充表单字段回归测试，覆盖筛选后增量全选与保留隐藏已选项场景。

### 修复 (Fix)

- **修复节点设置弹窗的提示遮挡与标题栏误拖拽问题**:
  - 将节点设置“应用”成功提示切换为专用 `Toast` 分组，并固定显示在左下角，避免遮挡右上角关闭按钮。
  - 关闭 `NodeConfigModal` 主弹窗与节点帮助弹窗的标题栏拖拽，避免影响文本双击选中。

- **修复工作流结果预览在大字段 / 宽表场景下的卡顿问题**:
  - 为 `DataAnalysisModal` 与 `DataDisplayPanel` 引入统一的结构化预览序列化层，按行数、列数、字段长度和文本预算分级截断，避免直接渲染超大 JSON 文本。
  - 表格结果改为仅展示有限 `KEYS` 与最多 3 条样本记录，超出部分统一省略提示，降低宽表和长字符串导致的渲染开销。
  - 报告、图表、文件与普通 JSON 结果改为摘要式预览，并保留按需展开的文本预览入口，减少首屏不必要的字符渲染。

### 优化 (Improve)

- **优化数据预览面板的视觉层级与信息密度**:
  - 重绘预览面板为更紧凑的双区卡片布局，收敛顶部摘要区高度，增强关键信息与样本数据的阅读效率。
  - `KEYS` 区改为可滚动字段胶囊列表，移除冗余说明文案，提升窄区域下的适配性和直观性。

### 测试 (Test)

- **补充结果预览序列化与面板渲染回归测试**:
  - 新增结构化预览序列化测试，并覆盖 `DataAnalysisModal` / `DataDisplayPanel` 在宽表、超长文本和超大 JSON 场景下的截断行为。

### 新增 (Feat)

- **完成 server storage 的 lowdb 文件持久化实现**:
  - 将 `src/server/storage.ts` 从进程内 `Map` 存储改为基于 `lowdb` 的文件持久化，并新增 `storageRepository` 统一管理用户目录、工作流文件和历史文件读写。
  - server 模式现按“每用户目录 + 每工作流一个文件 + 单独 history 文件”落盘，支持工作流、版本历史和执行历史在服务重启后继续保留。
  - `/api/storage/*` 与 workflow MCP 相关存储读取链路已同步改为异步持久化访问，保持前端协议不变。

### 测试 (Test)

- **补充 storage 持久化回归测试**:
  - 为 `storageRoutes` 增加服务端模块重载后的工作流/版本/历史恢复测试，覆盖真实落盘场景。

### 文档 (Docs)

- **补充 server storage 部署说明**:
  - 在部署文档、环境变量示例和 `工作流系统.md` 中新增 `WORKFLOW_STORAGE_DATA_DIR`、单实例假设和 lowdb 落盘说明。

## [2026-04-15]

### 优化 (Improve)

- **加固 workflow MCP 内嵌桥接的生命周期、鉴权与可观测性**:
  - 新增 `list_workflow_tools` 工具，统一返回当前 workflow MCP 工具清单，减少 opencode 对 prompt 中硬编码工具名的依赖。
  - `workflow MCP` 现支持可选内部鉴权；当配置 `WORKFLOW_MCP_AUTH_TOKEN` 时，服务端会校验 `x-workflow-mcp-auth-token`，gateway 也会自动透传该请求头。
  - 新增 `GET /api/opencode/workflow-mcp/health`，用于暴露 MCP 鉴权开关、agent session 会话存活数、过期清理计数与工具调用统计，便于排查挂载失败和会话堆积问题。
  - `agentSessionStore` 增加基于 TTL 的过期会话清理和最大会话数淘汰策略，避免长时间运行时进程内会话无限增长。

- **优化分析代理工作台的消息流交互与视觉反馈**:
  - `AgentWorkspace` 增加消息区自动滚动与滚动条样式，生成期间自动保持底部视角，减少长会话中的手动定位成本。
  - `AgentMessageList` 将执行细节收敛为可折叠的 `AgentThinkingBlock`，并重绘用户 / 助手消息气泡样式，强化流式输出、失败态与业务卡片的视觉层级。
  - `AgentComposer`、`AgentProgressBar`、`AgentTimeline` 补充输入区聚焦态、按钮层次和运行中旋转反馈，提升分析代理会话过程的可读性与状态感知。

### 修复 (Fix)

- **修复 opencode agent 会话收尾阶段的消息解析兼容性问题**:
  - `gateway` 在收尾读取 opencode assistant 消息时，改为优先选择可解析的 structured / text 消息；若只拿到 error assistant，则直接透传底层错误，不再统一报“未返回可解析的助手消息”。
  - 对模型偶发返回的字符串型 `findings`、`methods`、`risks`、`recommendations` 做归一化兼容，避免结构化响应因数组字段类型漂移而直接失败。
  - 补充对应网关回归测试，覆盖 timeout 错误透传和字符串列表字段兼容场景。

## [2026-04-14]

### 设计 (Design)

- **补充 opencode 会话化替换设计稿**:
  - 明确分析代理将彻底移除自研 `agent-loop`、`preset`、`iteration` 与自研编排协议，改为由 `opencode sdk` 的 session / message / event / tool 模型统一驱动。
  - 设计新的 `agent session bridge + projection + workflow MCP` 架构，并约束前端改为单列对话式业务流，融合工作流业务态、分析业务态与执行业务态展示。
  - 规划新的 agent API、迁移步骤、异常处理策略与端到端验收标准，作为后续实施依据。

### 优化 (Improve)

- **优化前端构建拆包并收敛构建告警**:
  - 调整 Vite `manualChunks` 规则，拆分 Vue 生态、PrimeVue 主题、Element Plus 与 PDF 导出相关依赖，消除 `vendor-vue` 与 `vendor` 之间的循环 chunk 告警。
  - 将 `html2pdf.js` 改为按需动态加载，避免普通进入工作流页面时提前加载整套 PDF 导出依赖。

### 新增 (Feat)

- **分析代理主链完全切换为 opencode session / message / projection 架构**:
  - 服务端新增 `/api/agent/sessions/*` 会话接口，覆盖会话创建、消息发送、事件流订阅、projection 恢复与画布同步，不再以内置 `agent-loop` 为主路径。
  - 新增 `agentSessionStore` 与 `projection` 映射层，把 `opencode sdk` 原始事件收敛为工作流业务态、分析业务态、执行业务态、画布同步态与错误态。
  - `workflow MCP` 继续作为 agent 的业务能力入口，供 opencode 会话访问当前工作流、节点目录、版本与画布执行能力。

- **分析代理工作台切换为面向数据分析业务的单列对话流**:
  - `AgentWorkspace` 现以单列消息流融合工作流草案、分析结论、执行动作、画布同步与异常信息，不再保留自研 `preset`、`iteration` 和左右割裂的运行 rail。
  - `WorkflowCanvas` 的联动横幅改为直接读取 session/projection 状态，保持与当前多因子分析业务语义一致。
  - `WorkflowAiPanel` 和相关 workflow 侧兼容入口已切到新 session/projection 模型，保留业务摘要、上下文提示和模型配置能力。

### 测试 (Test)

- 补充并更新分析代理工作台、工作流画布、opencode 网关、路由、service 和 store 的回归测试。
- 已验证 `pnpm test:unit`、`pnpm build` 与基于真实 `opencode + glm-4.7` 的服务端会话链路复测通过。

## [2026-04-13]

### 优化 (Improve)

- **优化主界面中节点库与执行记录的层级、布局和收起动画**:
  - 将右侧节点库提升为页面级浮层，始终延伸到页面底部，避免展开执行记录时底部右侧区域出现空白或被执行记录遮挡。
  - 执行记录改为保留全宽容器，仅让内部头部与内容区按节点库宽度动态让位，既不遮挡节点库，也不再产生灰底/白底占位区域。
  - 补充执行记录与节点库联动的过渡动画，让执行记录在节点库收起/展开时同步使用更平滑的位移动画效果。

- **提升结果预览与图表节点箱线图的视觉表现**:
  - `DataChart` 中的单表箱线图与多组箱线图统一升级为更接近参考组件的样式，补齐居中滚动图例、白底描边 tooltip、半透明箱体填充、强调态边框和轻量 dataZoom。
  - `chart-display` 终端节点输出的箱线图同步使用同一套视觉配置，避免节点结果预览与静态图表输出风格割裂。
  - 补充组件与节点定义回归测试，覆盖单表 / 多组箱线图的图例、tooltip、缩放条与箱体样式约束。

### 修复 (Fix)

- **修复数据合并分组集合结果无法正常选择箱线图因子且表格为空的问题**:
  - `tableCollection` 结果在组合预览中改为优先走交互式 `DataChart`，不再被静态 `chartOption` 抢占，从而支持按公共数值字段选择多组箱线图因子。
  - `tableCollection` 的表格页改为使用分组表格 viewer，按组切换查看各来源数据，不再误落到普通表格 viewer 导致空表。
  - 新增 grouped 公共字段推导逻辑，并让节点配置中的上游字段候选也能识别分组集合结果；补充相关组件回归测试覆盖 grouped 图表、表格与字段候选场景。

- **修复数据合并节点横向关联下来源键字段缺少候选项且无法强制输入的问题**:
  - `data-merge` 的“来源键字段”改为根据当前行选择的来源节点，动态读取对应上游表的字段列表，不再错误复用全局上游字段候选。
  - 单选 `options` 字段补齐 `forceInput` 回车写入链路，在候选为空时会提示“可直接输入后按回车添加”，并允许手动录入来源键字段。
  - 补充节点定义与属性字段回归测试，覆盖横向关联来源键字段的动态候选与强制输入场景。

- **为节点库中的多输入节点补充显式标识**:
  - `NodeSidebar` 现在会根据节点定义的 `inputMode` 自动为多输入节点显示“多输入”徽标，位置紧跟在“接入 / 处理 / 输出”分类标签右侧。
  - 该标识会同时出现在主列表、搜索结果以及“最近使用 / 上下文推荐 / 当前可接入”等快捷区卡片中，便于用户快速识别多输入节点。
  - 补充节点库回归测试，覆盖主列表与快捷区中的多输入标识展示。

- **去掉结果预览表格外层留白**:
  - 删除 `TableViewer` 最外层容器的 `p-4`，让表格预览在独立模式和分屏模式下都直接贴合外层边框显示。

- **为图表节点补齐上游字段下拉提示能力**:
  - `chart-display` 的 `xAxis` 与 `yAxis` 参数改为使用上游字段选项源，支持点击输入框后直接下拉选择字段名。
  - 两个字段同时保留可编辑输入，仍可在候选项之外手动输入字段名。
  - 补充节点定义回归测试，确保图表节点持续使用带字段提示的参数类型。

- **完善结果预览表格的列宽与表头交互能力**:
  - 结果预览表格切换为基于 PrimeVue `DataTable` 的列宽管理，支持列宽拖拽、表头高度拖拽、列头文本截断 tooltip 和横向滚动。
  - 左上角新增列宽设置入口，支持搜索多选列、输入列宽后防抖实时生效、步长 `10px` 微调和仅重置列宽不清空已选列。
  - 修复列宽过小时无法生效、输入框光标不可见、面板外点击关闭误伤 `MultiSelect`、拖拽手柄不可发现以及列宽只能放大不能缩小等问题。

- **优化结果预览弹窗右侧视图的首开空白体验**:
  - `DataAnalysisModal` 的右侧结果 viewer 改为通过统一 `Suspense fallback` 显示加载态，避免表格、图表等异步视图在首开时短暂空白。
  - 新增加载态回归测试，覆盖异步 report viewer 尚未 resolve 时的占位提示展示。

- **补充宿主 iframe token 对接文档说明**:
  - 在 `工作流系统.md` 中新增“宿主 iframe 看板 token 对接”小节，明确 `neighbor-system` 嵌入宿主网站后的 token 传递方式、可识别消息格式与排查建议。
  - 在 `工作流节点说明.md` 的看板数据对接节点条目下补充宿主 token 前置要求，并指向系统文档中的详细接入说明。

- **修复构建阶段的测试类型报错**:
  - 更新 `BaseNode` 测试断言，兼容当前 `@vue/test-utils` 对 `get()` 返回值的类型约束，避免因调用不存在的 `exists()` 方法导致 `vue-tsc` 失败。
  - `PropertyFieldSelectButton` 改为接受只读 `options` props，并在传给 PrimeVue `SelectButton` 前显式转换为可变数组，兼容测试中的只读配置对象与组件库内部类型要求。
  - 恢复 `pnpm build` 与 `pnpm vue-tsc --build` 的通过状态。

- **为树形字段补充已选叶子节点数量提示**:
  - `PropertyFieldTreeInput` 在存在已选叶子节点时，会在组件下方显示简洁的 `已选数 / 总叶子数` 提示，例如 `2 / 18`。
  - 未选择任何叶子节点时默认隐藏该提示，避免空状态下增加额外视觉干扰。
  - 补充树形字段组件回归测试，覆盖“已选时显示”和“未选时隐藏”两类场景。

- **修复日期范围字段回填持久化值时的解析报错问题**:
  - `datetime-range` 字段在进入 PrimeVue `DatePicker` 前，新增统一值规范化逻辑，可自动兼容已保存的日期字符串、时间戳和数组形式。
  - 修复节点设置等后续复用场景下，日期范围字段回填历史值时出现的 parse error。
  - 补充表单字段回归测试，覆盖字符串数组回填为日期范围值的兼容场景。

- **修复树形字段选中叶子节点后父层级无法折叠的问题**:
  - 拆分 `TreeV2` 的勾选状态同步与展开状态同步，避免折叠节点时重复回写 `checkedKeys` 导致 checkbox 闪烁。
  - 收起父节点时同时清理其全部后代的展开键，避免 `TreeV2` 根据后代展开状态自动把祖先节点重新展开。
  - 补充树形字段回归测试，覆盖折叠节点时的勾选回写与后代展开键清理场景。

- **修复运行时输入被错误持久化导致看板日期再次打开报错的问题**:
  - 节点设置弹窗中的运行时输入改为仅用于当次查看、填写和调试，点击“应用”后不再写回节点持久化配置。
  - 工作流保存、导出、复制与脏检查快照统一剥离所有 `isRuntimeInput` 字段，避免把运行时值误当成静态节点设置保存。
  - 修复看板数据对接节点在查询日期等运行时输入被保存后，关闭节点设置窗口再次打开时出现日期解析错误的问题。
  - 补充节点配置弹窗与工作流持久化回归测试，并同步更新工作流系统文档中的运行时输入约束说明。

## [2026-04-11]

### 修复 (Fix)

- **增强节点调试界面保存成功时的反馈提示**:
  - 在节点调试弹窗底部点击“应用”后，新增短时“保存成功”提示，明确告知节点配置已应用。
  - 补充节点调试弹窗回归测试，覆盖保存后成功提示的反馈场景。

- **补齐单节点调试运行中的加载反馈与终止入口**:
  - 节点卡片与节点配置弹窗中的“调试节点”入口在单节点调试运行期间会切换为加载态，并禁用重复点击。
  - 调试运行期间新增停止图标按钮，可直接终止当前单节点调试执行。
  - 工作流 Store 新增当前执行上下文标记，用于准确区分单节点调试与全局运行，并补充对应组件回归测试。

- **修复 Pearson / Spearman / Kendall 相关性共用报告的展示控制与排行顺序问题**:
  - 相关性节点将“重点展示因子数”拆分为“热力图显示因子数”和“排行图显示因子数”，并兼容旧 `topN` 配置。
  - 修复小因子数设置不生效的问题，展示数量最小值现在允许为 `1`。
  - 柱状排行图改为从上到下按相关系数从大到小显示，避免顶部仍展示最小值。
  - 热力图右上角新增“显示数值”开关，可在因子较多时关闭格内数值以提升可读性。
  - 同步更新相关性报告 viewer、AI 节点默认配置与工作流文档，并补充回归测试覆盖上述场景。

### 新增 (Feat)

- **为结果预览图表新增归一化趋势对比模式**:
  - `DataChart` 在单表折线对比场景下新增“原始值 / 归一化”切换，并支持 `Min-Max 0~1` 与 `Z-Score` 两种归一方式。
  - 归一化仅影响图表显示层，不改写 `NodeResult` 原始数据；上下限过滤与导出结果继续按原始值口径处理。
  - 归一化视图与方式会写入本地缓存，切换图表 / 分屏模式或重新打开结果预览后仍可恢复上次选择。
  - 补充 `DataChart` 与 `TableChartComboViewer` 回归测试，覆盖归一化切换、过滤口径、状态持久化与分屏切换场景。

### 修复 (Fix)

- **修复数据预览默认分屏且切换模式后图表配置被重置的问题**:
  - `TableChartComboViewer` 默认改为优先显示图表，不再在同时存在表格与图表时默认进入分屏。
  - 图表区与表格区改为常驻挂载并按模式切换显示，避免 `DataChart` 在图表、分屏之间切换时被重新挂载，导致分析因子、采样和过滤条件回退为默认值。
  - 补充组合预览测试，覆盖默认图表模式与切换显示模式后保留图表配置的回归场景。

- **修复启动参数弹窗切换“沿用上次参数”时重置当前表单的问题**:
  - `RuntimeInputModal` 现在在切换“沿用上次启动参数”时只更新复用策略，不再把当前已填写的运行时参数回退为默认值。
  - 补充运行时参数弹窗测试，覆盖切换复用开关后保留当前启动参数的回归场景。

- **修复结果看板全屏模式下单卡详情弹窗不可见的问题**:
  - `WorkflowResultDashboardModal` 在进入全屏后，会将单卡详情弹窗挂载到看板容器内部，避免弹窗落到全屏树外导致不可见。
  - `DataAnalysisModal` 新增 `appendTo` 透传，并补充结果看板与详情弹窗测试，覆盖全屏打开详情与宿主透传场景。

- **将 `multi-options` 的正则搜索改为默认开启**:
  - `PropertyFieldMultiOptionsInput` 现在默认使用正则过滤模式，只有显式传入 `allowRegexSearch: false` 时才回退为普通搜索。
  - 保留现有正则切换按钮交互，并补充表单字段组件测试，覆盖默认启用与显式关闭两类回归场景。

### 新增 (Feat)

- **将规划阶段实时过程拆成独立消息块，强化 Agent 对话流实时感**:
  - 规划阶段的 `attempt`、`stage`、`tool` 与 `stream` 信息不再全部堆叠在单一状态消息中，而是拆成独立过程消息按时间流展示。
  - 工具调用事件现在优先以实时过程消息进入对话流，更接近 Claude Code / Codex 一类 agent coding 工具的交互节奏。

- **收敛 Agent Workspace 对话流结构，减少系统信息重复展示**:
  - 新增轻量 `step_group` 组件，把阶段轨迹直接嵌入消息流，强化 agent-loop 的过程可读性。
  - 移除底部独立结论卡，改为统一通过消息流中的系统卡片呈现最终结论。
  - 右侧工具侧栏改为仅展示活跃或异常工具，已完成工具保留在对话流内，减少双栏重复信息。

- **将系统关键节点卡片正式接入 Agent Workspace 消息流**:
  - 为数据理解完成、待确认信息与画布同步结果补充稳定的 artifact 映射，不再仅依赖普通文本提示。
  - `agentWorkspaceAdapter` 现在会把 schema 摘要、缺失信息与同步结果插入到消息流中，强化类似 agent coding 工具的系统事件展示节奏。

- **补齐 Agent Workspace 服务中间层入口，收敛前端依赖边界**:
  - 新增 `src/services/agentWorkspace` facade，统一承接 Agent Workspace / Agent Loop 相关的模型配置、会话运行、补充输入与自动循环执行调用。
  - `workflowAiStore` 与相关测试改为依赖该中间层，而不再直接绑定底层 `workflowAi` 服务实现，为后续对接真实 agent-loop 服务与事件流演进预留稳定入口。

- **完成 Agent Workspace A2 混合方案第一轮界面重构与适配层抽离**:
  - 左侧分析代理工作台重构为 A2 混合方案：外层框架采用稳态 SaaS 控制台风格，工具调用块强化为轻终端日志感，系统关键节点切换为产品化白底卡片。
  - 新增紧凑顶部进度条、模型设置弹窗与输入区图标化控制，移除大卡片式状态区，并修复工具侧栏空白占位导致的突兀矩形间隔。
  - 抽离 `agentWorkspaceAdapter` 作为中间层，统一承接原始 Agent Loop 事件到 UI 消息流、工具块、系统卡片和时间线的映射，为后续对接真实 agent-loop 服务做准备。
  - 右侧执行画布新增 Agent Loop 执行横幅，补充 Agent Workspace 与 WorkflowCanvas 的回归测试，并增加后续实现计划文档以继续推进统一事件流渲染。

### 文档 (Docs)

- **同步 AgentLoop benchmark 的默认模型配置与系统文档**:
  - 将示例环境变量与部署说明中的默认兼容端点更新为 `https://open.bigmodel.cn/api/coding/paas/v4`。
  - 在工作流系统文档中补充分析代理自动闭环运行接口、Agent Loop 流式事件与结果约束说明。

### 修复 (Fix)

- **稳定 AgentLoop benchmark 的结论与清洗收敛行为**:
  - 服务端自动执行补齐 `data-cleaning` 新版配置名兼容，支持 `deduplicationMode` 与 `missingValueStrategy`。
  - 结论生成阶段在模型返回空摘要或弱结构化结果时回退到带执行上下文的摘要，避免 benchmark 因关键词丢失误判失败。

### 新增 (Feat)

- **补齐 AgentLoop benchmark 的服务端规划与执行链路**:
  - 新增服务端安全节点目录与基准脚本，支持基于真实模型配置执行 `base` / `adversarial` benchmark。
  - 扩展工作流 AI 的计划、诊断与 AgentLoop 协议，补齐流式事件、服务端接口与前端消费链路。
  - 优化工作流规划提示策略与 BigModel coding endpoint 的请求兼容行为，降低规划阶段空返回带来的不稳定性。

## [2026-04-09]

### 修复 (Fix)

- **修复看板工序自动回填与树形字段父节点混入的问题**:
  - 为 `neighbor-system` 的工序 `multi-options` 增加“依赖刷新后自动全选”能力，在产品切换后自动回填全部可选工序，同时保留已有手动选择不被覆盖。
  - 修复 `PropertyFieldTreeInput` 在多选勾选状态包含父节点时错误输出父节点 key 的问题，统一只向外保留叶子节点的 `selectedKeys` 与 `values`。
  - 补充对应表单字段与树形字段组件测试，覆盖自动全选、已有值保护和父节点过滤等回归场景。

- **优化节点调试失败时的错误可见性与重试入口**:
  - 在节点调试弹窗右侧输出区顶部新增失败诊断卡，仅在节点执行报错时显示，减少正常态界面打扰。
  - 诊断卡直接展示当前节点的可读错误信息，并提供“重新调试”“重跑上游后调试”“查看执行日志”快捷操作。
  - 为工作台补充日志区展开事件联动，并新增节点调试弹窗定向测试，覆盖错误卡渲染与日志跳转行为。

- **统一树形字段组件的 modelValue 协议并补充组件说明**:
  - `PropertyFieldTreeInput` 不再根据节点是否提供 `data.value` 切换 `modelValue` 结构，统一输出 `{ selectedKeys, values }`。
  - 组件内部保留对旧版 `TreeSelectionKeys` 与 `{ selectedKey, value }` 结构的读取兼容，避免已有配置直接失效。
  - 为组件补充中文使用说明，并同步更新 `neighbor-system` 对树形配置值的解析逻辑与相关测试。

- **修复树形字段切换到 Element Plus Tree V2 后无法展开与勾选的问题**:
  - 为 `TreeV2` 显式补充 `props.value = 'key'`，让组件内部节点主键与现有树数据结构对齐。
  - 修复内部节点 key 丢失后导致的展开、勾选和选中状态同步失效问题。
  - 补充树形字段定向测试，防止后续再次遗漏 `key` 映射。

- **限制树形字段安全展开与搜索超限渲染，避免大树卡死**:
  - 将树形字段的“完全展开”调整为“安全展开”，为展开键数量增加预算限制，避免 2000 项三级树被一次性全部挂载。
  - 搜索结果过多时改为保留命中分组、预算内命中项与摘要提示节点，避免搜索高命中场景直接渲染整棵命中树。
  - 搜索态下手动展开父节点时仅展示符合搜索条件的子树，不再回退显示未命中的原始子节点。
  - 合并树节点索引与对象值模式探测的遍历，并将展开状态改为浅响应对象，降低额外响应式开销。

## [2026-04-08]

### 修复 (Fix)

- **修复树形字段测试对内部规范化字段过度敏感的问题**:
  - `PropertyField` 树形字段测试从严格全量相等调整为结构匹配，允许节点搜索增强逻辑附加内部元数据而不影响行为断言。

### 修复 (Fix)

- **修复分析代理展开后工作流画布与节点库布局异常的问题**:
  - 移除右侧执行工作区标题与结果 / 报告切换壳层，恢复右侧原始画布区结构，确保未打开代理时保持既有工作流体验。
  - 修复 `WorkflowCanvas` 高度链路塌陷导致的节点库不可见问题，并在窄屏下改为左侧覆盖式代理面板，避免把右侧工作流区挤出视口。
  - 优化顶部工具栏响应式约束，消除窄屏场景下整页横向偏移。
  - 调整节点库收起动画为向右拖出并渐隐，恢复更贴近原设计的过渡体验。

### 新增 (Feat)

- **新增聊天式分析代理工作台第一版，并兼容回退到原画布体验**:
  - 新增 `analysis-agent` 前后端兼容接口与前端服务封装，复用既有 `workflow-ai` session 主链承接分析代理会话。
  - `workflowAiStore` 新增面向工作台的派生状态，支持消息流、阶段时间线、工具调用、结论卡片与执行区 Tab 状态。
  - 新增 `components/agent/` 组件组，落地聊天消息、工具卡、思考块、审批卡和输入区等 agent 界面基础能力。
  - `WorkflowCanvas` 升级为“左侧 Agent Workspace + 右侧 Execution Workspace”的工作台容器，并确保未打开代理时保持原有单画布 UI/UX。
  - 同步更新相关回归测试、系统文档与设计规格文档，覆盖默认旧布局、代理展开后新布局以及执行区 Tab 行为。

### 新增 (Feat)

- **看板数据对接节点新增场景树选择与对象值协议**:
  - `neighbor-system` 节点新增“场景选择”树形单选，配置链路调整为“产品 -> 场景 -> 因子全集”。
  - `KanbanDataBridge` 新增 `listScene`，并让因子目录与工序候选支持按已选场景加载。
  - 树形属性组件新增对象值包装协议，支持单选输出 `{ selectedKey, value }`、多选输出 `{ selectedKeys, values }`。
  - 看板数据获取参数改为直接透传因子 `val[]` 与场景对象，补充集成层、节点定义与工作流运行回归测试。

### 修复 (Fix)

- **优化 `PropertyField` 树形字段搜索性能并抽取复用逻辑**:
  - 树形搜索新增 `150ms` 防抖，减少连续输入时对整棵树的重复过滤与展开计算。
  - 预先缓存节点标准化搜索文本，并将过滤结果树与展开键收敛到同一轮遍历中生成，降低大树搜索时的热路径开销。
  - 新增 `usePropertyFieldTreeSearch` 复用逻辑与独立测试，保持树形字段组件现有单选、多选和对象值协议行为不变。

## [2026-04-07]

### 修复 (Fix)

- **修复 Pearson / Spearman / Kendall 报告在字段较多时纵向内容过长的问题**:
  - 为 `分析摘要`、`结果可信提示`、`X / Y 字段相关明细` 三个区块补充统一折叠能力。
  - `分析摘要` 与 `结果可信提示` 默认保持展开，便于直接查看结论与风险。
  - `X / Y 字段相关明细` 默认改为折叠，减少大字段集场景下的首屏占用。
  - 补充 `ReportViewer` 回归测试，覆盖默认展开状态与手动展开/收起交互。

- **修复带启动参数的工作流在取消输入时仍显示已停止并弹出结果看板的问题**:
  - 为运行时参数弹窗新增统一取消执行入口，避免前端直接清空 `pendingExecution` 后遗留全局运行收尾状态。
  - 全局运行在“启动参数弹窗主动取消”场景下不再生成 `lastRunDashboard` 与执行历史，也不会弹出结果看板。
  - 补充工作流 store 与 `WorkflowCanvas` 回归测试，覆盖取消输入后的状态回收与弹窗抑制行为。

- **修复 mock 看板数据对接服务下节点设置动态选项为空的问题**:
  - 为默认 mock 看板桥接补充本地兜底访问凭证，未接入宿主 token 时仍可在节点设置中加载产品、工序、物料类型、阶段方案等动态选项。
  - `neighbor-system` 节点统一改为通过解析后的访问凭证读取动态选项，真实宿主桥接场景仍保留缺少 token 时的报错校验。
  - 因子全集树结构由 `scene -> process -> factor` 调整为 `process -> factor` 两层，移除固定单一场景的冗余首层展示。
  - 补充 mock 看板选项回归测试，并同步更新看板集成与节点定义测试桩。

- **修复 `PropertyField` 树形字段搜索后节点未自动展开到底层的问题**:
  - 为树形搜索结果新增自动展开逻辑，命中过滤条件后会沿路径展开到最下层叶子节点。
  - 在搜索框右侧新增“完全展开”“完全收起”两个图标按钮，便于快速切换整棵树的展开状态。
  - 补充 `PropertyFieldTreeInput` 定向测试，并回归验证 `PropertyField` 现有 tree 行为未受影响。

- **修复空白工作流中节点库“常用起点”错误推荐非数据接入节点的问题**:
  - 将空白工作流下的静态起点推荐收敛为 `file-import`、`manual-json-import`、`neighbor-system` 3 个数据接入节点。
  - 保持已有“上下文推荐”“当前可接入”逻辑不变，仅修正常用起点兜底集合。
  - 补充 `NodeSidebar` 回归测试，防止后续再次把分析或处理节点误放入空白工作流起点推荐。

## [2026-04-01]

### 重构 (Refactor)

- **工作流模板改为基于 JSON 配置加载，并统一默认使用手动 JSON 输入起步**:
  - 模板展示元数据继续集中在 `src/workflow/templates.ts`，实际工作流骨架改为放入 `src/workflow/template-configs/*.json`，直接复用系统现有工作流导出格式。
  - `workflowStore` 创建模板时改为从 JSON 工作流结构实例化节点和连线，统一重建运行态 id，减少后续新增模板的维护成本。
  - 内置模板默认入口统一切换为 `manual-json-import`，并补齐最小样例数据与关键字段配置，保证模板创建后可直接运行。
  - 工作流管理中心新增“数据入口建议”提示，明确手动 JSON 输入可替换为文件导入或看板数据获取节点。
  - 同步更新模板回归测试与工作流系统文档，补充模板 JSON 协议和默认数据源约束。

### 新增 (Feat)

- **新增工作流分析模板体系，支持从模板一键创建工作流**:
  - 新增统一模板注册表，内置相关性排查、关键因子筛选、变量解释分析、看板数据对比分析 4 个模板。
  - `workflowStore` 新增从模板创建未保存工作流的能力，统一负责节点骨架生成、连线恢复和编辑态切换。
  - 在工作流头部和工作流管理中心中新增模板入口，支持直接从模板生成可继续编辑的工作流骨架。

### 修复 (Fix)

- **修复工作流管理中心模板卡片被裁切、下半段内容不可见的问题**:
  - 将“分析模板”改为结果导向的大卡片布局，强化模板核心产出、适合场景和关键节点的层级展示。
  - 放大工作流管理中心宽度，避免模板卡片在弹窗中过度拥挤。
  - 移除模板列表内部的二级滚动容器，改为由弹窗内容区统一滚动，解决模板卡片底部说明和创建按钮被隐藏的问题。

### 重构 (Refactor)

- **拆分 `ReportViewer` 与 `PropertyField` 组件，提升可维护性并保持行为一致**:
  - 将 `ReportViewer` 拆分为报告分节组件、导出逻辑、整图预览逻辑和报告派生逻辑，主组件仅保留装配职责。
  - 将 `PropertyField` 拆分为输入渲染器、选项派生逻辑、正则过滤逻辑与多个独立输入子组件，收敛超长条件分支。
  - 补充回归测试，覆盖 `resolveOptions` 加载失败提示与 `forceInput` 多选字段在已有手工值后的继续输入提示。

### 修复 (Fix)

- **修复历史记录查看时节点结果丢失与结果看板入口缺失的问题**:
  - 进入历史模式时不再清空历史快照中的节点输出、错误与运行状态，支持继续查看单节点历史结果。
  - 历史记录回放时会基于快照重建结果看板摘要，恢复历史模式下的结果聚合查看能力。
  - 历史模式保留“结果看板”入口，但隐藏运行操作；补充 store 与画布层回归测试，覆盖历史结果保留与入口显示场景。

- **优化 SHAP 原始整图预览交互与因子趋势排序**:
  - `ReportViewer` 中的后端原始整图改为独立弹窗预览，支持桌面端滚轮缩放与鼠标拖动画布。
  - 修复缩放比例低于 1 时无法平移的问题，并统一预览区拖动光标反馈。
  - SHAP“因子趋势明细”区块按特征贡献排行顺序展示单因子影响曲线，提升阅读一致性。

- **限制 Xgboost + SHAP 单因子依赖图数量并保持摘要全量展示**:
  - `xgboost-shap` 节点新增“依赖图数量上限”参数，默认值为 8，只限制单因子影响曲线图和完整报告明细区数量。
  - SHAP 蜂群图与特征贡献排行继续保留全量因子展示，不受依赖图数量上限影响。
  - 修复完整报告在限制明细因子数量时的索引越界问题，并补充后端与节点层回归测试。

- **合并 Xgboost + SHAP 报告中的因子趋势区块**:
  - 将 `ReportViewer` 中原本分离的“关键因子趋势”和“完整因子明细”合并为单一“因子趋势明细”区块，减少重复信息。
  - 新区块默认展示前 N 个高重要性因子，同时支持搜索全量因子和一键展开全部，保留完整可访问性。
  - 同步收敛 Xgboost + SHAP 节点报告结构与前端回归测试，移除重复的 `details` section。

- **忽略本地 Python 3.10 虚拟环境目录**:
  - 将 `.venv310/` 加入 `.gitignore`，避免本地算法后端测试环境进入版本控制。

- **修复 Xgboost + SHAP 完整大图重叠及 Python 3.10 环境兼容问题**:
  - 修复完整报告在部分 SHAP 版本下被意外重置为小画布，导致前端“后端原始整图”中的标题、图表和文字全部挤压重叠。
  - 为 SHAP 解释器补充分级回退策略，依次兼容 `shap.Explainer`、`TreeExplainer` 和 `model.predict` 包装路径，解决 Python 3.10 环境下 `xgboost` 与 `shap` 组合运行报错的问题。
  - 新增后端回归测试，覆盖完整报告尺寸稳定性，以及 SHAP 解释器在不同兼容分支下的回退行为。

- **修复 Xgboost + SHAP 节点在本地 Python 环境下的运行兼容性问题**:
  - 修复 `backend/algorithms/xgboost_shap_analysis.py` 在 `python backend/main.py` 启动方式下写死 `backend.algorithm` 导致的导入失败。
  - 为 SHAP 报表生成链路补充 Matplotlib 样式回退，兼容旧版环境中仅提供 `seaborn-whitegrid` 的情况。
  - 为 SHAP 绘图调用补充新旧版本 API 兼容处理，避免 `beeswarm`、`bar`、`scatter`、`dependence_plot` 在旧版 SHAP 下因不支持 `ax` 等参数报错。
  - 新增后端回归测试，覆盖 Xgboost + SHAP 导入回退与绘图兼容分支，并在浏览器端重新验证节点执行成功。

- **修复工作流管理中心只显示当前工作流运行历史的问题**:
  - 工作流管理中心的“运行历史”页签改为统一展示所有工作流的运行记录，不再受当前打开工作流限制。
  - 顶部工具栏中的运行历史弹层继续保持当前工作流视角，避免影响现有快速回溯体验。
  - 补充 `WorkflowManagerModal` 定向测试，覆盖“当前打开工作流与历史归属不一致时仍应显示记录”的回归场景。

## [2026-03-31]

### 修复 (Fix)

- **优化工作流运行历史与日志的内存占用**:
  - 历史快照改为按结果类型直接构造轻量化裁剪副本，避免在写入运行历史时先完整深拷贝大结果造成额外峰值内存。
  - 为 `report` / `chart` 历史结果补充明细项与图表数据裁剪策略，并保留 `historyTruncated` 等元信息用于历史模式展示。
  - 工作流日志区限制为最近 500 条，避免长时间调试或多次运行后日志数组无限增长。
  - 移除执行链路中的固定 300ms 人工等待，并补充中断执行与历史裁剪回归测试。
- **支持树形配置项的单选模式**:
  - `PropertyField` 中的 `tree` 类型新增 `singleSelect` 配置，保持 checkbox 视觉样式不变，但可限制为仅选中一个节点。
  - 补充 `PropertyField` 定向测试，覆盖 tree 默认多选与单选模式的回归行为。

## [2026-03-30]

### 修复 (Fix)

- **修复工作流导出误带运行缓存数据的问题**:
  - 工作流导出 JSON 改为仅包含可导入的结构与节点配置，不再携带任何节点缓存输出。
  - 保留现有保存策略不变：持久化工作流时仍只为冻结节点保留输出，避免影响既有冻结复用能力。
  - 补充 `workflowStore` 回归测试，并同步更新工作流系统文档中的持久化/导出边界说明。
- **修复分析报告 PDF 导出的预渲染与载荷膨胀问题**:
  - `ExportViewer` 改为仅在用户点击导出时临时挂载隐藏报告容器，导出完成后立即卸载，避免初次渲染时提前创建整份报告与图表。
  - `data-export` 节点在生成 `report-pdf` 文件结果时改为写入轻量化 report payload，移除 supplements 中的大图附件，降低结果体积与快照压力。
  - 补充对应回归测试，覆盖导出容器懒挂载/卸载行为，以及 PDF 导出载荷裁剪行为。
- **修复 PDF 导出对 `oklch` 颜色函数的兼容性问题**:
  - 在报告 PDF 导出阶段对克隆 DOM 中的 `oklch(...)` 颜色值做兼容转换，避免 `html2canvas` 在导出相关性报告时报错中断。
  - 补充导出颜色归一化测试，覆盖普通颜色值与带透明度颜色值的转换行为。

### 功能 (Feature)

- **完成工作流 AI Tool-first Session 化编排主链**:
  - 新增 session 化 AI 编排接口 `POST /api/workflow-ai/session/start`、`POST /api/workflow-ai/session/:id/run`、`POST /api/workflow-ai/session/:id/input` 与 `GET /api/workflow-ai/session/:id`，前端主链切换为会话驱动编排。
  - 新增 recipe catalog / search、draft graph、tool-first orchestrator 与细粒度 `mutate_draft` 能力，AI 改为先构建隔离草稿，再 `finalize` 为最终计划。
  - 新增 AI 专用 schema inspector，支持画布缓存读取、服务端无副作用临时执行、draft 分支临时执行，并把字段摘要回灌到 AI 上下文中用于补齐配置。
  - AI 编排面板升级为“编排会话面板”，支持展示当前策略、草稿结构、缺失信息、工具轨迹与继续编排入口。
  - session store 从进程内内存升级为文件持久化实现，并增加 TTL 过期清理；同步补充服务端工具、会话状态、路由、前端 store 与面板的回归测试。
  - 同步更新 `工作流系统.md` 与 `docs/superpowers/specs/2026-03-30-workflow-ai-tool-first-orchestration-design.md`，标记该 spec 首期范围已完成。

- **增强 AI 编排的弱模型稳定性与实时观测**:
  - AI 编排后端改为“两阶段编排”，先生成最小工作流骨架，再补齐已选节点的最小运行配置，降低弱模型一次性输出复杂计划的失败率。
  - 模型可见节点目录收敛为精简提示词专用结构，并补充 JSON 场景优先选择 `manual-json-import`、必填字段不可留空等弱模型约束。
  - 新增“可编辑草案计划”兜底：当配置补全已拿到基本可用结构，但自动修复后仍无法补齐全部必填项时，前端仍可拿到带 `warnings / questions / diagnostics` 的草案计划继续编辑。
  - AI 编排面板新增实时进度、模型实时输出与诊断信息展示，流式接口按阶段持续推送增量事件，减少用户长时间停留在“生成中...”的不确定感。
  - 补充 AI 编排后端、流式路由、前端面板与 store 的回归测试，并同步更新工作流系统文档与弱模型提示词设计说明。

## [2026-03-29]

### 功能 (Feature)

- **重构节点库检索与浏览体验**:
  - `NodeSidebar` 新增类型筛选、任务场景分组、语义搜索命中提示、最近使用与上下文推荐快捷区，提升节点查找效率。
  - 节点库支持基于 `assistantHints.recommendedNextNodes` 的上下文推荐，并在待连线状态优先展示“当前可接入”节点。
  - 任务场景分组卡片与快捷区卡片支持单独收起 / 展开，收起状态仅在当前节点库实例内生效。
  - 为节点定义补齐节点库元数据字段与测试覆盖，并同步更新工作流系统文档与节点说明。

## [2026-03-28]

### 功能 (Feature)

- **增强数据导出节点的分析报告 PDF 能力**:
  - `data-export` 节点新增 `PDF（分析报告）` 导出格式，支持将分析报告结果按需生成阅读版 PDF。
  - 表格结果仍保留 CSV、Excel、JSON 导出；报告导出统一走 PDF，避免导出语义混乱。
  - 补充统一导出文件命名规则、导出成功/失败提示，以及 `file-viewer` 的按需 PDF 生成链路。

- **增强图表展示节点的分布图能力**:
  - `chart-display` 节点新增 `分布图（直方图）` 类型，支持对单个数值字段自动分箱，直接查看样本分布形状。
  - 分布图默认输出区间样本数与占比提示，不新增额外高级配置，保持现有图表配置界面简洁。
  - 补充节点执行测试、标准结果协议测试、节点帮助文案与里程碑进度说明。

- **增强数据聚合节点时间窗口模式**:
  - `data-aggregation` 节点新增 `时间窗口` 模式，保留原有按行 `移动窗口` 能力不变，并新增按固定分钟 / 小时 / 天分桶输出新表的聚合方式。
  - 时间窗口模式支持配置时间字段、窗口长度、时间单位、统计指标和目标字段，输出窗口起止时间、窗口样本数以及各字段聚合结果。
  - 补充节点执行测试、标准结果协议测试、帮助文案与节点说明，并同步更新开发任务进度。

- **增强数据清洗节点去重能力**:
  - `data-cleaning` 节点新增去重方式配置，支持按整行去重或按指定字段去重，并可按当前顺序保留首条或末条记录。
  - 去重能力收敛在现有数据清洗节点内，不新增独立节点，也不增加排序字段配置；若需保留最早或最晚记录，可先接排序节点控制顺序。
  - 补充节点执行测试与标准结果协议测试，并同步更新节点帮助文案与节点说明。

### 修复 (Fix)

- **统一节点调试中运行时输入的卡片层级**:
  - 移除节点调试面板里“运行时输入”外层额外的浅色矩形包裹，只保留内部主卡片。
  - 让“运行时输入”区域的视觉层级与上方“输入数据”保持一致，减少多余嵌套感。

- **统一回归类分析节点的预测拟合图表达**:
  - 将多元线性回归与随机森林特征重要性的“预测值对比”统一为标准回归拟合图，补齐 `实际值 / 预测值` 坐标语义与 `理想拟合线` 参考线。
  - 抽取共享的回归拟合图配置生成逻辑，避免不同分析节点各自维护散点图细节。
  - 补充节点执行测试，锁定回归拟合图的核心图形结构。

- **统一分析节点的因子字段多选配置**:
  - 将 `multiple-linear-regression`、`pca`、`random-forest-feature-importance`、`vif`、`xgboost-shap` 的 `factorNames` 从 `tags` 收敛为 `multi-options`。
  - 统一补齐 `default`、`editable` 与 `forceInput` 配置，使分析类节点的因子选择交互与字段校验提示保持一致。
  - 补充节点定义测试，锁定上述分析节点的 `factorNames` 多选配置约束。

- **修正数据清洗节点去重配置的显示顺序**:
  - 去重相关参数统一收拢到“去重方式”下方显示，避免 `去重字段` 因字段优先级规则提前到上方。
  - 将“保留记录”改为“去重保留方式”，减少与其他清洗配置的语义混淆。

- **收敛节点调试与启动参数界面的说明噪音**:
  - 节点调试弹层将长段说明收敛为 `Help Icon + Tooltip`，保留紧凑的“调试说明”和“相关性分析配置建议”入口，减少主配置区干扰。
  - 运行时输入弹层改为紧凑头部信息与缺失项徽标，沿用参数说明迁移到悬浮提示，不再占用主操作区。
  - 配置表单移除“核心参数 / 补充参数”分组标签，字段级提示仅保留“字段受限 / 缺少上游”等高价值诊断信号；必填继续使用红色 `*`，默认值不再重复提示，并补充对应回归测试。

- **收口分析节点帮助元数据的一致性**:
  - 统一补齐 `lasso`、`multiple-linear-regression`、`random-forest-feature-importance`、`anova`、`vif`、`pca`、`xgboost-shap` 的 `assistantHints` 最小集。
  - 仅增强帮助元数据的可读性与自动编排提示，不新增新的常驻 UI 文案或页面结构，保持主界面简洁。

### 功能 (Feature)

- **新增随机森林特征重要性节点**:
  - 新增 `random-forest-feature-importance` 终端节点，支持选择目标变量、影响因子、树数量与最大深度，输出特征重要性排行、累计重要性、预测值对比与结果解读提示。
  - 新增前端分析请求、Node 代理路由与 Python 算法实现，统一沿用现有 `/api/analysis/*` 算法出口链路。
  - 补充节点执行测试、结果协议测试、Node 路由测试与 Python 算法测试，并同步更新节点说明、部署文档与里程碑进度。

- **新增方差分析节点**:
  - 新增 `anova` 终端节点，在前端本地完成单因素方差分析，输出 F 值、P 值、分组均值对比、分组箱线图与分组明细。
  - 复用现有通用 `report-viewer` 展示链路，不新增专用 viewer 协议。
  - 补充节点执行测试、结果协议测试，并同步更新节点说明与里程碑进度。

- **新增 PCA 主成分分析节点**:
  - 新增 `pca` 终端节点，在前端本地完成主成分分析，输出分析摘要、解释方差占比、字段载荷热力图与载荷明细。
  - 复用现有通用 `report-viewer` 展示链路，不新增专用 viewer 协议。
  - 补充节点执行测试、结果协议测试，并同步更新节点说明与里程碑进度。

- **新增多元线性回归分析节点**:
  - 新增 `multiple-linear-regression` 终端节点，支持选择目标变量与影响因子，输出模型摘要、回归系数排序、预测值对比与残差分布。
  - 新增前端分析请求、Node 代理路由与 Python 算法实现，统一沿用现有 `/api/analysis/*` 算法出口链路。
  - 补充节点执行测试、结果协议测试、ReportViewer 回归测试与 Python 算法测试，并同步更新节点说明与里程碑进度。

- **新增 VIF 共线性检测节点**:
  - 新增 `vif` 终端节点，在前端本地完成多重共线性诊断，输出 VIF 排序、风险提示与字段明细。
  - 复用现有通用 `report-viewer` 展示链路，不新增专用 viewer 协议。
  - 补充节点执行测试、结果协议测试，并同步更新节点说明与里程碑进度。

### 文档 (Docs)

- **补齐 P0 收口测试与工作流文档说明**:
  - 为工作流 UI 文案与空状态补充回归测试，覆盖节点配置弹窗、节点卡片和字段空状态提示。
  - 在 `工作流系统.md` 中补充单节点调试策略说明，明确“调试节点”与“重跑上游后调试”的差异及续跑约束。
  - 同步更新 P0 / Milestone 文档中的完成状态，收口 `P0-7` 测试与文档任务。

## [2026-03-27]

### 修复 (Fix)

- **节点调试三栏布局支持横向拖拽并优化分割线观感**:
  - 节点调试界面左、中、右三栏之间新增可拖拽分割线，支持分别调整输入区、设置区和输出区的横向宽度。
  - 分割线视觉改为顶格全高的精简细轨与中段刻痕样式，减少多层装饰叠加，提升整体精致感。
  - 在 `AGENTS.md` 中补充约束：纯 UI / 视觉样式优化默认不要求执行 TDD 测试，优先采用最小必要验证。

### 修复 (Fix)

- **完善看板数据对接节点与运行时输入样式**:
  - 看板数据对接节点改为显式选择工序，节点配置与启动参数改为通过中间层适配后的领域数据构建产品、因子、方案与工序选项。
  - 切换产品时自动重置因子、工序、方案、任务令类型与物料类型等依赖字段，避免旧配置残留污染后续查询。
  - 运行时输入区域统一增加白色卡片与父级分层容器，提升与弹层背景的区分度，并同步补充相关回归测试。
  - 启动方式选择卡片收敛为更紧凑的查询策略样式，移除多余说明文案，降低视觉噪音。

### 杂项 (Chore)

- **新增通用 Linux 半自动部署目录**:
  - 新增 `deploy/` 目录，集中提供前端/Node/Python 的部署手册、环境变量模板、健康检查脚本、构建脚本与半自动发布脚本。
  - 新增 Nginx 与 systemd 模板，便于在非 Docker 的 Linux 环境中快速落地部署。

### 修复 (Fix)

- **Node 统一代理 Python 算法接口**:
  - 为 `Lasso` 与 `Xgboost + SHAP` 节点新增 Node 侧 `/api/analysis/*` 代理接口，前端不再直接请求 Python 服务。
  - 新增前端算法服务层，统一通过 `VITE_API_BASE_URL` 访问 Node API，并由 Node 使用 `PYTHON_ANALYSIS_API_BASE_URL` 转发到 Python 算法后端。
  - 补充 Node 路由测试、算法节点请求路径断言，以及 Node + Python 的部署手册与工作流系统文档说明。

### 修复 (Fix)

- **节点调试弹层期间画布快捷键误触修复**:
  - 修复打开节点调试界面后按 `Backspace` 会误删底层选中节点、导致界面空白的问题。
  - 在节点配置、运行时输入、结果看板、工作流管理、帮助中心与未保存确认等前台弹层打开期间，统一禁用画布删除、框选、多选、缩放和平移相关键盘快捷键。
  - 补充 `WorkflowCanvas` 回归测试，覆盖节点调试弹层打开时画布快捷键应被禁用的场景。

### 杂项 (Chore)

- **清理 Prettier 独立配置并同步开发记录**:
  - 删除仓库根目录下的 `.prettierrc.json`，减少独立格式化配置维护成本。
  - 同步更新自动生成的导入类型相关文件。
  - 补充 `想法.md` 中的工作流交互与结果看板后续事项记录。

## [2026-03-25]

### 功能 (Feature)

- **相关性分析节点支持 X/Y 交叉计算与结果交互优化**:
  - 将 `Pearson`、`Spearman`、`Kendall` 三个相关性节点的配置从单一 `targetField` 调整为 `X 字段 + Y 字段` 多选模式，只计算所选字段的交叉相关。
  - 相关性热力图改为仅展示选中的 `X × Y` 范围，并将热力图 `visualMap` 移到图表上方，避免遮挡底部标签。
  - 相关性排行图改为支持在结果区按 `Y` 字段切换，并新增坐标轴标签截断位数输入，便于处理超长因子名称。
  - `report-viewer` 新增轻量图表控件协议，支持报告分节内的下拉切换与坐标轴标签截断交互。
  - 同步更新相关节点帮助文案、工作流系统文档与节点说明文档。
  - 补充热力图分节的标签截断交互，让热力图与排行图都支持在结果区单独控制轴标签截断长度。

### 功能 (Feature)

- **数据合并横向关联并入字段合并能力**:
  - 将原“字段合并”节点的按来源键宽表合并能力并入 `数据合并` 节点的“横向关联”模式。
  - `数据合并` 在横向关联模式下改为使用 `统一键名称 + 每个来源一个来源键字段` 的配置协议，并移除各来源原始 key 字段。
  - 删除独立的“字段合并”节点定义、注册、帮助说明与相关测试，统一节点入口与用户心智。

### 修复 (Fix)

- **节点参数配置应用按钮交互修复**:
  - 将节点参数配置底部按钮文案从“应用并保存”调整为“应用”。
  - 点击“应用”后仅保存当前节点配置，不再自动关闭节点配置弹窗。
  - 补充 `ConfigFooter` 与 `NodeConfigModal` 回归测试，覆盖按钮文案与“保存不关闭”的行为。

- **Lasso 节点真实化与相关回归修复**:
  - 新增 Python 后端 `Lasso` 分析接口，将前端 `Lasso 回归` 节点从模拟结果切换为真实计算。
  - 后端拆分为基础回归数据准备、算法专属 pipeline 与独立接口入口，避免 `main.py` 导入时强依赖 SHAP 运行栈。
  - 前端 `Lasso` 报告改为展示模型摘要、特征系数排序、正则路径与入选特征，并补充前后端回归测试。
  - 修复 `workflowAiProfiles` 测试对环境变量的隐式依赖、`WorkflowHeader` 测试事件名断言错误，以及 `NodeConfigModal` 多输入调试模板的类型收窄问题。

- **节点调试默认输入模板协议修复**:
  - 修复节点调试界面左侧“输入数据”中的“生成模板”仍输出旧版 `data` 包装结构的问题。
  - 单输入节点现在生成标准 `NodeResult` 模板，多输入节点生成 `{ inputs[].result }` 标准执行协议模板。
  - 补充节点调试模板生成回归测试，覆盖单输入与多输入两类场景。

- **结果预览大 JSON 全局托底修复**:
  - 为左侧结果预览序列化增加统一预算保护，限制遍历节点数与预估输出体积，避免超大 JSON 在 `DataAnalysisModal` 中触发主线程卡顿。
  - 为根层超大普通对象增加快速降级路径，超过预算后直接返回 `budgetExceeded` 安全占位摘要，不再继续深度展开。
  - 补强 `report` 结果的预览兜底范围，`meta`、`lineage` 与 `preview.props` 也会参与安全序列化，修复 Pearson 节点输出仍可能在左侧预览中完整展开超大 JSON 的问题。
  - 补充 `previewSerialization` 与 `DataAnalysisModal` 回归测试，覆盖超大普通对象的全局托底场景。

- **AI 编排入口遮挡节点库修复**:
  - 将 `AI 编排` 入口从右侧悬浮按钮迁移到顶部工具区，避免覆盖节点库中的节点卡片内容。
  - 顶部入口支持在“AI 编排 / 收起 AI”之间切换，并保留右侧 AI 面板原有展开交互。
  - 移除底部浮动区旧的 AI 入口，并补充顶部入口打开 AI 面板的回归测试。

- **节点库图标视觉比例优化**:
  - 调整 `NodeSidebar` 节点卡片中的图标区域，缩小白色外框留白并放大节点图标主体。
  - 改善节点库中图标偏小、白框内边距过多的问题，提升节点识别效率与整体观感。

- **切换工作流后结果看板残留状态修复**:
  - 修复运行过一份工作流后切换到新的空白工作流时，“结果看板”按钮仍沿用上一份工作流摘要继续显示的问题。
  - 当 `lastRunDashboard` 被清空时，`WorkflowCanvas` 现在会同步关闭结果看板并清空本地摘要，避免跨工作流保留旧运行结果状态。
  - 补充切换新工作流后结果看板按钮与摘要应立即消失的回归测试。

- **运行时输入与工作流管理弹窗位置优化**:
  - 调整 `RuntimeInputModal` 与 `WorkflowManagerModal` 的遮罩层垂直对齐方式，弹窗不再默认严格居中显示。
  - 为两个弹窗增加更靠上的顶部留白，优化首次打开时的视觉重心与内容阅读起点。

- **工作流结果看板展示空间与全屏专注模式优化**:
  - 为 `WorkflowResultDashboardModal` 增加全屏入口，并在全屏后默认进入专注模式，自动收起“节点选择”并隐藏运行概览。
  - “节点选择”面板支持折叠为窄侧栏，顶部与工作台工具栏间距同步收紧，优先为右侧聚合分析结果释放展示空间。
  - 全屏内容区补充“按 Esc 退出全屏模式”提示与显式退出按钮，并修复节点选择折叠后重复出现两个展开图标的问题。
  - 补充结果看板专注模式、侧栏折叠与全屏退出提示相关回归测试。

- **启动参数沿用逻辑修复**:
  - 为 Trigger 节点增加“沿用上次启动参数”开关，默认关闭，避免运行一次后后续自动复用上次启动参数。
  - 调整运行时输入弹窗回填逻辑，关闭沿用时按默认值重新初始化本次启动参数，开启沿用时才保留并复用上次确认值。
  - 调整工作流执行引擎对运行时参数的判定与清理逻辑，确保单节点调试、全局连续补录和下次重新启动三种场景行为一致。
  - 修复单节点调试过程中若上游 Trigger 缺少启动参数，补完参数后不会自动继续当前调试节点、必须再次手动点击调试的问题。
  - 补充 `workflowStore` 与 `RuntimeInputModal` 回归测试，并同步修正文档说明。

- **运行时输入面板与焦点可见性修复**:
  - 移除 `main.css` 中全局清空焦点态与强制靛蓝主色覆盖，恢复浏览器与 PrimeVue 默认焦点可见性，避免键盘导航失去定位。
  - 修复节点配置弹窗左侧 `RuntimeInputs` 区域高度链路不稳定的问题，为滚动区补充明确的全高约束与底部安全留白，避免底部字段被裁切。
  - 修复运行时输入弹窗在单字段场景下错误使用固定高度分栏的问题，文件上传控件等高个输入不再被上半区裁掉；多字段场景补充底部滚动安全空间。
  - 新增 `RuntimeInputs` 与 `RuntimeInputModal` 回归测试，覆盖单字段、不分栏、多字段滚动留白等场景。

- **结果分析弹窗大结果预览稳定性修复**:
  - 为 `DataAnalysisModal` 与 `JsonViewer` 增加统一的安全预览序列化，限制预览最大深度、数组项数、对象键数与字符串长度。
  - `table` / `tableCollection` 继续保留按行预览，`report` / `json` 改为摘要化与截断后的结构预览，避免大结果一次性渲染完整内容导致页面卡死或崩溃。
  - 保留“导出原始结果”能力，不改变原始结果数据本身，并补充大对象预览截断相关回归测试。

- **工作流结果看板标准网格单图表高度修复**:
  - 修复结果看板在标准网格模式下只展示一个图表节点时卡片高度过小、内容区无法正常展开的问题。
  - 为标准网格结果卡片增加稳定的最小行高约束，并补充单节点场景回归测试。

- **工作流画布历史模式与配置面板稳定性修复**:
  - 修复工作流画布历史模式提示条中文乱码，恢复“历史记录查看模式”“返回编辑模式”等关键文案。
  - 将画布历史提示条与浮动运行控件拆分为独立组件，收敛 `WorkflowCanvas` 职责，降低主画布组件复杂度。
  - 为节点配置弹窗新增纵向拖拽 composable，确保拖拽分割线在弹窗卸载时清理 `document` 监听。
  - 为工作流头部、配置表单、运行时输入和数据展示面板补充类型化事件声明，提升 Vue 组件边界的类型安全。
  - 补充 `WorkflowCanvas` 与 `NodeConfigModal` 回归测试，覆盖乱码文案与拖拽监听清理场景。

## [2026-03-24]

### 新增

- **服务端存储用户隔离能力**:
  - 为 `IStorageProvider` 增加当前存储用户上下文读取能力，本地模式返回空用户，服务端模式返回当前登录用户。
  - 新增 TypeScript `/api/storage/*` 存储接口，支持按当前用户隔离工作流列表、单工作流读取与运行历史。
  - `workflowStore` 新增当前存储用户状态加载能力，为后续“仅展示当前用户工作流”提供统一入口。

### 配置 (Config)

- **ESLint Prettier 规则调整**:
  - 关闭 `prettier/prettier` 报错，避免 Windows 行尾差异导致 `Delete "CR"` 类规范错误阻塞开发。

### 修复 (Fix)

- **工作流未保存状态提示优化**:
  - 在工作流头部名称区域新增“未保存”状态标签，明确提示当前编辑内容尚未持久化。
  - 保存按钮在有改动时切换为“保存更改”并增强强调样式，降低用户遗漏保存的概率。
  - 补充 `WorkflowHeader` 未保存提示相关单元测试，确保状态展示与按钮联动行为稳定。

- **PropertyField 集合新增按钮可见性优化**:
  - 调整 `PropertyField` 中集合类属性的“添加...”按钮样式，改为更明确的描边按钮。
  - 为“添加来源配置”等入口补充更明显的虚线边框、白底与悬停反馈，降低用户忽略入口的概率。

### 文档 (Docs)

- **补充环境变量示例文件**:
  - 新增仓库级 `.env.example`，提供前端存储驱动、后端 API 地址与 AI Workflow 默认环境变量模板。
  - 明确 `OPENAI_API_KEY` 需通过环境变量注入，避免再次将默认密钥写入代码或提交记录。

## [2026-03-23]

### 修复 (Fix)

- **AI 编排端到端链路修复**:
  - 修复前端 AI 请求仍落到旧 Python 后端的问题，改为默认走 TypeScript `/api/workflow-ai` 服务。
  - 为 Vite 开发环境加入工作流 AI 中间件，开发时无需额外启动独立 AI 服务即可联调。
  - 修复系统默认 GLM-4.7 配置在前端脱敏后无法恢复 API Key 的问题。
  - 修复 GLM 返回旧版 OpenAI 兼容字段别名时，AI 计划无法应用到画布的问题。
  - 修复 GLM 返回抽象节点类别而非具体节点类型时，AI 计划无法创建节点的问题。
  - 为 AI 计划解析、节点类型归一化与前端失败提示补充回归测试和错误处理。

## [2026-03-22]

### 新增

- **TypeScript AI 编排后端**:
  - 新增独立的 TypeScript AI 服务，承接工作流 AI 模型配置、连通性测试与计划生成接口。
  - 前端 AI 编排改为只调用 TS 后端，Vercel AI SDK 与 OpenAI-compatible provider 仅在服务端执行。
  - 新增默认智谱 GLM-4.7 模型配置，并补充工作流 AI 计划与模型配置相关测试。

## [2026-03-21]

### 新增

- **工作流结果看板**:
  - 新增全局运行后的结果看板弹窗，默认聚合展示所有终止节点结果，无终止节点时回退展示本次执行目标的末端结果。
  - 结果看板支持左侧节点多选、右侧多结果面板聚合查看，并可在标准网格与自由布局两种模式间切换。
  - 标准网格模式支持拖拽调整结果面板展示顺序。
  - 自由布局模式改为基于原生流式布局的可缩放卡片，支持通过右下角拖拽手柄实时调整面板宽高，避免卡片重叠。
  - 补齐表格、分组表格与 JSON 结果 viewer，并统一接入结果看板与节点结果详情查看链路。

### 文档 (Docs)

- **Agent 约束补充**:
  - 在根目录 `AGENTS.md` 中新增 PrimeVue 文档入口说明，明确 `ref/primeVue` 下 `llms.txt`、`llms-full.txt` 与 `multiSelect.txt` 的使用方式。
  - 补充中文写入后的编码检查要求，涉及中文内容写入时必须检查是否出现乱码，若有乱码需先修复。

### 新增

- **JS代码执行节点**:
  - 新增 `JS代码执行` Action 节点，只暴露 `rows` 变量，仅支持同步 JS 转换，并固定输出 `table` 结果。
  - 节点配置编辑器支持 JavaScript 语法和 `rows` 声明提示，便于快速编写数据映射逻辑。

### 修复 (Fix)

- **JS代码编辑器交互修复**:
  - 修复 `JS代码执行` 节点编辑器无法输入空格的问题，禁用 Monaco `EditContext` 输入路径，回退到稳定的 `textarea` 输入模式。
  - 补全 Monaco `typescript` worker 配置，修复 JavaScript 编辑器无语法提示的问题。

### 修复 (Fix)

- **字段合并节点协议修正**:
  - 将 `data-key-merge` 节点的展示名调整为“字段合并”。
  - 将合并配置改为“每个来源仅配置一个来源键字段 + 节点级统一键名称”。
  - 调整执行逻辑：合并后移除各来源原始 key 字段，仅保留统一键名称对应的一列作为输出主键。
  - 补充对应节点执行逻辑与标准结果协议回归测试。

## [2026-03-20]

### 新增

- **数据处理节点扩展**:
  - 新增 `字段选择` 节点，支持包含 / 不包含两种模式，并支持通过普通搜索或正则搜索快速多选字段。
  - 新增 `排序` 节点，支持按多个优先级字段进行升序 / 倒序排序。
  - 新增 `数据量限制` 节点，支持保留最前面或最后面的 `n` 条记录。
  - 新增 `按键合并` 多输入节点，支持为每个上游来源指定独立合并 key，并按 key 并集输出对象级宽表结果。

### UI/UX

- **新节点图标与配置交互补充**:
  - 为新增数据处理节点补齐画布与节点库图标映射，提升节点识别效率。
  - 扩展 `multi-options` 配置控件，支持搜索头部与正则搜索开关，便于在大字段列表中快速定位目标字段。

### 文档 (Docs)

- 同步更新 `工作流系统.md` 与 `工作流节点说明.md`，补充新增节点清单、职责与多输入说明。

### 测试 (Test)

- 新增字段选择、排序、数据量限制、按键合并节点的执行逻辑与标准结果协议测试。
- 新增 `PropertyField` 多选正则搜索交互测试。

## [2026-03-20]

### 修复 (Fix)

- **工作流切换误报未保存**:
  - 修复运行工作流、加载旧工作流或首次打开工作流后被误判为“工作流已修改”的问题。
  - 将工作流脏检查收口到节点、连线的可持久化字段，忽略 Vue Flow 运行时补充的瞬时状态。
  - 加载与导入工作流时补齐节点默认配置，避免首次运行时因静默补默认值导致误报未保存。

### UI/UX

- **工作流视图复位时序修复**:
  - 修复打开工作流后画布布局会连续移动两次的问题，改为一次动画到位。
  - 修复页面刷新后的第一次打开工作流不自动复位、需要再次打开才生效的问题。
  - 调整工作流管理中心关闭与视图复位的时序，避免弹窗期间触发错误复位。

### 测试 (Test)

- 为工作流未保存状态补充旧工作流、连线瞬时状态等回归测试。
- 为工作流画布补充首次打开复位、单次动画复位与管理中心打开链路测试。

## [2026-03-20]

### UI/UX

- **工作流运行条高保真重绘**:
  - 将底部“开始运行工作流”主操作改为更贴近设计稿的双层信息按钮，恢复外壳容器、主按钮与停止按钮的组合结构。
  - 将运行条主按钮从 PrimeVue `Button` 改为原生 `button`，避免组件库默认圆角与内边距干扰视觉还原。
  - 重新设计空闲 / 运行中 / 等待补全输入三种状态，默认空闲态不再使用大面积蓝色激活区，降低“误以为正在执行”的认知负担。

### 测试 (Test)

- 重写 `WorkflowCanvas.spec.ts` 中与运行条相关的回归用例，补充原生按钮结构、空闲态样式与运行条文案断言。

## [2026-03-20]

### UI/UX

- **帮助中心与节点帮助体系**:
  - 在工作流顶部操作区新增 `帮助` 入口，提供全局帮助中心弹窗。
  - 帮助中心首屏新增固定“3 分钟上手”流程，并补充常见问题与按节点分类查帮助能力。
  - 在节点配置弹窗中新增轻量“节点简介”提示条，通过 `?` 图标打开完整节点帮助说明，避免帮助信息喧宾夺主。

### 架构 (Architecture)

- **节点帮助数据源收口**:
  - 为 `NodeDefinition` 扩展 `help` 与 `assistantHints` 字段，统一承载面向用户与未来 LLM 自动编排的帮助信息。
  - 新增节点帮助目录与帮助中心内容配置，收敛帮助中心、节点弹窗与节点注册表的帮助数据来源。
  - 为帮助草图目录新增 `.superpowers/` 忽略规则，避免本地可视化协作文件进入版本控制。

### 文档 (Docs)

- 新增帮助体系设计文档，明确帮助中心、节点帮助与 `assistantHints` 的首期边界。
- 同步更新 `工作流系统.md` 与 `工作流节点说明.md`，补充帮助中心入口、节点帮助字段与后续新增节点的维护约束。

### 测试 (Test)

- 新增帮助中心弹窗、节点帮助交互与节点帮助最小集断言测试。
- 调整 `App.spec.ts` 与工作流相关回归测试，确保新增帮助体系后 `pnpm test:unit` 继续稳定通过。

## [2026-03-20]

### UI/UX

- **工作流新建与保存守卫**:
  - 在工作流顶部操作区新增 `新建` 按钮，补齐新建工作流入口。
  - 为当前工作流增加未保存状态检测，覆盖节点、连线、工作流名称等编辑变更。
  - 在刷新/关闭页面、新建工作流、切换工作流、导入工作流前，若存在未保存改动，统一弹出“保存并继续 / 不保存 / 取消”确认。
  - 保存当前工作流后增加 Toast 反馈，明确提示保存成功或失败。

### 测试 (Test)

- 新增工作流未保存状态、顶部保存提示与页面离开保护相关单测，补强保存交互回归覆盖。

## [2026-03-19]

### 修复 (Fix)

- **导出与界面文案修复**:
  - 修复 `data-export` 节点中 `.xlsx` 导出实际仍生成 CSV 内容的问题，改为真正输出 Excel 工作簿。
  - 将 `WorkflowHeader` 中残留的英文用户文案改为中文，统一工作流界面的中文表达。
- **工程清理**:
  - 删除未使用的 `counter` 示例 store。
  - 移除仓库中的临时 Matplotlib 导出实验脚本与生成图片，降低项目噪音。

## [2026-03-19]

### 文档 (Docs)

- **工作流文档拆分整理**:
  - 精简 `工作流系统.md`，聚焦系统总览、协议、执行链路、持久化与 viewer 等系统级规则。
  - 新增 `工作流节点说明.md`，集中维护节点清单、节点职责，以及新节点创建规则与实现规范。
  - 同步更新 `AGENTS.md` 的工作流文档入口与维护约束，明确系统级文档与节点级文档的分工。

## [2026-03-19]

### 重构 (Refactor)

- **纯 `NodeResult` 协议收口**:
  - 去除执行链中剩余的兼容别名桥接，统一以下游节点直接消费标准化结果对象为主。
  - 多输入节点统一改为消费 `inputs[].result`，收敛合并、聚合等节点的输入协议。
- **快照与类型系统统一**:
  - 保存工作流与历史记录快照统一为 `WorkflowNodeSnapshot`，减少历史模式与运行态之间的结构分叉。
  - 收敛 `workflowStore.ts` 与相关类型定义，清理深层类型实例化问题，恢复 `vue-tsc` 与 `pnpm build` 的稳定通过。
- **工程与加载优化**:
  - 调整 viewer 注册与懒加载策略，继续收口 Monaco、viewer 相关异步加载链路。
  - 优化 Vite `manualChunks` 切包配置，降低工作流主包压力并改善首屏加载体验。

### 测试 (Test)

- 补充节点结果协议回归测试，覆盖结果 helper、剩余节点迁移路径、Store 兼容路径、多输入与历史截断等关键场景。

### 修复 (Fix)

- 修复协议迁移后 `workflowStore` 与工作流 UI 上的类型递归问题，避免 `TS2589` 再次阻塞构建。

## [2026-03-19]

### 重构 (Refactor)

- **统一节点结果协议**:
  - 引入统一的 `NodeResult` / `DataKind` 结果协议，覆盖表格、分组表格、报告、图表、导出与 JSON 等输出类型。
  - 提供 schema、预览摘要与 viewer 映射辅助能力，为后续节点扩展和数据预览打下统一基础。
- **核心节点批量迁移**:
  - 核心节点切换到新协议，包括导入、清洗、体检、聚合、合并、筛选、导出、相关性分析、Lasso、看板对接、XGBoost + SHAP 与图表展示。
  - `workflowStore`、节点配置面板、数据展示面板与分析弹窗统一优先消费标准结果，保留最小必要的历史兼容过渡。

### 测试 (Test)

- 新增结果协议与节点迁移测试，验证核心节点执行输出、结果预览与 Store 兼容逻辑。

## [2026-03-19]

### UI/UX

- **节点库中文乱码修复**:
  - 修复 `NodeSidebar` 中节点标题、分类文案与搜索相关文本的中文乱码问题，恢复节点库可读性。
  - 统一节点库内与工作流相关的用户可见文本，避免中英文与异常编码混杂。
- **节点图标补全**:
  - 为缺失图标的分析节点补齐 `NodeIcon` 映射，覆盖 Lasso / Pearson / Spearman / Kendall 等节点，提升节点识别效率。

## [2026-03-19]

### 优化 (Optimization)

- **多启动节点运行时补录体验优化**:
  - 优化多启动节点连续补录参数时的交互流程，减少在多个启动节点之间来回中断的操作成本。
  - 在补录过程中增加更明确的进度提示，帮助用户理解当前处于第几个待补录节点。
  - 改善运行时输入弹窗与全局执行链的协同逻辑，降低遗漏参数或误判完成状态的概率。
  - 细化日志与提示反馈，让补录流程在复杂工作流下仍然具备可追踪性。

## [2026-03-18]

### 维护 (Maintenance)

- **Git 忽略规则补充**: 将 `cloc-2.06.exe` 加入 `.gitignore`，避免本地二进制工具被误提交。

### 🎨 工作流编辑器宽屏体验优化

- **1920×1080 自适应布局参数化**:
  - 新增 `layout.ts` 统一管理宽屏布局指标，在 `>=1600px` 自动启用更平衡的侧栏宽度、底栏高度与画布留白，提升画布可视面积并保持信息区稳定。
  - `WorkflowCanvas` 统一接入动态布局参数，主画布、右侧节点库、底部运行区与日志面板联动缩放，避免各区域挤压感。
- **节点库与日志区密度微调**:
  - `NodeSidebar` 在宽屏下收敛卡片与分组间距，优化信息密度与浏览节奏。
  - `LogPanel` 下调工具栏与日志行高度，降低底栏视觉重量，展开态更舒适。
- **Header 操作区重设计**:
  - 将右上角状态、保存与文件入口重构为统一的操作舱（Action Deck），强化层级关系。
  - 主操作按钮升级为中文“保存”高强调样式，次级入口改为“文件”下拉按钮，提升可识别性与一致性。

### 🎨 UI/UX 体验优化

- **节点配置弹窗 (NodeConfigModal) 布局升级**:
  - **左侧边栏可调节布局**: 在左侧“输入数据”与“运行时输入”组件之间新增了可拖拽的分割线（Splitter），支持动态调整两个区域的高度占比。
  - **多行文本滚动支持**: 确保 `Textarea` 组件在输入内容超出高度时能正确显示滚动条并支持滚轮操作，防止布局被长文本撑破。

- **运行时输入弹窗 (RuntimeInputModal) 深度优化**:
  - **可调节比例布局**: 引入了可拖拽的分割线（Splitter），允许用户动态调节“启动方式”区域与下方输入区域的显示比例，有效解决长文本输入场景下的空间竞争。
  - **滚动体验优化**: 为属性列表增加了自定义超细滚动条，并确保所有输入组件（包括 `Textarea`）在溢出时均能流畅响应鼠标滚轮。
  - **布局稳定性增强**: 修复了当输入大量 SN 或任务令时，`Textarea` 无限增高导致上方“启动方式”组件被挤压变形的问题。
  - **高度限制与滚动**: 为多行文本输入框增加了 `280px` 的最大高度限制，并启用了内部滚动条，确保长列表输入时界面依然稳健。
  - **Flex 布局加固**: 为属性容器及其标签增加了 `shrink-0` 约束，确保核心交互组件在任何压力下都能保持原始尺寸。
  - **视觉重构**: 引入了更具专业感的设计，增加了 Settings 图标头部、蓝色的引导信息栏以及平滑的滑入动画。
  - **布局改进**: 增大了弹窗宽度 (520px -> 560px)，并优化了底部按钮布局，使其更符合现代 SaaS 审美。
  - **空状态美化**: 为没有运行时参数的节点设计了精致s的空状态插图与文案提示。
  - **交互 Bug 修复**: 修复了弹窗无法通过点击遮罩层或右上角关闭按钮关闭的问题。

- **核心渲染引擎 (PropertyField) 样式升级**:
  - **Tree 组件定制化**: 彻底重构了树状选择器的样式，采用更紧凑的行高、淡蓝色悬浮反馈以及符合整体风格的复选框设计。
  - **DatePicker 适配**: 深度定制了日期范围选择器的输入框样式，确保其与系统 `ndv-input` 规范完全对齐。
  - **Collection 项目美化**: 优化了集合配置项的卡片设计，采用更柔和的圆角、阴影以及更具描述性的项目标题（如“因子全集 #1”）。
  - **AutoComplete 令牌精简化**: 优化了标签（Tags）组件的 Token 样式，使其在多选场景下更加紧凑且易于阅读。
  - **必填标识**: 在所有属性标签中增加了红色星号 (`*`) 标识，直观区分必填项与选填项。

- **看板数据对接节点 (Neighbor System) 细节增强**:
  - **语义化必填项**: 明确标注了查询日期、物料类型、阶段方案等关键运行时参数为必填，引导用户正确完成流程启动。
  - **文案精修**: 优化了各模式下的占位符 (Placeholder) 与帮助提示，确保非技术背景用户也能顺畅操作。

## [2026-03-16]

### 维护 (Maintenance)

- **Git 历史清理防护**: 更新 `.gitignore`，忽略本地导出压缩包与 Python 缓存文件，避免大文件和缓存产物再次进入版本库。

### 优化 (Optimization)

- **DataChart**: 显著增加折线图线条宽度 (1 -> 2.5)，提升视觉清晰度。
- **图表布局**: 统一优化所有节点（包括数据体检、图表展示、SHAP分析）的图表上下留白 (15%)，并增加 Y 轴边界间隔，防止波峰贴边。
- **UI 交互**: 为 DataChart 重新启用并优化了 `dataZoom` 控件（滚动条与缩放），提升大数据量下的交互体验。

### 修复 (Fix)

- **Vue 警告**: 修复了 DataChart 模板中引用未定义变量 `sourceData` 导致的控制台警告。
- **布局微调**: 修正了分析对话框中图表的网格配置，确保图表元素不被遮挡。

### 重构 (Refactor)

- **存储层抽象化**: 彻底解耦了工作流与执行历史的存储逻辑。
  - 引入 `IStorageProvider` 接口，支持未来扩展多种存储介质（如 S3, Server API）。
  - 将所有持久化操作从 `workflowStore` 同步模式重构为 **Promise 异步模式**。
  - 建立 `src/utils/storage/` 模块化目录结构，统一管理 `LocalStorage` 与 `IndexedDB` 实现。
- **Store 瘦身**: 移除了 `workflowStore.ts` 中冗余的持久化具体实现代码，使其专注于状态管理。

### 修复

- **Sprint C - SHAP 体验修复**:
  - 将 Xgboost + SHAP 节点输出重构为“前端主报告 + 后端原始整图补充视图”，消除旧版双 tab 叙事冲突。
  - 新增 SHAP 报告摘要卡片、因子搜索与全量因子明细浏览能力，默认降噪展示但保留全部因子可达性。
  - 调整 ReportViewer 导出语义，区分“导出当前报告”与“导出原始整图”，避免页面视图与导出结果混淆。
  - 统一后端 /analyze/xgboost-shap 返回结构，输出 `summary`、`importance`、`dependence` 与 `assets` 四类数据。

### 优化 (Optimization)

- **健壮性增强**: 在存储层引入了环境自动检测与降级机制（如在测试环境自动回退至内存存储），提升了系统的稳定性。

## [Unreleased]

### 新增

- **看板数据对接桥接层**:
  - 新增宿主系统 iframe `token` 接收能力，并通过统一桥接层对接外部看板 API。
  - 新增产品、因子、方案、SN 与任务令等远程数据源的统一查询封装。

### 测试

- 补充看板桥接层与节点执行测试，覆盖 `token` 校验与中文文案展示场景。

### 修复

- **Xgboost + SHAP 原始整图尺寸修复**:
  - 修复后端完整版 SHAP 整图在 `beeswarm/bar` 绘制阶段被 SHAP 自动改写 figure 尺寸，导致标题、坐标轴与子图整体放大重叠的问题。
  - 为后端 summary 图显式绑定 Matplotlib `ax`，并禁用 `beeswarm` 的自动 `plot_size` 调整，统一由 `_draw_report` 的画布尺寸与留白逻辑控制最终布局。

### 测试

- 新增后端回归测试，覆盖完整版 SHAP 整图使用稳定手动布局、显式 `ax` 绑定以及禁用自动尺寸改写的行为。

### 新增

- **工作流节点复制功能**:
  - 在节点工具栏中新增“复制”按钮，支持一键克隆节点及其完整配置。
  - 自动处理新节点命名（增加“ (副本)”后缀）及位置偏移（+40, +40），确保视觉不重叠。
  - 复制过程仅保留节点定义与参数，不包含连线，保持逻辑清晰。
- **数据合并节点**: 新增多输入合并节点，支持纵向追加、按键横向合并、字段冲突处理、来源追踪与诊断信息输出。

### 优化

- 扩展节点定义协议，支持声明式多输入能力、最少输入数与最大输入数限制。
- 工作流执行引擎支持多上游触发与结构化 `inputs` payload，兼容单输入节点的既有执行方式。

### 测试

- 补充多 trigger、多输入 payload 以及 `data-merge` 节点相关 UI 展示的单元测试。

### 新增

- 相关性分析节点新增 `Spearman` 与 `Kendall` 两种秩相关算法，统一输出热力图、目标字段排序图与摘要表。

### 修复

- **多输入连接限制修复**: 修复了 `data-merge` 等多输入节点在连接多个上游节点时被 `validateConnection` 错误拦截的问题。
  - 修正了 `getNodeInputLimits` 中 `maxInputs` 的默认回退逻辑。
  - 优化了连接校验算法，正确处理 `maxInputs: null`（无上限）的情况。
- **多输入节点默认配置补齐**: 修复旧工作流或残缺配置下，一键运行在 `data-merge` 这类多输入节点上未补齐默认参数，导致结果与节点面板单节点调试不一致的问题。
  - 执行前会按节点定义自动回填缺失默认值，确保全局运行与单节点调试走同一套默认配置。
  - 补充回归测试，覆盖旧配置多输入节点在全局运行时的默认行为。
- **分组集合结果污染修复**: 修复 `data-merge` 使用 `collection` 模式时，一键运行经过 `chart-display` 等终点节点后，上游合并结果被误改写成首组扁平数据的问题。
  - 调整图表展示节点对分组集合输入的消费方式，避免直接修改上游节点输出对象。
  - 修复数据合并节点 collection 模式下预览图表引用未定义变量的问题，并补充对应回归测试。

### 重构

- **合并数据集节点**: 将“纵向追加”与“对象合并”节点整合为统一的“数据合并” (`data-merge`) 节点。
  - 支持通过 `mergeMode` 切换“纵向追加 (Append)”、“横向关联 (Join)”与“分组集合 (Collection)”模式。
  - 新增“分组集合”模式，支持将多路输入包装为 `[{name, data}, ...]` 结构，适配箱线图等对比分析场景。
  - 优化了属性布局，使用 `displayIf` 实现模式特定的参数联动显示。
  - 统一了执行逻辑与单元测试，减少了冗余代码并提升了工具箱的整洁度。
- 提取共享相关性分析核心，统一 `Pearson`、`Spearman`、`Kendall` 的数值字段识别、成对样本计算、显著性摘要与图表配置生成逻辑。

### 测试

- 补充相关性节点单测，覆盖 `Pearson`、`Spearman`、`Kendall` 在单调正负相关场景下的报告标题、图表元数据与排序结果。

### 完善

- **节点系统 - 相邻系统对接 (Neighbor System)**:
  - **功能增强**: 全面支持“时间范围”、“按方案”和“按 SN 集合”三种数据获取模式，满足不同业务维度的检索需求。
  - **属性体系优化**: 新增“产品型号”配置项；升级因子选择器为标准的 **3 层树状结构**（系统 -> 模块 -> 因子），提升了大规模因子管理的操作体验。
  - **执行引擎重构**: 实现了模拟的分步 API 调用逻辑（先检索 SN 列表，再基于 SN 和因子清单拉取数据），并在日志中打印详细的 API 路径和 Payload，便于后续真实接口对接。
  - **健壮性提升**: 增加了因子选择校验及异常模式处理，并同步更新了单元测试，确保逻辑覆盖率。

### 🎨 UI/UX 体验优化

- **画布布局与复位逻辑修复 (WorkflowCanvas)**:
  - **动态视口适配**: 实现了 `main` 容器 `bottom` 偏移与日志面板高度的实时联动。现在当日志面板展开或收缩时，画布容器会自动调整大小，确保 `fitView` (视图复位) 计算的中心点始终位于精确的可见区域几何中心，彻底解决了节点复位后偏离视觉中心的问题。
  - **层级与布局修复**: 将节点库 (`NodeSidebar`) 的 `z-index` 提升至 `130`，并将其 `bottom` 属性与日志面板高度挂钩。现在侧边栏高度会随日志伸缩动态变化，不再与日志面板产生物理遮挡，确保了全场景下的操作可达性。
  - **内部控件定位简化**: 随着父容器 `main` 尺寸的精确化，简化了画布内部 `Controls` 和“复位视图”按钮的定位逻辑，移除冗余的动态偏移，改用简洁的固定边距。
  - **平滑交互动画**: 为 `main` 容器添加了与日志面板同步的 `300ms` 平滑过渡动画，显著提升了界面伸缩时的视觉连贯性。

  - **数据聚合节点 UI 优化 (Data Aggregation)**:
  - **组件升级**: 仅将“聚合模式”参数从下拉框升级为极简风格的 **SelectButton (分段控制)**，提升了核心切换区域的视觉质感。
  - **保持原有 UX**: 严格保留了原有的 `collection` 任务配置布局及所有交互逻辑，确保用户习惯的一致性。

  ### 🔧 基础设施与测试优化

- **Vitest 脚本优化**:
  - 将默认的 `test:unit` 脚本修改为 `vitest run`（非监听模式），避免在自动化环境或 Agent 执行时需要手动确认。
  - 新增 `test:watch` 脚本，保留开发过程中的实时监听测试能力。

### 🏗️ 架构重构

- **解耦 NodeConfigModal 组件**: 针对原本过于臃肿的节点配置弹窗进行了深度重构，将其拆分为多个功能单一的原子组件，显著提升了代码的可维护性与扩展性。
  - **核心渲染引擎 `PropertyField.vue`**: 支持递归渲染 `collection` 类型，统一处理 `file`, `json`, `tags`, `datetime-range` 等 10 余种字段类型。
  - **独立功能模块**: 抽离了 `ConfigHeader` (头部信息/冻结控制)、`ConfigFooter` (操作按钮)、`ConfigForm` (参数表单) 及 `RuntimeInputs` (运行时输入)。
  - **精简主组件**: `NodeConfigModal.vue` 现在仅负责核心状态调度与数据流转，代码量减少了约 60%。
- **日志系统优化**: 配置字段中的操作（如文件上传成功）现在直接通过 `workflowStore` 记录日志，消除了繁琐的组件间事件冒泡。

### 🚀 智能数据转换与清洗系统

- **全自动数值转换**:
  - 文件导入及手动输入节点现已集成智能数据清洗功能，默认开启自动数字转换。
  - 自动识别并将 "N/A", "null", "NaN", "-", "undefined" 等异常占位符统一映射为标准 `null`，确保后续分析节点和图表渲染的稳定性。
- **排除字段管理 (Exclude Fields)**:
  - 引入了 `tags` 类型的属性定义，支持用户灵活指定不参与自动转换的原始字段。
  - **智能联想输入**: 使用 `AutoComplete` 多选组件，支持从已发现的字段列表中自动联想选择，点击输入框即可自动弹出建议。
  - **灵活输入保障**: 即使输入的内容不在建议列表中，按回车也可强制保留为标签，并支持通过退格或点击一键删除。
  - **动态 UI 联动**: 仅在开启“自动转换数字”时动态显示排除字段配置，减少界面冗余。

### ⌨️ 高效交互体验升级

- **AutoComplete 交互优化 (PropertyField.vue)**:
  - 改进了 `tags` 类型属性的选择体验，现在在下拉框中选择项后会自动保持开启，支持连续多次选择而无需重复点击。
  - 优化了聚焦逻辑，点击输入框即可立即触发联想查询并显示候选列表。
  - 增强了 Enter 键处理，在手动输入新标签并确认后自动维持下拉框状态，提升了批量因子录入的效率。
- **优化节点配置回车逻辑**:
  - 修复了在节点设置弹窗（如“数据清洗”节点）的输入框中按回车会导致弹窗意外关闭的 Bug。
  - **系统化去耦**: 移除了 `PropertyField` 中基础输入组件（文本、数字、下拉框）上的强制回车保存监听，确保复杂配置过程中的操作连贯性。
  - **标签输入优化**: 改进了 `AutoComplete` 标签组件的回车处理，现在回车仅用于确认添加新标签。只有在显式点击“应用并保存”或“调试节点”时才会提交更改，避免了连续添加标签时的误关闭。
  - **交互一致性**: 同步移除了节点名称编辑框的回车关闭逻辑，并更新了页脚提示文案以匹配新的交互规范。
- **IndexedDB 迁移**: 彻底重构了执行历史存储系统，将存储介质从有限的 `localStorage` (5MB) 迁移至 **IndexedDB**。
  - **海量容量支持**: 理论存储上限提升至数百 MB，完美解决了 SHAP 分析等节点产生的大量 Base64 高清图片导致“存储空间不足”的报错。
  - **完整记录保留**: 现在能够稳定保留最近 20 条执行记录，且每条记录的所有图表、图片和原始数据均可完整查看，无需任何截断。
  - **性能提升**: 采用异步读写机制，避免了处理大数据对象时序列化操作阻塞主线程导致的界面卡顿。
- **异步逻辑重构**: 更新了 `WorkflowCanvas` 和 `WorkflowHeader` 的调用链，全面适配异步存储接口，确保历史数据加载的确定性。

### 🚀 归因分析大图深度重构

- **布局一致性**: 彻底重构了后端 `_draw_report` 核心绘图逻辑，采用 3 列明细布局并优化了垂直间距，确保 Base64 生成的“归因分析大图”与原始算法输出 (`demo_full_report.png`) 的专业感完全一致。
- **超清渲染**: 将默认 DPI 从 100 提升至 **150**，解决了图表在网页端放大查看时的模糊问题，确保 Beeswarm 和趋势曲线的每个细节都清晰可见。
- **全量指标展示**: 报表大图现在能够动态展示模型 R² 和 MAE 指标，并支持动态高度计算，彻底解决了多因子情况下子图重叠挤压的顽疾。

### 🔧 修复与优化

- **导出颜色兼容性修复**: 彻底解决了 `ReportViewer` 在导出 PDF 或图片时因 Tailwind CSS v4 默认使用 `oklch` 颜色函数导致的 `html2canvas` 解析报错。
  - **智能颜色转换**: 引入了基于 Canvas 的实时颜色转换机制，能够自动将 `oklch` 或 CSS 变量颜色转换为 `rgba` 格式。
  - **导出稳定性增强**: 采用了 `html2canvas` 的 `onclone` 回调机制，在不影响原始 UI 的前提下对克隆 DOM 进行静默修复，确保导出任务的稳健性。
- **导出路径兼容性**: 移除了算法工具中潜在的路径硬编码风险，增强了在不同项目目录下的运行稳健性。
- **Base64 接口增强**: 统一了文件保存与内存生成的绘图入口，确保两种模式下的视觉产出完全同步。
- **导出逻辑优化**: 优化了 `ReportViewer` 对后端大图的导出处理。

## [1.4.3] - 2026-03-11

### 🎨 UI/UX 体验优化

- **预览面板滚动修复 (DataDisplayPanel)**:
  - 通过在 `NodeConfigModal` 和 `DataDisplayPanel` 中引入 `min-h-0` 约束和 `flex` 布局调整，彻底解决了在小屏幕或低高度界面下节点输入/输出预览区域内容被截断且无法滚动的问题。
  - 确保了 Monaco Editor 在手动输入模式下也能正确占据剩余空间并支持内部滚动。

- **Data Cleaning 节点增强**:
  - 支持指定目标字段进行清洗。
  - 新增 **特征缩放 (Scaling)** 功能，支持 Min-Max 归一化与 Z-Score 标准化。
  - 新增 **分类变量编码 (Encoding)** 功能，支持自动 Label Encoding。
  - 增强异常值处理，新增百分比剔除法。
  - 优化执行结果返回，提供详细的清洗统计数据。

- **Data Aggregation 节点增强**:
  - 支持 **维度分组统计 (Group By)**、**移动窗口计算 (Rolling)** 及 **行内多列合并** 三种模式。
  - **行内合并模式优化**: 移除复杂的权重配置，采用极简的 `Tags` 属性直接选择参与字段，大幅缩减配置项长度并提升操作效率。
  - 增加详细的中文参数描述和帮助提示，优化了 `collection` 嵌套的视觉体验。
  - 采用 `markRaw` 优化大数据量下的执行性能。

### 优化 (Refactor)

...

- **WorkflowCanvas**: 深度重构核心画布组件，将工作流管理逻辑解耦至独立组件 `WorkflowManagerModal`。
- **Store 层**: 封装所有持久化操作 (`localStorage`) 到 `workflowStore`，严格遵循依赖倒置原则。
- **稳定性**: 引入 `needsViewReset` 信号机制替代硬编码的 `setTimeout` 定时器，解决视图复位的竞态问题。
- **类型安全**: 完善 `WorkflowMetadata` 与 `SavedWorkflow` 接口定义，移除多处 `any` 类型。

### 测试 (Test)

- 增强单元测试鲁棒性，修复了测试环境下 IndexedDB 缺失导致的失败问题。
- Mock 掉 Monaco Editor 与网络请求，确保测试在 JSDOM 环境下的稳定性。

## [1.4.1] - 2026-03-09

### 🎨 UI/UX 体验优化

- **工作流管理中心布局重构**:
  - **消除重叠**: 将“创建新工作流”入口从滚动区域移出，固定在弹窗顶部。解决了在大数据量下，吸顶按钮可能与列表内容产生视觉重叠或滚动冲突的问题。
  - **智能排序**: 引入了按更新时间 (`updatedAt`) 倒序排列的逻辑，确保用户最新操作的工作流始终置于列表首位，提升了查找效率。
  - **运行历史精准过滤**: 修正了“运行历史记录”显示全部记录的问题。现在无论在顶部菜单栏还是管理中心，系统均会根据 `currentWorkflowId` 自动过滤并仅展示与当前工作流相关的执行历史。
  - **工作流加载状态重置**: 修复了打开已保存工作流时节点错误显示“运行中”或“成功/失败”状态的问题。现在在加载、保存、复制及导入导出工作流时，系统会自动重置所有非冻结节点的运行时状态，确保每次打开都是一个干净的起点。
  - **层次感增强**: 优化了静态创建区域与动态列表区域的间距（增加至 `gap-5`），使界面结构更加清晰、专业。
- **引入自动导入机制**:
  - 集成了 `unplugin-auto-import` 插件，系统性解决了 Vue 核心 API（如 `ref`, `computed`, `watch`）需手动引入的问题，提升了开发效率并减少了潜在的运行时 `ReferenceError`。
  - 配置了自动生成 TypeScript 类型声明文件 (`auto-imports.d.ts`)，确保了全自动导入下的类型安全。

## [1.4.0] - 2026-03-08

### 🚀 算法节点深度对接 (XGBoost + SHAP)

- **分析能力飞跃**:
  - 彻底重构了 `XGBoost + SHAP` 节点，从单一的柱状图升级为全方位的多维归因报告。
  - **蜂群图 (Beeswarm Plot)**: 首次引入基于散点抖动算法的 ECharts 蜂群图，通过颜色视觉映射直观展现各因子对目标值的正负贡献分布。
  - **依赖趋势图 (Dependence Plots)**: 自动为前 6 个核心重要因子生成独立的依赖关系图，支持分析因子取值与贡献度之间的非线性趋势。
  - **多维指标统计**: 增加了模型拟合优度 (R²) 和平均绝对误差 (MAE) 的明文展示。
- **鲁棒的错误处理**:
  - 废弃了“后端报错自动回退至 Mock 数据”的静默处理机制。
  - 现在系统会真实透传后端算法层的报错（如“有效数据过少，无法训练模型”），并在日志面板和节点状态中实时反馈，极大提升了分析过程的可信度与调试效率。
- **后端引擎增强**:
  - 升级了 `backend/main.py` 的数据提取逻辑，支持大规模 SHAP 矩阵的采样传输与特征归一化计算，确保了大数据量下前端渲染的流畅性。

## [1.3.9] - 2026-03-08

### 🏗 基础设施升级

- **引入 ESLint 体系**:
  - 集成了 ESLint 10.x 并采用最新的 **Flat Config** (eslint.config.js) 格式。
  - 配套安装了 Vue 3、TypeScript 及 Prettier 的官方插件，实现了代码质量校验与格式化的一体化。
  - 新增 `pnpm lint` 和 `pnpm lint:fix` 命令，支持一键整改代码问题。
- **全量代码清理与修复**:
  - 修复了 30 余处 `no-undef` 浏览器全局变量引用错误。
  - 清理了全项目 50 余处未使用的 import 导入及变量，显著提升了代码简洁度。
  - 修正了 `no-useless-assignment` 冗余赋值及 `preserve-caught-error` 异常处理不规范的问题。
  - 对 `NodeConfigModal`, `WorkflowCanvas`, `workflowStore` 等核心组件进行了深度的代码扫描与合规化调整。

## [1.3.8] - 2026-03-08

### 🔧 修复与优化

- **修复工作流保存逻辑错误**:
  - 解决了“点击新建工作流后保存会覆盖当前已打开工作流”的关键 Bug。
  - 在 `workflowStore` 中封装了标准的 `createNewWorkflow` 方法，强制在新建时清除 `currentWorkflowId`，确保后续保存操作能够正确生成新 ID 而非覆盖旧数据。
  - 统一了新建工作流时的状态重置逻辑（包括节点、连线、日志、名称及历史模式状态）。

## [1.3.7] - 2026-03-08

### 🎨 UI/UX 体验优化

- **管理中心滚动优化**:
  - 为“工作流管理中心”设置了更合理的 `420px` 最大高度限制，确保在各类屏幕尺寸下均能及时触发内部滚动，避免弹窗过长。
  - 实现了“创建新工作流”卡片的吸顶效果 (`sticky`)，在大数据量列表下依然触手可及。
- **视觉修正**:
  - 修复了运行历史列表中错误状态图标无法显示的 Bug。

## [1.3.6] - 2026-03-08

### 🎨 UI/UX 体验优化

- **连接线视觉重构**:
  - 显著提升了编辑状态下连接线的可见度，将默认颜色从浅灰 (`slate-200`) 调整为辨识度更高的中灰 (`slate-400`)。
  - 增强了连接线的交互反馈：鼠标悬浮时，线条会加粗（`1.5px` -> `2.5px`），颜色变更为靛蓝色，并带有轻微的发光阴影效果。
  - 优化了动画与颜色切换的平滑度，使整体画布操作更加跟手且专业。

## [1.3.5] - 2026-03-08

### 🔧 修复与优化

- **调试按钮视觉修正**:
  - 彻底解决了调试按钮在特定交互状态下出现的“绿色光圈”问题。
  - 通过将该按钮重构为原生 HTML 元素并手动实现 Loading 逻辑，绕过了 UI 组件库（PrimeVue）内置的高优先级聚焦样式注入。
- **全局样式防御**:
  - 增强了全局 CSS 覆盖，强制将 PrimeVue 聚焦环变量设为透明，并统一了主色调变量，确保系统视觉的一致性。

## [1.3.4] - 2026-03-08

### 🚀 日志组件增强

- **多维度过滤系统**:
  - 引入了基于日志级别（全部、信息、警告、错误）的快速切换过滤。
  - **视觉一致性优化**:
    - 级别筛选按钮的图标现在**始终应用持久化配色**（Emerald, Amber, Rose），即使在未选中状态下也能通过颜色直观区分。
    - 日志列表中的状态圆点已全面升级为对应的**彩色功能图标**，增强了执行详情的辨识度与专业感。
  - 新增实时文本搜索功能，支持通过消息内容或节点 ID 进行关键词过滤，并伴有平滑的动画展开效果。
- **交互与导航优化**:
  - **节点一键跳转**: 点击日志中的节点 ID (#ID) 可直接触发该节点的配置弹窗，显著提升了调试排错效率。
  - **日志导出**: 新增 JSON 格式导出功能，文件名自动携带精确到秒的时间戳，便于离线分析与存档。
- **视觉美化**: 对日志面板 Header 进行了重构，采用了符合极简 SaaS 风格的 Chip 切换器和内敛式搜索框设计。

## [1.3.3] - 2026-03-08

### 🔧 修复与优化

- **帮助系统激活**:
  - 全局补全了 "?" (HelpCircle) 帮助图标缺失的 `v-tooltip` 指令。
  - 现在在节点配置弹窗、运行时输入弹窗中，鼠标悬浮至属性名称旁的帮助图标，即可正确显示算子定义的详细描述。
  - 为集合配置项（Collection）内部的子属性和输入框也增加了即时帮助提示。
- **状态反馈增强**:
  - 为画布节点的执行状态图标（正在运行、执行成功、执行失败）添加了 Tooltip 提示，增强了执行过程的直观感。
- **UI 细节优化**:
  - 在 `main.css` 中定制了全局 Tooltip 样式，采用深色微透背景（Slate-800）与极简圆角设计，确保在各类复杂弹窗背景下均具有良好的易读性和层次感。

## [1.3.3] - 2026-03-08

### 🚀 技术架构升级

- **专业级编辑器集成**: 引入 **Monaco Editor** 替换原有简单的 `Textarea`，显著提升了 JSON 数据的编辑与调试体验。
- **本地化异步加载**:
  - 通过 `vite-plugin-monaco-editor` 插件将核心分析 Worker 静态化到本地，不依赖任何 CDN。
  - 实现按需懒加载机制，仅在用户打开 JSON 编辑界面时触发资源下载，确保首屏性能。
- **体验优化**: 支持 JSON 语法高亮、自动格式化、实时语法校验以及代码折叠，符合专业分析工具质感。

## [1.3.2] - 2026-03-08

### 🚀 新增功能

- **手动输入数据节点**:
  - 新增 `manual-json-import` 算子，支持直接在界面输入 JSON 格式的原始数据集。
  - 属性系统新增 `json` 类型支持，并在配置弹窗和运行时弹窗中通过 `Textarea` 实现多行文本编辑。
  - 增强了 JSON 数据校验与自动包装逻辑，确保手动输入的数据能平滑流转至后续算子。
  - 为该节点配置了专属的“编辑”图标及橘色视觉标识。

## [1.3.1] - 2026-03-08

### 🎨 UI/UX 细节优化

- **全局指针效果增强**:
  - 在 `main.css` 中建立了全局基础准则，确保所有 `button` 元素、`[role="button"]` 以及 PrimeVue 核心组件（Select, MultiSelect, DatePicker 等）在悬浮时统一呈现 `cursor: pointer` 手型光标，消除交互歧义。
  - 针对非标准按钮元素（如自定义图标按钮、标签页、列表操作项）进行了专项补全，覆盖了工作流画布、节点工具栏、日志面板及所有配置弹窗。
- **交互一致性整改**:
  - 修正了“复位视图”、“执行记录切换”、“数据分析导出”等 20 余处点击区域缺失 hover 反馈的问题。
  - 确保了工作流管理中心、节点配置弹窗内的所有 Tab 切换和删除操作均具有即时的光标反馈。

## [1.3.0] - 2026-03-08

### 🚀 新增功能

- **工作流历史运行记录查看**:
  - **只读快照回放**: 支持查看所有工作流的历史运行快照，包括当时的节点配置、执行状态、输出数据及日志。
  - **历史模式隔离**: 进入历史模式后，画布进入只读状态，自动禁用节点拖拽、连线、删除及配置编辑功能。
  - **视觉提示系统**:
    - 新增琥珀色 (Amber) 顶部提示条，明确告知当前处于历史查看模式。
    - 画布应用了轻微的去色滤镜，并微调网格颜色，增强“回溯历史”的视觉质感。
  - **便捷入口**: 在 Header 工作流名称右侧增加了历史记录图标按钮，支持通过 Popover 快速预览最近 20 条记录。
  - **状态一键还原**: 提供“返回编辑模式”按钮，支持从历史快照平滑恢复到进入前的编辑现场。

## [1.2.9] - 2026-03-08

### 🚀 新增功能

- **工作流克隆/复制**:
  - 在“工作流管理中心”为每个工作流增加了一键复制功能。
  - 复制操作会自动生成新的唯一 ID，并为副本名称添加“ (副本)”后缀。
  - 优化了管理界面的交互，复制按钮仅在鼠标悬浮时显示，保持界面极简美感。

## [1.2.8] - 2026-03-08

### 🔧 修复与优化

- **工作流执行引擎增强**:
  - **修复递归执行状态残留**: 解决了当流程在触发器（如文件导入）处等待运行时输入时，后续节点（如数据清洗）会错误地停留在“运行中”状态的问题。现在子节点等待输入时，父节点会自动重置为 `idle`。
  - **实现运行时输入自动续行**: 重构了 `resumeExecution` 逻辑。用户补全缺失输入（如上传文件）后，系统现在会自动重新触发全局运行，无需第二次手动点击“开始运行工作流”按钮。
  - **交互一致性修复**: 修复了在等待运行时输入期间“停止执行”按钮消失的问题。现在停止按钮在整个生命周期内（包括弹窗等待期）均可见，且支持一键取消当前挂起的流程。
  - **防重触发保护**: 在等待输入期间自动禁用“开始运行”按钮，避免用户重复触发导致的状态竞态冲突。

## [1.2.7] - 2026-03-08

### 📝 文档规范

- **确立 UI/UX 设计规范**:
  - 在 `GEMINI.md` 和 `AGENTS.md` 中新增了系统级 UI 设计准则。
  - 明确了极简现代 SaaS 风格、Slate/Blue 商务色系、沉降式画布以及物理交互稳定性等核心开发要求。
  - 强制规定所有面向用户的界面文案必须使用中文。

## [1.2.6] - 2026-03-08

### 🎨 UI/UX 体验优化

- **节点库 (NodeSidebar) 专业化重构**:
  - 全面移除了具有“AI 感”的紫色调，替换为更严谨的 `Slate-900` 和 `Blue-600` 商务色系。
  - 优化了搜索框聚焦状态及连接模式下的提示面板，视觉重心更加稳固。
  - 对节点分类进行了更规范的中文命名调整（数据源、算子、分析）。
  - 统一了连接模式下的交互反馈，采用与整体 SaaS 风格一致的灰度系虚线设计。

## [1.2.5] - 2026-03-08

### 🎨 UI/UX 体验优化

- **画布背景与网格重构**:
  - 将画布背景色优化为 `Slate-50` (`#f4f7fa`)，并强化了与顶部/侧边栏的视觉区隔。
  - 引入了双层交叉线 (Lines) 网格系统，显著提升了空间坐标感和专业度。
  - 为主画布容器增加了内阴影 (Inset Shadow)，实现了更具深度的“沉降式”视觉层次。
  - 修复了网格在线条模式下因配置缺失导致的显示不清晰问题。

## [1.2.4] - 2026-03-08

### 🏗 架构重构

- **顶部菜单组件化**:
  - 将 `WorkflowCanvas.vue` 中庞大的 `header` 逻辑完整抽离至独立的 `WorkflowHeader.vue` 组件。
  - 优化了组件间通信，通过自定义事件实现了解耦，显著提升了核心画布组件的可维护性。

### 🔧 修复与优化

- **菜单图标视觉修正**:
  - 修复了 "Save" 按钮右侧导入/导出按钮图标显示异常及比例不当的问题。
  - 将图标统一调整为 **16px** 并在容器内居中，确保了极简 SaaS 风格的视觉精确度。
  - 移除了冗余的 Icon 导入并精简了画布组件的依赖。

## [1.2.3] - 2026-03-08

### 🚀 新增功能

- **工作流删除确认**:
  - 为删除已有工作流的操作增加了二次确认对话框，有效防止误删风险。
  - 对话框采用 PrimeVue `ConfirmationService` 驱动，样式与极简 SaaS 风格统一。

### 🎨 UI/UX 体验优化

- **管理中心弹窗视觉重构**:
  - 更换标题图标为更具结构感的 `Layers2`，并将色调改为深灰系 (`Slate-600/900`)，消除“AI 感”。
  - 重构了弹窗页眉布局，显式添加了始终可见的关闭图标按钮。
  - 将“创建新工作流”卡片升级为深色模式 (`Slate-900`)，提升了视觉对比度和操作引导。

### 🔧 修复与优化

- **全局服务注册**:
  - 在 `main.ts` 中正确注册了 `ConfirmationService` 和 `ToastService`，解决了由此导致的组件渲染错误。

## [1.2.2] - 2026-03-08

### 🎨 UI/UX 体验优化

- **极简现代 SaaS 风格重构**:
  - 全面重构了节点 (`BaseNode`)、连线 (`N8nEdge`) 和顶部菜单栏 (`WorkflowCanvas`) 的视觉设计。
  - 采用了 Crisp 1px 描边、纯白背景以及基于 `ring` 的内敛状态反馈，风格对标 Vercel/Linear 的极简美学。
  - 移除了冗余的渐变、噪点和模糊效果，显著提升了界面长久使用的舒适度和专业度。
- **连接点交互体验优化**:
  - 修复了连接点 (Handle) 在鼠标悬浮时因 `transform` 冲突导致的位移跳变问题。
  - 引入了基于 `box-shadow` 的外发光反馈机制，确保物理中心点绝对静止，使连接操作更加精准稳定。

## [1.2.1] - 2026-03-08

### 🎨 UI/UX 体验优化

- **节点库 (Node Library) 视觉重构**:
  - 将 "Node Library" 汉化为 "节点库"，并全面汉化了搜索占位符及连接提示。
  - 修复了搜索框图标不可见的问题，通过增加 `z-10` 层级和加粗图标提升了可见度。
  - 采用了更细腻的搜索框设计，增加了聚焦时的发光效果和快速清除按钮。
  - 优化了节点分类的间隔和标题样式，增加了渐变分割线和更具工业感的图标装饰。

### 🔧 修复与优化

- **提交规范升级**:
  - 在 `GEMINI.md` 和 `AGENTS.md` 中明确了 Git 提交消息必须使用中文的规范。
  - 移除了 `NodeSidebar.vue` 中未使用的 `InputText` 导入。

## [1.2.0] - 2026-03-08

### 🚀 新增功能

- **节点数据冻结 (Pin)**:
  - 实现了 n8n 风格的节点数据锁定功能。用户可以通过节点工具栏或配置弹窗冻结节点输出。
  - 被冻结的节点在后续运行（包括全局运行）中将直接返回缓存结果，并拦截对其上游节点的无效计算请求。
  - 在 `BaseNode` 和 `DataDisplayPanel` 中增加了直观的冻结状态指示器（图钉图标和状态勋章）。

### 🔧 修复与优化

- **全局运行逻辑优化**:
  - 重构了 `runGlobal` 流程。在启动前会智能重置所有未冻结节点的输出状态，确保运行结果的实时性。
  - 引入了“单次运行缓存共享”机制，解决了多终点工作流中共享上游节点被重复重算的问题，显著提升了大型流程的执行效率。
- **项目规范增强**:
  - 在 `GEMINI.md` 和 `AGENTS.md` 中明确了“提交代码必须同步更新 CHANGELOG”的开发规范。

### 🧪 测试补全

- 为 `workflowStore` 增加了针对 Pin 拦截机制的自动化单元测试用例，并确保全量测试通过。

## 2026-03-16

### 修复

- 修复工作流运行历史快照查看时节点不可点击的问题，历史模式下允许打开节点详情面板查看输入输出数据。
- 补充 `WorkflowCanvas` 回归测试，覆盖历史模式节点仍可选中的交互行为。

## 2026-03-16

### 修复

- 将工作流持久化从 `localStorage` 扩展为优先使用 IndexedDB，避免复制大型工作流时触发 `saved_workflows` 存储配额溢出。
- 增加本地存储提供器回归测试，覆盖 `QuotaExceededError` 场景下的工作流保存与读取。

## [1.1.0] - 2026-03-07

### 🚀 新增功能

- **后端集成**:
  - 创建了基于 Python 的 FastAPI 服务器 (`backend/main.py`)，作为调用复杂相关性分析算法的桥梁。
  - 实现了 `/analyze/xgboost-shap` 接口，支持与 `RobustAnalyzerTool` 的深度对接。
- **独立算法节点**:
  - 将原本集成的 `algorithm` 节点拆分为三个专业节点：`XGBoost + SHAP`、`Lasso 回归` 和 `Pearson 相关系数`。
  - 为每个算法节点增加了完整的属性定义和模拟/真实执行逻辑。
- **工作流生命周期管理**:
  - **执行历史**: 增加了对运行历史记录的支持，数据存储在 `localStorage` 中。每条记录包含节点、连线及结果的完整快照。
  - **快照恢复**: 用户现在可以从“运行历史”选项卡中查看并恢复之前的执行状态。
  - **启动引导**: 实现了启动时自动弹出项目管理对话框，引导用户创建新工作流或加载已有项目。
- **高级数据清洗**:
  - 实现了真实的缺失值填充逻辑（均值、中位数、零值填充或直接删除）。
  - 增加了基于 IQR（四分位距）法的异常值检测，并支持自定义系数。
- **相邻系统对接增强**:
  - 扩展了数据拉取模式，支持“按时间范围”、“按方案 (Scheme)”和“按 SN 集合”。
  - 集成了用于选择监测因子的层级树状选择器。

### 🎨 UI/UX 体验优化

- **n8n 风格现代化**:
  - 重新设计了**节点侧边栏**，增加了视觉层次感、渐变效果和交互悬浮动效。
  - 优化了**顶部导航栏**，改进了项目切换和在线状态显示。
  - 统一了**对话框**样式，采用高对比度、现代化的设计美学，对标主流工作流工具。
- **自动化反馈**:
  - 工作流运行成功结束后，系统现在会自动弹出对应的结果分析弹窗 (DataAnalysisModal)。
- **通用配置渲染**:
  - 重构了 `NodeConfigModal.vue`，支持递归渲染嵌套的 `collection` 属性，实现了无需硬编码字段的动态 UI 生成。

### 🔧 修复与优化

- **执行稳定性**:
  - 优化了 `workflowStore.ts` 中的 `isStopping` 信号传播，确保异步任务能被毫秒级中断。
  - 修复了上游节点的 `WAIT_INPUT` 或 `STOPPED` 状态无法正确阻止下游节点执行的问题。
- **连接规则限制**:
  - 验证并修复了连接规则，确保 `trigger` 类别节点可以直接连接到 `terminal` 类别节点。
  - 统一了节点初始化逻辑，确保无论通过拖拽还是快速连接添加的节点都能正确应用默认配置。
- **性能提升**:
  - 使用 `markRaw` 处理大型数据对象，消除了 Vue 响应式系统在处理海量 JSON 时的性能开销。

### 🧪 测试补全

- 增加了针对以下内容的综合单元测试：
  - 新增算法节点（xgboost-shap 等）的执行逻辑。
  - 数据清洗逻辑的准确性。
  - 执行历史的保存与加载状态。
  - 相邻系统模拟数据生成。
- 修复了由于属性重命名和递归逻辑导致的现有测试失败。

## 2026-03-15

### 新增

- 新增“数据体检”节点，支持字段类型识别、缺失率统计、常量列识别、疑似 ID 字段提示与重点字段风险建议。

### 优化

- 将 Pearson 节点从演示数据升级为真实相关性计算，新增相关矩阵热力图、目标字段重点因子排序和显著性摘要。
- 为报告查看器补充热力图渲染支持，确保相关性分析结果可直接可视化展示。

### 修复

- 为本地存储提供器增加 IndexedDB 不可用时的 localStorage 降级逻辑，消除测试环境下的历史记录告警。

## 2026-03-15 补充记录

### 数据体检增强

- 新增目标字段可用性检查、重复样本比例、异常值占比与字段风险等级。
- 重写数据体检节点实现，统一字段类型识别、风险分级、异常值统计与摘要文案生成逻辑。
- 补充数据体检单测，覆盖目标字段可用性、重复样本率、异常值占比和风险等级输出。

### 数据筛选节点

- 新增“数据筛选”节点，支持多条件筛选、全部满足/任一满足模式，以及数值比较、文本包含、空值判断。
- 补充数据筛选节点单测，覆盖多条件数值筛选与字符串匹配场景。

## 2026-03-16 调试修复

### 修复

- 修复首个文件导入节点在调试运行时补全文件后错误触发全局运行、并报“未找到分析模型”的问题。
- 新增挂起调试续跑能力与回归测试，确保运行时输入确认后恢复原节点执行而非进入全局运行。

## [2026-03-20]

### UI/UX

- **工作流复制命名交互**:
  - 在工作流管理中心点击“复制工作流”时，改为先弹出命名弹窗，允许用户在创建副本前设置新工作流名称。
  - 新工作流名称默认填充为“原工作流名 (副本)”，并在名称为空时禁用“确认复制”按钮。

### 测试 (Test)

- 新增工作流管理中心复制命名弹窗交互测试，覆盖默认名称展示与确认复制流程。
- 补充工作流存储层副本自定义名称回归测试，确保副本名称按用户输入持久化。

## [2026-03-20]

### UI/UX

- **工作流运行条高保真重绘**:
  - 将底部“开始运行工作流”主操作改为更贴近设计稿的双层信息按钮，恢复外壳容器、主按钮与停止按钮的组合结构。
  - 将运行条主按钮从 PrimeVue `Button` 改为原生 `button`，避免组件库默认圆角与内边距干扰视觉还原。
  - 重新设计空闲 / 运行中 / 等待补全输入三种状态，默认空闲态不再使用大面积蓝色激活区，降低“误以为正在执行”的认知负担。

### 测试 (Test)

- 重写 `WorkflowCanvas.spec.ts` 中与运行条相关的回归用例，补充原生按钮结构、空闲态样式与运行条文案断言。
