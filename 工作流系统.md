# 工作流系统

本文档聚焦当前仓库中“工作流编辑器 + 执行引擎 + 结果协议”的整体设计，作为工作流系统级规则的主入口。

如果需要查看节点清单、节点职责、节点创建规则与节点实现规范，请直接阅读 `./工作流节点说明.md`。

适用范围：
- 工作流画布与连接规则
- 执行引擎、运行时输入、冻结输出与历史模式
- `NodeResult` / 多输入协议 / 持久化快照等系统级协议
- viewer 注册、预览链路与前端切包策略
- 帮助中心、节点帮助与固定上手流程

参考核心文件：
- `src/stores/workflowStore.ts`
- `src/workflow/templates.ts`
- `src/nodes/types.ts`
- `src/nodes/result.ts`
- `src/utils/storage/types.ts`
- `src/components/workflow/resultView.ts`
- `src/components/workflow/viewers/registry.ts`
- `src/components/workflow/HelpCenterModal.vue`
- `src/components/workflow/help/NodeHelpPanel.vue`

---

## 1. 系统定位

这是一个多因子相关性分析工作流系统，交互形态接近 n8n：
- 用户在画布中拖拽、连接节点构建流程
- Trigger 节点提供数据源或运行时入口
- Action 节点负责中间处理与数据变换
- Terminal 节点负责生成最终分析、图表或导出结果

系统当前有两层核心标准化：
- 节点定义标准化：所有节点通过 `NodeDefinition` 声明式注册
- 结果协议标准化：节点输出统一使用 `NodeResult`

节点层面的详细说明已迁移到 `./工作流节点说明.md`，这里只保留系统级摘要。

---

## 2. 节点体系摘要

当前节点体系分为三类：
- Trigger：如 `file-import`、`manual-json-import`、`neighbor-system`
- Action：如 `data-cleaning`、`data-filter`、`js-transform`、`field-selection`、`sort`、`data-limit`、`data-aggregation`、`data-merge`、`data-profiling`
- Terminal：如 `multiple-linear-regression`、`random-forest-feature-importance`、`vif`、`xgboost-shap`、`lasso`、`pearson`、`spearman`、`kendall`、`chart-display`、`data-export`

补充说明：
- `data-merge` 是当前核心多输入节点
- `data-merge` 的“横向关联”模式用于按各来源键字段做对象级宽表合并
- `js-transform` 是单输入同步代码转换节点，只暴露 `rows`，固定输出 `table`
- `data-profiling` 虽然输出 `report`，但实现类别仍为 `action`
- 具体节点职责、输入输出与创建规范统一放在 `./工作流节点说明.md`

---

## 3. 节点流与连接规则

连接规则由 `workflowStore.ts` 中的 `CONNECTION_RULES` 与节点输入限制共同决定。

### 3.1 类别级连接规则

- Trigger -> Action / Terminal
- Action -> Action / Terminal
- Terminal -> 无

这意味着：
- Terminal 节点不能继续向下输出连线
- 节点不能回连到 Trigger
- 当前没有单独的 Model 类别，分析终点统一归到 `terminal`

### 3.2 输入数量规则

- 默认节点是单输入
- 多输入节点必须显式声明 `inputMode = 'multiple'`
- 输入数量限制由 `minInputs` / `maxInputs` 控制
- 连线校验在 `validateConnection()` 中完成

### 3.3 全局运行目标选择规则

全局运行时：
- 若存在 terminal，则优先执行所有 terminal 节点
- 若不存在 terminal，则退化为执行所有叶子节点

这样可以同时覆盖：
- 标准分析流跑到终点节点
- 仅由中间节点组成的调试流也能独立执行

---

## 4. 节点定义协议摘要

所有节点都通过 `src/nodes/types.ts` 中的 `NodeDefinition` 声明。

```ts
export interface NodeDefinition<TInput = any, TConfig = Record<string, any>, TOutput = any> {
  name: string
  displayName: string
  icon: string
  category: 'trigger' | 'action' | 'terminal'
  description: string
  help?: NodeHelpDoc
  assistantHints?: NodeAssistantHints
  libraryGroup?: NodeLibraryGroupId
  libraryAliases?: string[]
  libraryKeywords?: string[]
  properties: NodeProperty[]
  inputMode?: 'single' | 'multiple'
  minInputs?: number
  maxInputs?: number | null
  execute: (input: TInput, config: TConfig) => Promise<TOutput> | TOutput
}
```

系统级关注点：
- `category` 决定连接权限与全局运行行为
- `help` 决定帮助中心与节点配置弹窗中的用户帮助内容
- `assistantHints` 为后续 LLM 自动建节点、自动建工作流提供结构化提示
- `inputMode` / `minInputs` / `maxInputs` 决定输入协议
- `execute()` 的输出必须进入标准结果协议

关于字段设计、属性类型、创建步骤与实现规范，请看 `./工作流节点说明.md`。

---

## 4.1 宿主 iframe 看板 token 对接

当本项目以 iframe 形式嵌入现有网站，且工作流中使用 `neighbor-system` / 看板数据对接节点时，宿主页面需要先把访问凭证传入 iframe，节点配置拉取动态选项与节点执行都会复用该凭证。

实现入口：
- 启动时会在 `src/main.ts` 调用 `initializeKanbanHostBridge()`
- 接收逻辑位于 `src/services/kanbanIntegration/index.ts`
- `neighbor-system` 节点通过 `getResolvedKanbanAuthToken()` 读取当前宿主凭证

支持的接入方式：
- `window.postMessage`
- iframe 内窗口的自定义事件 `kanban-host-auth-token`

### 4.1.1 推荐方式：宿主页面通过 `postMessage` 传入

推荐宿主页面在 iframe 加载完成后执行：

```js
const iframe = document.getElementById('analysis-frame')

iframe.contentWindow.postMessage(
  {
    type: 'KANBAN_HOST_AUTH_TOKEN',
    token: '你的业务 token',
  },
  'https://你的 iframe 页面域名',
)
```

也支持以下等价 payload：

```js
iframe.contentWindow.postMessage(
  {
    channel: 'kanban-data-bridge',
    accessToken: '你的业务 token',
  },
  'https://你的 iframe 页面域名',
)
```

### 4.1.2 可识别的消息格式

满足以下任一条件即可被识别为宿主凭证消息：
- `type` 为 `KANBAN_HOST_AUTH_TOKEN`
- `type` 为 `kanban-host-auth-token`
- `type` 为 `KANBAN_DATA_TOKEN`
- `channel` 为 `kanban-data-bridge`

token 字段支持以下任一位置：
- 顶层 `token`
- 顶层 `accessToken`
- `payload.token` / `payload.accessToken`
- `data.token` / `data.accessToken`

如果消息体本身是 JSON 字符串，桥接层也会先尝试解析再读取 token。

### 4.1.3 备选方式：直接派发自定义事件

若宿主页面与 iframe 间具备直接脚本访问能力，也可以向 iframe 内窗口派发：

```js
iframe.contentWindow.dispatchEvent(
  new CustomEvent('kanban-host-auth-token', {
    detail: {
      token: '你的业务 token',
    },
  }),
)
```

### 4.1.4 失败表现

若宿主未传入 token：
- 使用真实宿主桥接时，`neighbor-system` 动态选项读取与执行会报错 `未接收到宿主系统传入的访问凭证`
- 使用默认 mock 桥接时，本地演示数据仍可工作，但这不代表真实宿主集成已经完成

宿主接入排查建议：
- 确认发送时机在 iframe 页面初始化完成之后
- 确认 `postMessage` 的目标源与 iframe 实际地址一致
- 确认消息中至少包含一个可识别的 `type` 或 `channel`
- 确认 token 字段名为 `token` 或 `accessToken`

---

## 5. 标准结果协议：NodeResult

当前执行链已切换到纯 `NodeResult` 协议，不再向下游节点传递旧版 `{ data: [...] }` alias。

### 5.1 基本结构

定义位于 `src/nodes/result.ts`：

```ts
export interface NodeResult<T = unknown> {
  kind: DataKind
  payload: T
  schema?: NodeSchema
  meta?: Record<string, unknown>
  preview?: NodePreviewSpec
  lineage?: Record<string, unknown>
}
```

### 5.2 当前主用结果类型

- `table`
- `tableCollection`
- `report`
- `chart`
- `file`
- `json`

协议还预留了：
- `scalar`
- `matrix`
- `timeseries`
- `custom`

### 5.3 结果辅助能力

`normalizeNodeResult()` 会补齐：
- `table` 的 `schema`
- `table` 的 `meta.rowCount`
- 缺失时的默认 `preview.viewer`

当前常用 helper 包括：
- `createTableResult()`
- `createTableCollectionResult()`
- `createReportResult()`
- `createChartResult()`
- `createFileResult()`
- `createJsonResult()`

### 5.4 结果读取 helper

执行层与 UI 主要通过以下 helper 读取结果：
- `extractTableRows()`
- `extractTableCollectionGroups()`
- `getResultRows()`
- `getResultGroups()`
- `getResultReport()`
- `getResultChartOption()`
- `getResultFileInfo()`

兼容边界：
- 执行层已经不再接受 legacy `{ data: [...] }` 输入包装
- UI 读侧仍保留少量旧结构兼容，主要用于老快照和老测试回放

---

## 6. 多输入协议

当前多输入节点统一使用以下结构：

```ts
export interface MultipleNodeExecutionItem<TResult = NodeResult | null> {
  sourceNodeId: string
  sourceNodeLabel: string
  edgeId?: string
  order?: number
  result: TResult
}

export interface MultipleNodeExecutionInput<TResult = NodeResult | null> {
  inputs?: Array<MultipleNodeExecutionItem<TResult>>
}
```

关键约束：
- 多输入节点统一消费 `inputs[].result`
- `result` 必须是已标准化的 `NodeResult`
- 不再向多输入节点透传旧版 `payload` alias

这也是后续扩展矩阵、时序、标量等结果类型的基础。

---

## 7. 执行引擎

执行引擎位于 `src/stores/workflowStore.ts`。

### 7.1 单节点执行

`executeNode()` 的核心流程：
1. 定位节点并补齐默认配置
2. 检查 Trigger 是否缺少必填运行时输入
3. 若开启手动模拟输入，则优先消费 `manualInput`
4. 递归执行上游节点
5. 组装单输入或多输入协议
6. 普通节点调用节点 `execute()`；`file-import` 会走前端后台解析任务分支
7. 对结果执行 `normalizeNodeResult()` 并写回节点输出

### 7.2 全局执行

`runGlobal()` 的主要流程：
1. 选出 terminal 节点；若无则退化为叶子节点
2. 重置未冻结节点的运行态与缓存
3. 收集缺失运行时输入的 Trigger
4. 逐个执行目标节点
5. 运行结束后写入历史快照

### 7.3 运行时输入

对于必填但缺失的 Trigger 运行时参数：
- 系统会记录 `pendingExecution`
- UI 通过 `RuntimeInputModal` 引导用户补录
- 在全局运行中支持多 Trigger 顺序补录与继续执行

运行时输入现在增加了节点级开关 `reuseLastRuntimeInputs`：
- 默认值为 `false`，即“不沿用上次启动参数”
- 当开关关闭时，Trigger 节点在一次成功执行后会清空运行时参数，下次启动会再次要求输入
- 当开关开启时，节点会保留本次确认过的运行时参数，后续运行可直接沿用上次输入
- `RuntimeInputModal` 会在补录时允许用户切换该开关，并按开关状态决定是否用上次值回填本次表单
- “运行设置”Tab 提供“重置已保存启动参数”按钮，会立即清空当前 Trigger 节点缓存的运行时输入，并同步关闭沿用开关

补充约束：
- `NodeConfigModal` 左侧“运行时输入”区域只用于当次查看、临时填写和调试，不会随着“应用”写回节点持久化配置
- 工作流保存、导出与脏检查签名都会主动剥离所有 `isRuntimeInput` 字段，避免把运行时值误当成静态节点设置持久化
- `file-import` 在补录完成后会启动纯前端后台解析任务，并通过独立任务态暴露阶段、进度、文件名和取消能力
- 当前后台解析阶段包括：读取文件、解析文件内容、清洗并识别字段、整理结果
- `file-import` 的后台任务不进入 `SavedWorkflow`、版本快照和运行时输入持久化，只在当前会话内存在

### 7.4 手动模拟输入

节点支持调试模式：
- `useManualInput = true`
- `manualInput` 为 JSON 字符串
- 执行时直接把它作为节点输入

### 7.5 单节点调试策略

单节点调试现在明确区分两种策略：

- `调试节点`
  - 只重新执行当前节点
  - 默认复用上游节点已有缓存输出
  - 适合快速验证参数调整对当前节点结果的影响

- `重跑上游后调试`
  - 会沿当前链路重新执行上游节点，再执行当前节点
  - 适合验证上游输入已经变化、需要整段链路重新计算的场景

执行引擎补充约束：

- 单节点调试时，执行策略会写入日志，日志区可直接看到“复用上游缓存”或“强制重跑上游”的提示
- 若调试过程中遇到缺失运行时输入，`pendingExecution` 会保留当前调试策略，补录后继续按原策略恢复执行
- 全局运行仍统一视为整条链路执行，不复用“单节点调试策略”入口语义

### 7.6 冻结输出

节点支持 `isPinned`：
- 冻结后优先复用已有输出
- 工作流保存时会保留被冻结节点的输出
- 非冻结节点保存时只保留结构，不保留运行态输出

### 7.7 停止机制

全局停止依赖 `isStopping`：
- 递归执行链会主动检查中止标记
- 停止后会清理 `pendingExecution`
- 全局运行状态会回收为非运行中
- 若当前存在 `file-import` 后台解析任务，停止时会主动触发取消并清理对应任务态

---

## 8. 持久化与历史快照

### 8.1 持久化模型

当前工作流保存结构为 `SavedWorkflow`：
- `id`
- `name`
- `updatedAt`
- `nodes: WorkflowNodeSnapshot[]`
- `edges: Edge[]`

保存前统一经过 `serializeWorkflowNodes()`：
- 节点状态重置为 `idle`
- 只有冻结节点会保留输出

导出工作流 JSON 时使用更轻量的结构快照：
- 同样会补齐默认配置并把节点状态重置为 `idle`
- 不保留任何节点运行缓存，包括冻结节点输出
- 导出文件的目标是“可再次导入并恢复工作流结构与配置”，不是分发当前运行结果

编辑态还维护一份“最近一次已保存快照”签名，用于判断当前工作流是否存在未保存改动：
- 节点、连线、工作流名称变化都会触发未保存状态
- 脏检查仅比较可持久化字段，不把 `selected`、`dragging` 等瞬时画布交互状态计入“已修改”
- 连线同样只比较可持久化字段，不把 Vue Flow 在运行时补充的坐标、选中态等内部字段计入“已修改”
- 加载旧工作流或导入工作流时，会先补齐节点定义中的默认配置，避免首次运行时因运行态补默认值而被误判为已修改
- 新建空白工作流默认视为干净状态
- 导入 JSON 后默认视为未保存状态，需用户显式保存
- 在刷新 / 关闭页面、切换工作流、导入新工作流、新建工作流前，若存在未保存改动，UI 会提示“保存并继续 / 不保存 / 取消”
- 用户主动保存后，会通过 Toast 提示保存成功或失败

存储抽象补充：
- `IStorageProvider` 现在允许返回“当前存储用户”上下文
- 本地模式返回 `null`，表示仅保存在当前浏览器，不引入用户归属概念
- server 模式通过服务端接口解析当前用户，并按当前用户隔离工作流列表、单工作流读取与运行历史
- server 模式下工作流归属以服务端识别的当前用户为准，不要求前端在 `SavedWorkflow` 中显式写入 `userId`
- server 模式的后端持久化当前支持两种实现：
  - `lowdb` 文件落盘：适合单机部署，继续使用 `WORKFLOW_STORAGE_DATA_DIR`
  - `mysql`：适合多实例共享，服务端会自动创建工作流、版本和历史表，但不会自动创建数据库
- `WORKFLOW_STORAGE_BACKEND` 用于在 `lowdb` 与 `mysql` 间切换；前端 `/api/storage/*` 协议与 `IStorageProvider` 不变

### 8.2 工作流版本模型

当前工作流在“保存”和“回滚”两个动作上都会额外生成版本快照，版本协议定义位于 `src/utils/storage/types.ts`：

- `WorkflowVersionMetadata`
  - `id`
  - `workflowId`
  - `workflowName`
  - `createdAt`
  - `workflowUpdatedAt`
  - `source: 'save' | 'rollback'`
- `WorkflowVersionDetail`
  - 在元数据基础上额外包含完整 `workflow: SavedWorkflow`
- `WorkflowRollbackResult`
  - `workflow`
  - `version`

版本快照约束：

- `workflowStore.saveWorkflow()` 成功后，存储层必须追加一个 `source = 'save'` 的版本快照
- `workflowStore.rollbackWorkflowVersion()` 成功后，必须先恢复目标历史快照，再追加一个 `source = 'rollback'` 的新版本，避免回滚链路本身不可追溯
- server 模式通过以下接口提供版本能力：
  - `GET /api/storage/workflows/:workflowId/versions`
  - `GET /api/storage/workflows/:workflowId/versions/:versionId`
  - `POST /api/storage/workflows/:workflowId/versions/:versionId/rollback`
- local 模式当前不额外扩展 IndexedDB schema，而是直接把版本快照写入本地 `workflow_versions` 存储键
- 分析代理工作台左侧侧栏会展示版本历史舱，支持：
  - 版本列表
  - 单版本详情预览
  - 一键回滚到指定版本

### 8.3 历史记录模型

历史记录使用 `ExecutionRecord`：
- `id`
- `workflowId`
- `workflowName`
- `startTime`
- `duration`
- `status`
- `nodes: WorkflowNodeSnapshot[]`
- `edges: Edge[]`

### 8.4 历史快照截断策略

为避免历史记录过大，当前会对超大输出做截断：
- 历史快照不会先完整深拷贝整份节点输出再截断，而是直接按结果类型构造轻量快照，避免大结果在写历史时额外放大一次峰值内存
- `table` 超过 10 行时，仅保留前 10 行
- `tableCollection` 中组内数据过大时，对每组仅保留前 5 行
- `report.meta.sourceData` 过大时，仅保留前 10 行
- `report.sections[].items / allItems / cards` 过大时，仅保留前 20 项
- `report` / `chart` 中图表配置的 `series[].data`、`dataset.source` 过大时，会裁剪到可回放的预览规模
- 截断后写入 `meta.historyTruncated`
- 原始行数写入 `meta.originalRowCount`

### 8.5 历史模式

进入历史模式后：
- 画布切换到历史快照
- 原始编辑态工作流会暂存
- UI 进入只读语义
- 可以退出并恢复编辑态

---

## 9. 结果展示链路

### 9.1 DataDisplayPanel

`DataDisplayPanel.vue` 是统一的数据预览面板，负责：
- 标准化读取 `NodeResult`
- 对表格、分组集合、报告、图表、文件做摘要预览
- 对大数据量做智能截断展示
- 支持打开详情弹窗
- 支持调试模式下编辑手动输入

补充说明：
- `file-import` 在后台解析期间，`RuntimeInputModal` 与 `NodeConfigModal` 会优先展示解析状态横幅，而不是尝试渲染结果预览
- `file-import` 完成后输出结果会自带 `schema`、`meta.rowCount` 与 `preview.summary`，下游字段读取和预览优先复用这些摘要信息，避免再次全表推断

`DataAnalysisModal.vue` 与 `JsonViewer.vue` 补充约束：
- 左侧结果预览与 JSON viewer 不直接渲染完整大对象
- `table` / `tableCollection` 继续采用按行截断预览
- `report` / `json` 使用安全序列化后的摘要结构预览，限制最大深度、数组项数、对象键数与字符串长度，并标记预览已截断
- “导出原始结果”始终导出完整原始数据，不受预览截断策略影响
- 表格派生图由 `DataChart.vue` 承担交互式预览；在单表折线对比场景下，支持把已选数值字段切换为 `Min-Max` 或 `Z-Score` 归一化显示
- `DataChart.vue` 在消费 `table` / `tableCollection` 结果时，优先复用 `NodeResult.schema` 或分组公共 schema，不再为了图表字段选择重复全量推断整份宽表 schema
- 归一化仅作用于图表展示层，不修改 `NodeResult.payload`、原始导出内容和过滤口径；现有上下限过滤始终按原始值生效
- 画布中节点悬浮 toolbox 现在提供“结果预览”快捷入口；若节点已有输出，可直接打开独立 `DataAnalysisModal`，无需先进入节点配置弹窗

全局运行完成后，系统还会自动打开 `WorkflowResultDashboardModal.vue`：
- 默认选中所有终止节点的结果面板
- 若当前工作流没有终止节点，则回退为本次全局执行目标中的末端结果节点
- 左侧允许继续补选其他有结果节点，右侧以多面板方式聚合展示已选节点结果
- 看板布局为会话级状态，不写入 `SavedWorkflow` 与 `ExecutionRecord`
- 标准网格模式采用分层最小高度策略：普通结果卡片至少保留 `360px` 内容空间，`chart` / `table` / `tableCollection` 这类图表优先型结果卡片至少保留 `420px`，避免卡片头部、摘要和 viewer 内部工具栏挤压后图表绘图区被遮挡
- 看板支持浏览器级全屏专注模式：进入后默认收起左侧“节点选择”并隐藏运行概览，仅保留紧凑标题栏与工作台工具栏
- “节点选择”支持折叠为窄侧栏，优先为右侧聚合分析结果释放空间
- 运行概览在普通模式下默认展示，在全屏专注模式下可按需临时展开
- 结果看板进入浏览器全屏后，所有结果 viewer 内部浮层必须挂载到当前全屏容器子树中，不能继续默认挂到 `document.body`；否则浏览器会因 Fullscreen API 限制导致下拉、弹窗、列菜单等 overlay 实际打开但不可见
- 约定统一复用工作流 overlay host 上下文：
  - PrimeVue 的 `Select` / `MultiSelect` / `DatePicker` / `Dialog` / `Popover` 等 overlay 组件使用统一 `appendTo`
  - 自定义浮层与菜单使用统一 `Teleport` 目标
  - 新增结果 viewer、结果看板卡片内交互控件或全屏容器时，必须沿用该宿主约定，禁止在全屏链路中硬编码 `body`

### 9.2 viewer 注册

异步 viewer 注册位于 `src/components/workflow/viewers/registry.ts`：
- `chart-viewer` -> `ChartViewer.vue`
- `file-viewer` -> `ExportViewer.vue`
- `report-viewer` -> `ReportViewer.vue`
- `table-preview` -> `TableViewer.vue`
- `table-collection-preview` -> `TableCollectionViewer.vue`
- `table-chart-combo-viewer` -> `TableChartComboViewer.vue`
- `json-viewer` -> `JsonViewer.vue`

补充约束：
- `report-viewer` 支持节点在 `report.sections[].controls` 中声明轻量图表交互控件
- 当前已支持图表分节内的下拉切换、坐标轴标签截断输入与布尔开关，适用于相关性报告这类“同一数据集多视角切换”场景；例如热力图可在右上角直接切换是否显示格内数值
- `file-viewer` 既支持直接下载已生成文件，也支持对分析报告结果按需生成 PDF 后再下载
- `table-chart-combo-viewer` 在普通表格结果上支持图表 / 表格 / 分屏切换；其图表模式复用 `DataChart.vue`，因此也继承归一化趋势对比能力
- `table-chart-combo-viewer` 的非激活 pane 按需挂载：图表模式不预先创建表格 viewer，表格模式不预先创建图表 viewer，仅在分屏模式下同时挂载两侧，避免宽表结果预览打开和关闭时额外挂载隐藏视图
- `table-preview` 已切换为基于 `AG Grid Community` 的整表虚拟滚动预览，不再依赖原先的分页式 DOM 表格；默认支持快速搜索、排序、基础筛选、列拖动排序、列显隐/固定、批量列宽、密度切换与底部状态栏
- 结果预览中的功能设置现在按节点写入本地存储并自动恢复，包括侧栏折叠、预览条数、图表筛选/归一化/采样/预设、组合预览模式，以及表格分组选择、列布局、排序和筛选；面板展开态、快速搜索词、列搜索词与预设命名输入框等瞬时状态不做持久化
- `DataChart.vue`、`TableViewer.vue`、`ReportViewer.vue` 等 viewer 若包含下拉、列菜单、二级弹窗等 overlay，必须消费统一的 workflow overlay host，而不是各自直接挂到 `body`
- 所有嵌入结果看板卡片的轻量 viewer（如 `chart-viewer`、`table-chart-combo-viewer`、`table-collection-preview`、`json-viewer`）根节点都必须遵循 `h-full + min-h-0 + overflow-hidden` 的布局链路，禁止再额外包一层会重复消耗高度的外边距壳体
- 图表型 viewer 必须给 `VChart` 的直接宿主提供稳定的 `flex-1 + min-h-0` 高度链路，不能只依赖父层 `h-full` 级联；否则在结果看板标准网格中容易因为头部、摘要和 padding 叠加而拿到接近 0 的实际绘图区

这些 viewer 会同时服务于：
- 节点配置弹窗中的结果详情查看
- 工作流结果看板中的多面板结果渲染

### 9.3 结果标准化入口

UI 读侧统一通过 `src/components/workflow/resultView.ts`：
- `normalizeWorkflowResult()`
- `getResultViewerKey()`
- `getResultRows()`
- `getResultGroups()`
- `getResultReport()`
- `getResultChartOption()`
- `getResultFileInfo()`

`WorkflowResultDashboardModal.vue` 还会在此基础上读取一次全局运行摘要：
- `workflowStore.runGlobal()` 结束后写入 `lastRunDashboard`
- UI 根据 `executionTargetIds`、`executionScopeNodeIds` 与当前节点运行态构造结果看板摘要
- 当前仍保留 `lastExecutedTerminalNodeId` 兼容字段，但默认自动展示链路已切换到结果看板

### 9.4 帮助体系

当前工作流 UI 已内置一套轻量帮助体系：

- `WorkflowHeader` 提供全局“帮助”入口
- `HelpCenterModal` 首页第一屏固定展示“3 分钟上手”
- 帮助中心内置常见问题与节点分类索引
- 节点分类索引中的摘要统一复用 `NodeDefinition.help.summary`
- 点击节点后的帮助详情与 `NodeConfigModal` 中的 `NodeHelpPanel` 读取同一份 `NodeDefinition.help`

节点配置弹窗中：

- 节点帮助位于“参数设置”区域顶部
- 帮助区默认展示节点用途、适用场景、输入要求、关键参数、输出与下一步、常见问题
- 若节点定义缺失或帮助内容缺失，UI 必须安全降级，不影响参数编辑和输入输出预览

帮助体系当前采用固定模板而非动态引导：

- 第 1 步：导入数据
- 第 2 步：数据准备
- 第 3 步：分析输出

这套固定模板不仅面向用户，也为后续自动建流能力提供统一流程骨架。

### 9.5 节点库体验

当前右侧 `NodeSidebar` 已从单纯的三大类列表升级为“类型筛选 + 任务场景浏览 + 强搜索”的组合结构：

- 顶部提供 `全部 / 数据接入 / 数据准备 / 分析输出` 类型筛选
- 主列表默认按任务场景分组，例如“导入数据”“清洗与筛选”“字段与结构处理”“统计分析”“模型分析”“结果展示与导出”
- 搜索不仅匹配 `displayName`，还会匹配 `help.summary`、`help.whenToUse`、`libraryAliases` 与 `libraryKeywords`
- 搜索结果会显示命中原因，例如“名称命中”“别名命中”“用途匹配”
- 节点库顶部快捷区支持“最近使用”“上下文推荐”；当用户处于待连线状态时，快捷区会优先切换为“当前可接入”
- “上下文推荐”优先读取当前上下文节点 `assistantHints.recommendedNextNodes`，无法推断时才退回静态起点集合
- 快捷区卡片与任务场景分组卡片都支持单独收起/展开
- 任务场景分组卡片支持单独收起/展开，收起状态只在当前节点库实例内生效，不做持久化

节点库排序与检索所需元数据仍以 `NodeDefinition` 为单一来源，不允许在工作流 UI 内额外维护第二套节点映射。

### 9.6 工作流模板入口

当前工作流系统已提供一套轻量模板创建能力，用于帮助用户快速上手和创建常见分析流程。

模板入口包括：

- `WorkflowHeader` 中的“模板”按钮
- `WorkflowManagerModal` 中的“分析模板”Tab
- `WorkflowManagerModal` 在“我的工作流”页顶部的“从模板创建”入口卡片

模板系统约束：

- 模板展示元数据统一维护在 `src/workflow/templates.ts`
- 每个模板的工作流骨架统一维护在 `src/workflow/template-configs/*.json`
- 模板 JSON 必须直接复用工作流导出格式：`{ name, nodes, edges }`
- 模板创建逻辑统一由 `workflowStore.createWorkflowFromTemplate()` 承接
- 模板创建会直接生成一个新的未保存工作流
- 模板创建不会保留原工作流 `id`，`currentWorkflowId` 会重置为 `null`
- 模板创建时会基于 JSON 骨架重新生成节点 id 和连线 id，但保留模板中的节点配置、位置和连线关系
- 模板中的数据入口默认优先使用 `manual-json-import`，并提供最小样例数据，保障模板创建后可直接运行
- 模板中的手动 JSON 输入仅作为默认起步数据源，用户后续可按场景替换为 `file-import` 或 `neighbor-system`

当前内置模板包括：

- 相关性排查模板
- 关键因子筛选模板
- 变量解释分析模板
- 看板数据对比分析模板

当前模板定位是“最小可运行骨架”而不是“完整分析方案”：

- 节点数量控制在易理解范围内
- 用户创建后仍需按具体数据场景补充字段和参数
- 模板生成后仍完全处于普通编辑态，可继续修改、保存、运行

---

## 10. 性能与切包策略

### 10.1 运行态性能

- 节点输出写入前统一经过 `markRaw`
- 大对象避免进入 Vue 深层响应式跟踪
- 历史快照会主动截断超大结果
- 执行链路不再内置固定 300ms 人工等待，节点耗时只由真实执行逻辑决定
- 工作流日志区仅保留最近 500 条日志，避免长会话下日志数组无限增长

### 10.2 懒加载

以下组件已改为异步加载：
- `DataDisplayPanel.vue` 中的 `MonacoEditor`
- `PropertyField.vue` 中的 `MonacoEditor`
- `viewers/registry.ts` 中的各类结果 viewer

### 10.3 Vite manualChunks

当前构建切包已显式拆分：
- `vendor-monaco`
- `vendor-charts`
- `vendor-primevue`
- `vendor-vue-flow`
- `vendor-data`
- `vendor-vue`
- `vendor-ui`
- `vendor-export-canvas`
- `vendor-export-pdf`
- `vendor`

---

## 11. 维护原则

当以下任一内容发生变化时，必须同步更新本文档：
- 工作流连接规则、执行顺序或运行目标选择规则变化
- `NodeResult` 协议、多输入协议或持久化快照模型变化
- 执行引擎、运行时输入、冻结输出、历史模式变化
- viewer 注册、预览逻辑、分析弹窗链路变化
- 结果看板的默认选中规则、布局模式或自动打开行为变化
- 帮助中心入口、节点帮助展示方式或固定上手流程变化
- 构建切包与懒加载策略变化

当以下内容发生变化时，应同步更新 `./工作流节点说明.md`：
- 节点列表、节点职责、节点类别变化
- `NodeDefinition` / `NodeProperty` 设计变化
- 新节点创建规则、节点实现规范变化
## 2026-03-25 补充说明

- 原“字段合并”节点能力已并入 `data-merge` 的“横向关联”模式，不再保留独立节点。
- `data-merge` 在横向关联模式下使用“节点级统一键名称 + 每个来源一个来源键字段”的配置协议。
- 横向关联执行结果会移除各来源原始 key 字段，只保留 `unifiedKeyName` 对应的一列作为合并后的主键字段。

## 2026-03-22 补充说明

- 新增右侧折叠式“AI 编排”面板，位于节点库右侧，默认收起。
- AI 编排支持两种模式：从零创建工作流、基于当前画布修改现有工作流。
- AI 修改不会直接落图，而是先生成结构化变更计划，用户确认后再应用到画布。
- `workflowStore` 新增 AI 计划应用与回滚能力，包括可编辑快照、计划校验、批量创建/改名/连线/改配置。
- 新增 OpenAI 兼容模型配置链路：后端提供系统默认模型配置、模型连通性测试与代理转发接口；前端可保存自定义模型配置。

## 2026-03-22 TypeScript AI 后端迁移补充

- 工作流 AI 编排不再通过 Python `backend/main.py` 提供模型代理能力，改为独立的 TypeScript 服务进程承接。
- 新增 TS 后端接口：`GET /api/workflow-ai/model-profiles`、`POST /api/workflow-ai/model-profiles/test`、`POST /api/workflow-ai/plan`。
- 前端只通过 `fetch` 调用 TS 后端，不再在浏览器侧直接使用 Vercel AI SDK 组装 OpenAI-compatible provider。
- Vercel AI SDK 与 OpenAI-compatible provider 现在只在 TS 后端执行，系统默认模型配置与系统 API Key 也只保存在 TS 后端。
- 本地开发需要同时启动前端 `pnpm dev` 与 AI 服务 `pnpm dev:server`；Vite 通过 `/api/workflow-ai/*` 代理到 TS 服务。
- Python 后端继续保留算法分析类接口，例如 `/analyze/xgboost-shap`，不再承接工作流 AI 编排请求。

## 2026-03-29 AI 编排稳健性与观测补充

- `POST /api/workflow-ai/plan` 不再只返回单一计划对象，成功时返回 `{ plan, diagnostics }`，失败时返回 `{ message, diagnostics }`。
- 新增 `POST /api/workflow-ai/plan/stream`，使用 `application/x-ndjson` 按行返回流式事件，前端可实时展示编排进度与模型原文输出。
- `diagnostics` 统一描述 AI 编排阶段信息，当前阶段包括：
  - `model_request`
  - `parse`
  - `validate`
  - `apply`
- 流式接口当前事件类型包括：
  - `started`
  - `attempt_started`
  - `stage_changed`
  - `text_delta`
  - `diagnostic`
  - `completed`
  - `failed`
- TS 后端在 AI 编排计划生成时会先做一次标准生成；若遇到模型输出非 JSON、结构不完整或计划校验失败，会自动带着上一轮失败原因发起一次“定向修复重试”。
- AI 编排当前默认优先生成“最小可行工作流”，复杂增强项优先落到 `warnings` 或 `questions`，减少弱模型一次性输出过度复杂计划导致的失败。
- 若模型请求阶段超时或上游模型长时间不返回，后端会在超时后结束本轮请求，并返回明确失败事件，避免前端长期停留在“生成中”。
- 计划校验不再只检查节点是否存在，还会校验：
  - 节点类型是否存在
  - 连接规则是否满足
  - 单输入 / 多输入数量限制是否满足
  - 配置字段是否属于节点定义
  - 最终计划态下必填非运行时配置是否补齐
  - 空计划是否附带必要的追问或风险提示
- 前端 `WorkflowAiPanel` 新增诊断卡，统一展示失败阶段、自动修复重试记录、校验问题与模型原始输出摘要。
- 前端 `WorkflowAiPanel` 新增“实时进度”和“模型实时输出”区域，生成期间会持续展示当前阶段、尝试记录和模型增量文本。
- AI 编排关键事件会同步写入工作流日志区，统一前缀为“AI编排”，便于和运行日志一起排查。

## 2026-03-30 AI 编排弱模型提示词策略补充

- `POST /api/workflow-ai/plan` 与 `POST /api/workflow-ai/plan/stream` 现在统一采用“两阶段编排”：
  - 第一阶段先生成工作流骨架，只允许输出 `createNode` 与 `connectNodes`
  - 第二阶段再围绕骨架中的已选节点补齐最小可运行配置
- 第一阶段的目标是提高弱模型成功率，因此默认优先生成“最小可行工作流”，复杂增强步骤应尽量转入 `warnings` 或 `questions`，而不是一次性塞进 `operations`。
- 骨架阶段现在会先按用户需求召回少量候选编排模板（recipe），并把模板中的最小骨架、推荐入口节点与推荐终端节点注入提示词，减少模型从零自由拼接节点导致的误选。
- 模型可见的节点目录不再直接透传完整节点定义，而是裁剪为精简目录，只保留：
  - 节点基础身份信息
  - 输入模式与输入数量约束
  - 允许连接到的下游类别
  - 必填配置字段名、运行时输入字段名
  - 少量关键词
- 第二阶段只下发当前骨架中实际使用到的节点子集，避免把无关节点噪音继续暴露给模型。
- 骨架阶段的计划校验会放宽“必填配置已补齐”要求，但仍会严格校验：
  - 节点类型是否存在
  - 连线规则是否合法
  - 输入数量限制是否满足
  - 空计划是否携带必要的追问或风险提示
- 配置补全阶段仍使用完整计划校验，最终落给前端确认的计划必须满足标准结果协议下的配置完整性要求。
- 若配置补全阶段已经拿到结构合法、但仍缺少少量必填配置的草案，而后续自动修复重试又失败，系统会优先回退到这份“可编辑草案计划”，并把待补充字段写入 `warnings`、`questions` 与 `diagnostics`，避免整次生成直接报错结束。
- 当前“可编辑草案计划”已通过内部 draft graph 层生成，再转换回标准 `WorkflowAiPlan` 输出，为后续把 AI 编排全面升级到 draft-first / tool-first 架构预留稳定中间层。
- 仓库内已新增独立的 AI 专用摘要模块，用于把 `table` 或可转表的 JSON 对象数组压缩为字段摘要、样本值和候选目标/因子信息；它现在已经接入前端 AI 编排会话的本地预处理链路，采用“缓存优先，必要时无副作用临时执行”的策略。
- 若任一阶段输出失败，系统仍会保留当前阶段的失败原因与原始输出摘要，并触发一次定向修复重试；流式接口会继续实时暴露阶段变化与模型增量文本。

## 2026-03-30 AI 编排 Session 化 Tool-first 主链补充

- AI 编排现在新增会话化主链：
  - `POST /api/workflow-ai/session/start`
  - `POST /api/workflow-ai/session/:id/run`
  - `POST /api/workflow-ai/session/:id/input`
  - `GET /api/workflow-ai/session/:id`
- 旧接口 `POST /api/workflow-ai/plan` 与 `POST /api/workflow-ai/plan/stream` 仍保留，作为兼容入口；新前端主链优先走 session 接口。
- 前端 `workflowAiStore` 在发起编排前，会先在本地执行应用内工具预处理，生成 `contextHints` 与 `toolTrace`：
  - `get_workflow_context`
  - `search_recipes`
  - `inspect_cached_schema`
- session 运行期会把本地上下文桥接到 TS 后端 orchestrator，再由 orchestrator 按结构化工具流程推进：
  - 读取工作流上下文
  - 召回 recipe
  - 匹配候选节点与节点定义
  - 读取字段摘要
  - 调用既有两阶段计划生成链
  - 将计划转换为 draft mutations 并写回 draft graph
  - 校验 draft、finalize plan 并产出缺失信息
- 服务端 `inspect_upstream_schema` 已不再只依赖前端桥接摘要：
  - 若请求里已有节点缓存输出，优先直接读取真实缓存结果生成摘要
  - 若没有可用缓存，会在 TS 后端对可支持的 Trigger / Action 链执行无副作用临时执行
  - 若当前目标来自草稿分支，会先把 draft 物化为临时工作流再执行最短链路摘要
  - 默认不会直接对 terminal 节点做临时摘要，而是回溯到最近的可摘要上游节点
- session 流式事件现在支持结构化会话观测，新增事件包括：
  - `recipe_selected`
  - `tool_started`
  - `tool_completed`
  - `draft_updated`
  - `missing_info`
- 当会话进入 `waiting_user` 时，前端会把缺失信息转为可填写表单；用户提交后，答案会写入 `contextHints.userAnswers`，再继续复用同一 session 执行后续编排。
- `completed` 事件除最终 `plan` 外，还会同时携带 `draft`，前端可直接展示当前草稿结构而不必自行从计划反推。
- `WorkflowAiPanel` 已从“模型输出面板”升级为“编排会话面板”，除保留实时进度、诊断和模型原始输出外，还新增：
  - 当前策略
  - 草稿结构
  - 缺失信息
  - 应用内上下文
- “缺失信息”区不再只是只读提示；当前会话若存在阻塞问题，用户可直接在面板内填写补充答案并点击“继续编排”，无需重新发起新会话。
- AI 编排 session store 已从进程内内存提升为文件持久化实现，并带有 TTL 过期清理；在共享同一持久化路径的部署场景下，可被多个实例共同读取。

## 2026-04-14 opencode 会话化分析代理补充

- 分析代理已从旧的 `analysis-agent + run-agent-loop` 主链切换为纯 `opencode sdk` 会话模型：
  - 不再以自研 `agent-loop`、`preset`、`iteration` 作为主路径
  - 服务端统一通过 `/api/agent/sessions/*` 提供 agent 能力
  - 当前生产接口包括：
    - `POST /api/agent/sessions`
    - `GET /api/agent/sessions/:id`
    - `POST /api/agent/sessions/:id/messages`
    - `GET /api/agent/sessions/:id/events`
    - `GET /api/agent/sessions/:id/projection`
    - `POST /api/agent/sessions/:id/canvas-sync`

- 后端 agent 集成层当前拆为四层：
  - `Agent Session Bridge`：创建 / 恢复 opencode session，发送消息并转发原始事件
  - `Projection Service`：把原始事件映射为当前系统可消费的业务快照
  - `Workflow MCP`：向 opencode 暴露工作流上下文、节点目录、版本历史与画布能力
  - `Workflow Execution Capability`：复用现有节点执行器、画布同步与版本快照能力

- opencode 通过远程 HTTP MCP 接入当前系统能力，统一入口为：
  - `POST /api/opencode/workflow-mcp`
  - `GET /api/opencode/workflow-mcp/health`
  - 实际由 `src/server/opencode/workflowMcpServer.ts` 按 Streamable HTTP MCP 协议承接
  - 当前工具命名统一收敛为 `workflow_*`
  - 当前内置工具包括：
    - `list_workflow_tools`
    - `get_analysis_session_context`
    - `get_node_catalog`
    - `get_node_definition`
    - `list_session_data_sources`
    - `get_data_source_schema`
    - `validate_workflow_plan`
    - `execute_workflow_plan`
    - `get_execution_result`
    - `get_saved_workflow`
    - `list_workflow_versions`
    - `get_workflow_version`
    - `rollback_workflow_version`
  - 若设置 `WORKFLOW_MCP_AUTH_TOKEN`，opencode 挂载 workflow MCP 时必须同时携带 `x-workflow-mcp-auth-token`
  - `GET /api/opencode/workflow-mcp/health` 用于返回当前 MCP 鉴权开关、会话存活数、过期清理计数与工具调用统计

- Projection 是当前工作流系统和 opencode 之间的唯一业务翻译层，快照统一包含：
  - `workflow`：当前工作流草案摘要、节点数、连线数、版本信息与可应用计划
  - `analysis`：分析目标、候选目标/因子、方法、结论、风险与建议
  - `execution`：最近动作、工具调用、待确认事项与失败摘要
  - `canvasSync`：当前画布同步状态
  - `error`：当前会话级业务错误

- 事件流不再暴露旧 loop 轮次事件，前端主消费事件改为：
  - `session.status.updated`
  - `message.delta`
  - `message.completed`
  - `projection.workflow.updated`
  - `projection.analysis.updated`
  - `projection.execution.updated`
  - `projection.canvas_sync.updated`
  - `projection.error.updated`
  - `failed`

- 工作台布局仍保持“Agent Workspace + Execution Workspace”的业务协同，但 agent 工作台内部已改为单列对话式业务流：
  - 工作流业务态、分析业务态、执行业务态、画布同步结果和错误信息全部融合进同一条消息流
  - 不再保留左右分裂的运行 rail、`preset` 选择和 `第 N 轮` 文案
  - `AgentComposer` 继续提供业务提问入口，并保留“同步当前画布”动作

- `WorkflowCanvas.vue` 当前联动约束：
  - 顶部“分析代理”开关仍负责展开或收起 `Agent Workspace`
  - 右侧画布结构变化会自动回写当前 agent 会话的画布摘要
  - 画布侧运行 banner 直接读取 session / projection 状态，不再依赖旧 `AgentLoopOutput`

- `workflowAiStore` 当前以 `session + projection` 为主状态模型：
  - `activeSession`
  - `projectionSnapshot`
  - `sessionMessages`
  - `streamingMessage`
  - `agentMessages`
  - 旧 `agentLoop*` 字段只允许保留最小兼容映射，不再作为主链状态真相源

- 版本历史仍是工作流系统统一能力：
  - 既服务于用户手动查看与回滚
  - 也被 `workflow MCP` 复用为 agent 可调用能力
  - 前后端围绕同一套版本协议工作，不允许再维护第二套 agent 专用版本快照结构

- 当前 agent session / workflow MCP 会话仍以内嵌进程内存为主，但已补充基础生命周期治理：
  - `agentSessionStore` 会按 TTL 清理过期会话
  - 当会话数达到上限时，会优先淘汰最旧会话，避免长时间运行时无限堆积
  - 这些指标会同步暴露到 workflow MCP 健康接口，便于排查 opencode 挂载或会话泄漏问题

## 2026-04-15 MCP 闭环验证补充

- agent session 现在允许携带 `dataSources`，作为会话级可绑定数据源清单：
  - 当前支持 `file`
  - 预留 `kanban-recording`
- 每个数据源统一描述：
  - `id`
  - `kind`
  - `entryNodeType`
  - `label`
  - `sourceMeta`
  - `schemaSummary`
  - `bindingPayload`
- `file-import` 与 `neighbor-system` 的服务端闭环验证不再只依赖前端缓存输出：
  - MCP 可先列出当前 session 数据源
  - 再读取字段摘要
  - 再把工作流 trigger 节点绑定到指定数据源并执行计划
  - 最后按 `executionId` 读取节点执行摘要与终止节点结果
- 当前新增的 workflow MCP 工具语义：
  - `list_session_data_sources`：列出当前会话内可绑定的数据源及入口节点类型
  - `get_data_source_schema`：读取指定数据源的字段摘要、候选目标和候选因子
  - `execute_workflow_plan`：按 `{ plan, bindings }` 执行工作流计划，其中 `bindings` 表示 trigger 节点到 session 数据源的绑定表
  - `get_execution_result`：读取指定 `executionId` 的节点执行摘要与终止节点结果
- 为适配 `n8n-mcp + skills` 风格的 agent 搭建与调试，workflow MCP 现在同时提供更细粒度的 `workflow_*` 工具族：
  - `workflow_get_session_context`：读取当前分析上下文、缺失信息和已有画布快照
  - `workflow_list_data_sources` / `workflow_get_data_source_schema`：读取会话级数据源及字段摘要
  - `workflow_search_nodes` / `workflow_get_node` / `workflow_get_node_options`：供模型搜索候选节点、读取节点说明、解析属性候选项与运行时要求
  - `workflow_create_workflow` / `workflow_get_workflow` / `workflow_update_partial_workflow` / `workflow_update_full_workflow`：供模型创建空白工作流、读取现状、按 diff 增量搭建，或整包替换
  - `workflow_validate_workflow`：对已保存工作流或临时快照做结构校验
  - `workflow_debug_node`：单节点调试入口。会自动裁剪到目标节点的上游子图并执行，返回目标节点结果、原始报错和可选 upstream trace
  - `workflow_test_workflow` / `workflow_executions`：运行完整工作流并查询执行历史、单节点结果和产物摘要
  - `workflow_workflow_versions`：查询、读取和回滚工作流版本
- 当前推荐的 agent build / debug 闭环是：
  - 先用 `workflow_get_session_context`、`workflow_list_data_sources`、`workflow_get_data_source_schema` 明确数据源与缺失参数
  - 再用 `workflow_search_nodes`、`workflow_get_node`、`workflow_get_node_options` 选择节点并确定配置
  - 通过 `workflow_create_workflow` + `workflow_update_partial_workflow` 逐步搭建画布
  - 每轮修改后用 `workflow_validate_workflow` 校验结构
  - 节点报错时优先用 `workflow_debug_node` 做单节点调试；整链路验证则用 `workflow_test_workflow`
  - 结果和历史版本分别通过 `workflow_executions`、`workflow_workflow_versions` 回读
- agent session store 现在除消息和 projection 外，还会保留会话级执行记录：
  - 每次通过 `execute_workflow_plan` 触发执行后，都会生成新的 `executionId`
  - 执行记录包含计划摘要、绑定关系、每个节点的执行摘要、终止节点结果和失败信息
- 为保证 Excel / CSV 验证链路可复用，服务端新增了文件型 session 数据源构建能力：
  - 可直接把 CSV / JSON / Excel 文件解析成 `rows`
  - 同步生成标准 `schemaSummary`
  - 供 benchmark、手动验收和后续数据源注册入口复用

## 2026-03-27 Node 统一算法出口补充

- 前端算法节点不再直接请求 Python `backend/main.py`，统一改为调用 TS/Node 后端接口：
  - `POST /api/analysis/multiple-linear-regression`
  - `POST /api/analysis/random-forest-feature-importance`
  - `POST /api/analysis/lasso`
  - `POST /api/analysis/xgboost-shap`
- TS/Node 后端新增算法代理层，收到 `/api/analysis/*` 请求后会转发到 Python 算法服务：
  - `/api/analysis/multiple-linear-regression` -> Python `/analyze/multiple-linear-regression`
  - `/api/analysis/random-forest-feature-importance` -> Python `/analyze/random-forest-feature-importance`
  - `/api/analysis/lasso` -> Python `/analyze/lasso`
  - `/api/analysis/xgboost-shap` -> Python `/analyze/xgboost-shap`
- 浏览器侧现在只需要配置 Node API 地址；`VITE_API_BASE_URL` 的语义统一为“前端访问 Node 的基础地址”。
- Python 算法服务地址通过 Node 运行环境变量 `PYTHON_ANALYSIS_API_BASE_URL` 注入，默认值为 `http://127.0.0.1:8000`。
- 推荐生产部署拓扑为“浏览器 -> Node -> Python”，不要再让浏览器直接跨域访问 Python。
