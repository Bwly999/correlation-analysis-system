# 工作流系统

本文档聚焦当前仓库中“工作流编辑器 + 执行引擎 + 结果协议”的整体设计，作为工作流系统级规则的主入口。

如果需要查看节点清单、节点职责、节点创建规则与节点实现规范，请直接阅读 `./工作流节点说明.md`。

适用范围：
- 工作流画布与连接规则
- 执行引擎、运行时输入、冻结输出与历史模式
- `NodeResult` / 多输入协议 / 持久化快照等系统级协议
- viewer 注册、预览链路与前端切包策略
- 帮助中心、节点帮助与固定上手流程

参考核心文件：
- `src/stores/workflowStore.ts`
- `src/nodes/types.ts`
- `src/nodes/result.ts`
- `src/utils/storage/types.ts`
- `src/components/workflow/resultView.ts`
- `src/components/workflow/viewers/registry.ts`
- `src/components/workflow/HelpCenterModal.vue`
- `src/components/workflow/help/NodeHelpPanel.vue`

---

## 1. 系统定位

这是一个多因子相关性分析工作流系统，交互形态接近 n8n：
- 用户在画布中拖拽、连接节点构建流程
- Trigger 节点提供数据源或运行时入口
- Action 节点负责中间处理与数据变换
- Terminal 节点负责生成最终分析、图表或导出结果

系统当前有两层核心标准化：
- 节点定义标准化：所有节点通过 `NodeDefinition` 声明式注册
- 结果协议标准化：节点输出统一使用 `NodeResult`

节点层面的详细说明已迁移到 `./工作流节点说明.md`，这里只保留系统级摘要。

---

## 2. 节点体系摘要

当前节点体系分为三类：
- Trigger：如 `file-import`、`manual-json-import`、`neighbor-system`
- Action：如 `data-cleaning`、`data-filter`、`js-transform`、`field-selection`、`sort`、`data-limit`、`data-aggregation`、`data-merge`、`data-profiling`
- Terminal：如 `multiple-linear-regression`、`random-forest-feature-importance`、`vif`、`xgboost-shap`、`lasso`、`pearson`、`spearman`、`kendall`、`chart-display`、`data-export`

补充说明：
- `data-merge` 是当前核心多输入节点
- `data-merge` 的“横向关联”模式用于按各来源键字段做对象级宽表合并
- `js-transform` 是单输入同步代码转换节点，只暴露 `rows`，固定输出 `table`
- `data-profiling` 虽然输出 `report`，但实现类别仍为 `action`
- 具体节点职责、输入输出与创建规范统一放在 `./工作流节点说明.md`

---

## 3. 节点流与连接规则

连接规则由 `workflowStore.ts` 中的 `CONNECTION_RULES` 与节点输入限制共同决定。

### 3.1 类别级连接规则

- Trigger -> Action / Terminal
- Action -> Action / Terminal
- Terminal -> 无

这意味着：
- Terminal 节点不能继续向下输出连线
- 节点不能回连到 Trigger
- 当前没有单独的 Model 类别，分析终点统一归到 `terminal`

### 3.2 输入数量规则

- 默认节点是单输入
- 多输入节点必须显式声明 `inputMode = 'multiple'`
- 输入数量限制由 `minInputs` / `maxInputs` 控制
- 连线校验在 `validateConnection()` 中完成

### 3.3 全局运行目标选择规则

全局运行时：
- 若存在 terminal，则优先执行所有 terminal 节点
- 若不存在 terminal，则退化为执行所有叶子节点

这样可以同时覆盖：
- 标准分析流跑到终点节点
- 仅由中间节点组成的调试流也能独立执行

---

## 4. 节点定义协议摘要

所有节点都通过 `src/nodes/types.ts` 中的 `NodeDefinition` 声明。

```ts
export interface NodeDefinition<TInput = any, TConfig = Record<string, any>, TOutput = any> {
  name: string
  displayName: string
  icon: string
  category: 'trigger' | 'action' | 'terminal'
  description: string
  help?: NodeHelpDoc
  assistantHints?: NodeAssistantHints
  libraryGroup?: NodeLibraryGroupId
  libraryAliases?: string[]
  libraryKeywords?: string[]
  properties: NodeProperty[]
  inputMode?: 'single' | 'multiple'
  minInputs?: number
  maxInputs?: number | null
  execute: (input: TInput, config: TConfig) => Promise<TOutput> | TOutput
}
```

系统级关注点：
- `category` 决定连接权限与全局运行行为
- `help` 决定帮助中心与节点配置弹窗中的用户帮助内容
- `assistantHints` 为后续 LLM 自动建节点、自动建工作流提供结构化提示
- `inputMode` / `minInputs` / `maxInputs` 决定输入协议
- `execute()` 的输出必须进入标准结果协议

关于字段设计、属性类型、创建步骤与实现规范，请看 `./工作流节点说明.md`。

---

## 5. 标准结果协议：NodeResult

当前执行链已切换到纯 `NodeResult` 协议，不再向下游节点传递旧版 `{ data: [...] }` alias。

### 5.1 基本结构

定义位于 `src/nodes/result.ts`：

```ts
export interface NodeResult<T = unknown> {
  kind: DataKind
  payload: T
  schema?: NodeSchema
  meta?: Record<string, unknown>
  preview?: NodePreviewSpec
  lineage?: Record<string, unknown>
}
```

### 5.2 当前主用结果类型

- `table`
- `tableCollection`
- `report`
- `chart`
- `file`
- `json`

协议还预留了：
- `scalar`
- `matrix`
- `timeseries`
- `custom`

### 5.3 结果辅助能力

`normalizeNodeResult()` 会补齐：
- `table` 的 `schema`
- `table` 的 `meta.rowCount`
- 缺失时的默认 `preview.viewer`

当前常用 helper 包括：
- `createTableResult()`
- `createTableCollectionResult()`
- `createReportResult()`
- `createChartResult()`
- `createFileResult()`
- `createJsonResult()`

### 5.4 结果读取 helper

执行层与 UI 主要通过以下 helper 读取结果：
- `extractTableRows()`
- `extractTableCollectionGroups()`
- `getResultRows()`
- `getResultGroups()`
- `getResultReport()`
- `getResultChartOption()`
- `getResultFileInfo()`

兼容边界：
- 执行层已经不再接受 legacy `{ data: [...] }` 输入包装
- UI 读侧仍保留少量旧结构兼容，主要用于老快照和老测试回放

---

## 6. 多输入协议

当前多输入节点统一使用以下结构：

```ts
export interface MultipleNodeExecutionItem<TResult = NodeResult | null> {
  sourceNodeId: string
  sourceNodeLabel: string
  edgeId?: string
  order?: number
  result: TResult
}

export interface MultipleNodeExecutionInput<TResult = NodeResult | null> {
  inputs?: Array<MultipleNodeExecutionItem<TResult>>
}
```

关键约束：
- 多输入节点统一消费 `inputs[].result`
- `result` 必须是已标准化的 `NodeResult`
- 不再向多输入节点透传旧版 `payload` alias

这也是后续扩展矩阵、时序、标量等结果类型的基础。

---

## 7. 执行引擎

执行引擎位于 `src/stores/workflowStore.ts`。

### 7.1 单节点执行

`executeNode()` 的核心流程：
1. 定位节点并补齐默认配置
2. 检查 Trigger 是否缺少必填运行时输入
3. 若开启手动模拟输入，则优先消费 `manualInput`
4. 递归执行上游节点
5. 组装单输入或多输入协议
6. 调用节点 `execute()`
7. 对结果执行 `normalizeNodeResult()` 并写回节点输出

### 7.2 全局执行

`runGlobal()` 的主要流程：
1. 选出 terminal 节点；若无则退化为叶子节点
2. 重置未冻结节点的运行态与缓存
3. 收集缺失运行时输入的 Trigger
4. 逐个执行目标节点
5. 运行结束后写入历史快照

### 7.3 运行时输入

对于必填但缺失的 Trigger 运行时参数：
- 系统会记录 `pendingExecution`
- UI 通过 `RuntimeInputModal` 引导用户补录
- 在全局运行中支持多 Trigger 顺序补录与继续执行

运行时输入现在增加了节点级开关 `reuseLastRuntimeInputs`：
- 默认值为 `false`，即“不沿用上次启动参数”
- 当开关关闭时，Trigger 节点在一次成功执行后会清空运行时参数，下次启动会再次要求输入
- 当开关开启时，节点会保留本次确认过的运行时参数，后续运行可直接沿用上次输入
- `RuntimeInputModal` 会在补录时允许用户切换该开关，并按开关状态决定是否用上次值回填本次表单
- `NodeConfigModal` 的“运行设置”Tab 也可管理该开关，便于在不触发运行的情况下提前调整策略
- “运行设置”Tab 提供“重置已保存启动参数”按钮，会立即清空当前 Trigger 节点缓存的运行时输入，并同步关闭沿用开关

### 7.4 手动模拟输入

节点支持调试模式：
- `useManualInput = true`
- `manualInput` 为 JSON 字符串
- 执行时直接把它作为节点输入

### 7.5 单节点调试策略

单节点调试现在明确区分两种策略：

- `调试节点`
  - 只重新执行当前节点
  - 默认复用上游节点已有缓存输出
  - 适合快速验证参数调整对当前节点结果的影响

- `重跑上游后调试`
  - 会沿当前链路重新执行上游节点，再执行当前节点
  - 适合验证上游输入已经变化、需要整段链路重新计算的场景

执行引擎补充约束：

- 单节点调试时，执行策略会写入日志，日志区可直接看到“复用上游缓存”或“强制重跑上游”的提示
- 若调试过程中遇到缺失运行时输入，`pendingExecution` 会保留当前调试策略，补录后继续按原策略恢复执行
- 全局运行仍统一视为整条链路执行，不复用“单节点调试策略”入口语义

### 7.6 冻结输出

节点支持 `isPinned`：
- 冻结后优先复用已有输出
- 工作流保存时会保留被冻结节点的输出
- 非冻结节点保存时只保留结构，不保留运行态输出

### 7.7 停止机制

全局停止依赖 `isStopping`：
- 递归执行链会主动检查中止标记
- 停止后会清理 `pendingExecution`
- 全局运行状态会回收为非运行中

---

## 8. 持久化与历史快照

### 8.1 持久化模型

当前工作流保存结构为 `SavedWorkflow`：
- `id`
- `name`
- `updatedAt`
- `nodes: WorkflowNodeSnapshot[]`
- `edges: Edge[]`

保存前统一经过 `serializeWorkflowNodes()`：
- 节点状态重置为 `idle`
- 只有冻结节点会保留输出

编辑态还维护一份“最近一次已保存快照”签名，用于判断当前工作流是否存在未保存改动：
- 节点、连线、工作流名称变化都会触发未保存状态
- 脏检查仅比较可持久化字段，不把 `selected`、`dragging` 等瞬时画布交互状态计入“已修改”
- 连线同样只比较可持久化字段，不把 Vue Flow 在运行时补充的坐标、选中态等内部字段计入“已修改”
- 加载旧工作流或导入工作流时，会先补齐节点定义中的默认配置，避免首次运行时因运行态补默认值而被误判为已修改
- 新建空白工作流默认视为干净状态
- 导入 JSON 后默认视为未保存状态，需用户显式保存
- 在刷新 / 关闭页面、切换工作流、导入新工作流、新建工作流前，若存在未保存改动，UI 会提示“保存并继续 / 不保存 / 取消”
- 用户主动保存后，会通过 Toast 提示保存成功或失败

存储抽象补充：
- `IStorageProvider` 现在允许返回“当前存储用户”上下文
- 本地模式返回 `null`，表示仅保存在当前浏览器，不引入用户归属概念
- server 模式通过服务端接口解析当前用户，并按当前用户隔离工作流列表、单工作流读取与运行历史
- server 模式下工作流归属以服务端识别的当前用户为准，不要求前端在 `SavedWorkflow` 中显式写入 `userId`

### 8.2 历史记录模型

历史记录使用 `ExecutionRecord`：
- `id`
- `workflowId`
- `workflowName`
- `startTime`
- `duration`
- `status`
- `nodes: WorkflowNodeSnapshot[]`
- `edges: Edge[]`

### 8.3 历史快照截断策略

为避免历史记录过大，当前会对超大输出做截断：
- `table` 超过 100 行时，仅保留前 10 行
- `tableCollection` 中组内数据过大时，对每组做截断
- `report.meta.sourceData` 过大时，仅保留前 10 行
- 截断后写入 `meta.historyTruncated`
- 原始行数写入 `meta.originalRowCount`

### 8.4 历史模式

进入历史模式后：
- 画布切换到历史快照
- 原始编辑态工作流会暂存
- UI 进入只读语义
- 可以退出并恢复编辑态

---

## 9. 结果展示链路

### 9.1 DataDisplayPanel

`DataDisplayPanel.vue` 是统一的数据预览面板，负责：
- 标准化读取 `NodeResult`
- 对表格、分组集合、报告、图表、文件做摘要预览
- 对大数据量做智能截断展示
- 支持打开详情弹窗
- 支持调试模式下编辑手动输入

`DataAnalysisModal.vue` 与 `JsonViewer.vue` 补充约束：
- 左侧结果预览与 JSON viewer 不直接渲染完整大对象
- `table` / `tableCollection` 继续采用按行截断预览
- `report` / `json` 使用安全序列化后的摘要结构预览，限制最大深度、数组项数、对象键数与字符串长度，并标记预览已截断
- “导出原始结果”始终导出完整原始数据，不受预览截断策略影响

全局运行完成后，系统还会自动打开 `WorkflowResultDashboardModal.vue`：
- 默认选中所有终止节点的结果面板
- 若当前工作流没有终止节点，则回退为本次全局执行目标中的末端结果节点
- 左侧允许继续补选其他有结果节点，右侧以多面板方式聚合展示已选节点结果
- 看板布局为会话级状态，不写入 `SavedWorkflow` 与 `ExecutionRecord`
- 看板支持浏览器级全屏专注模式：进入后默认收起左侧“节点选择”并隐藏运行概览，仅保留紧凑标题栏与工作台工具栏
- “节点选择”支持折叠为窄侧栏，优先为右侧聚合分析结果释放空间
- 运行概览在普通模式下默认展示，在全屏专注模式下可按需临时展开

### 9.2 viewer 注册

异步 viewer 注册位于 `src/components/workflow/viewers/registry.ts`：
- `chart-viewer` -> `ChartViewer.vue`
- `file-viewer` -> `ExportViewer.vue`
- `report-viewer` -> `ReportViewer.vue`
- `table-preview` -> `TableViewer.vue`
- `table-collection-preview` -> `TableCollectionViewer.vue`
- `json-viewer` -> `JsonViewer.vue`

补充约束：
- `report-viewer` 支持节点在 `report.sections[].controls` 中声明轻量图表交互控件
- 当前已支持图表分节内的下拉切换与坐标轴标签截断输入，适用于相关性报告这类“同一数据集多视角切换”场景

这些 viewer 会同时服务于：
- 节点配置弹窗中的结果详情查看
- 工作流结果看板中的多面板结果渲染

### 9.3 结果标准化入口

UI 读侧统一通过 `src/components/workflow/resultView.ts`：
- `normalizeWorkflowResult()`
- `getResultViewerKey()`
- `getResultRows()`
- `getResultGroups()`
- `getResultReport()`
- `getResultChartOption()`
- `getResultFileInfo()`

`WorkflowResultDashboardModal.vue` 还会在此基础上读取一次全局运行摘要：
- `workflowStore.runGlobal()` 结束后写入 `lastRunDashboard`
- UI 根据 `executionTargetIds`、`executionScopeNodeIds` 与当前节点运行态构造结果看板摘要
- 当前仍保留 `lastExecutedTerminalNodeId` 兼容字段，但默认自动展示链路已切换到结果看板

### 9.4 帮助体系

当前工作流 UI 已内置一套轻量帮助体系：

- `WorkflowHeader` 提供全局“帮助”入口
- `HelpCenterModal` 首页第一屏固定展示“3 分钟上手”
- 帮助中心内置常见问题与节点分类索引
- 节点分类索引中的摘要统一复用 `NodeDefinition.help.summary`
- 点击节点后的帮助详情与 `NodeConfigModal` 中的 `NodeHelpPanel` 读取同一份 `NodeDefinition.help`

节点配置弹窗中：

- 节点帮助位于“参数设置”区域顶部
- 帮助区默认展示节点用途、适用场景、输入要求、关键参数、输出与下一步、常见问题
- 若节点定义缺失或帮助内容缺失，UI 必须安全降级，不影响参数编辑和输入输出预览

帮助体系当前采用固定模板而非动态引导：

- 第 1 步：导入数据
- 第 2 步：数据准备
- 第 3 步：分析输出

这套固定模板不仅面向用户，也为后续自动建流能力提供统一流程骨架。

### 9.5 节点库体验

当前右侧 `NodeSidebar` 已从单纯的三大类列表升级为“类型筛选 + 任务场景浏览 + 强搜索”的组合结构：

- 顶部提供 `全部 / 数据接入 / 数据准备 / 分析输出` 类型筛选
- 主列表默认按任务场景分组，例如“导入数据”“清洗与筛选”“字段与结构处理”“统计分析”“模型分析”“结果展示与导出”
- 搜索不仅匹配 `displayName`，还会匹配 `help.summary`、`help.whenToUse`、`libraryAliases` 与 `libraryKeywords`
- 搜索结果会显示命中原因，例如“名称命中”“别名命中”“用途匹配”
- 节点库顶部快捷区支持“最近使用”“上下文推荐”；当用户处于待连线状态时，快捷区会优先切换为“当前可接入”
- “上下文推荐”优先读取当前上下文节点 `assistantHints.recommendedNextNodes`，无法推断时才退回静态起点集合
- 快捷区卡片与任务场景分组卡片都支持单独收起/展开
- 任务场景分组卡片支持单独收起/展开，收起状态只在当前节点库实例内生效，不做持久化

节点库排序与检索所需元数据仍以 `NodeDefinition` 为单一来源，不允许在工作流 UI 内额外维护第二套节点映射。

---

## 10. 性能与切包策略

### 10.1 运行态性能

- 节点输出写入前统一经过 `markRaw`
- 大对象避免进入 Vue 深层响应式跟踪
- 历史快照会主动截断超大结果

### 10.2 懒加载

以下组件已改为异步加载：
- `DataDisplayPanel.vue` 中的 `MonacoEditor`
- `PropertyField.vue` 中的 `MonacoEditor`
- `viewers/registry.ts` 中的各类结果 viewer

### 10.3 Vite manualChunks

当前构建切包已显式拆分：
- `vendor-monaco`
- `vendor-charts`
- `vendor-primevue`
- `vendor-vue-flow`
- `vendor-data`
- `vendor-vue`
- `vendor-ui`
- `vendor-export-canvas`
- `vendor-export-pdf`
- `vendor`

---

## 11. 维护原则

当以下任一内容发生变化时，必须同步更新本文档：
- 工作流连接规则、执行顺序或运行目标选择规则变化
- `NodeResult` 协议、多输入协议或持久化快照模型变化
- 执行引擎、运行时输入、冻结输出、历史模式变化
- viewer 注册、预览逻辑、分析弹窗链路变化
- 结果看板的默认选中规则、布局模式或自动打开行为变化
- 帮助中心入口、节点帮助展示方式或固定上手流程变化
- 构建切包与懒加载策略变化

当以下内容发生变化时，应同步更新 `./工作流节点说明.md`：
- 节点列表、节点职责、节点类别变化
- `NodeDefinition` / `NodeProperty` 设计变化
- 新节点创建规则、节点实现规范变化
## 2026-03-25 补充说明

- 原“字段合并”节点能力已并入 `data-merge` 的“横向关联”模式，不再保留独立节点。
- `data-merge` 在横向关联模式下使用“节点级统一键名称 + 每个来源一个来源键字段”的配置协议。
- 横向关联执行结果会移除各来源原始 key 字段，只保留 `unifiedKeyName` 对应的一列作为合并后的主键字段。

## 2026-03-22 补充说明

- 新增右侧折叠式“AI 编排”面板，位于节点库右侧，默认收起。
- AI 编排支持两种模式：从零创建工作流、基于当前画布修改现有工作流。
- AI 修改不会直接落图，而是先生成结构化变更计划，用户确认后再应用到画布。
- `workflowStore` 新增 AI 计划应用与回滚能力，包括可编辑快照、计划校验、批量创建/改名/连线/改配置。
- 新增 OpenAI 兼容模型配置链路：后端提供系统默认模型配置、模型连通性测试与代理转发接口；前端可保存自定义模型配置。

## 2026-03-22 TypeScript AI 后端迁移补充

- 工作流 AI 编排不再通过 Python `backend/main.py` 提供模型代理能力，改为独立的 TypeScript 服务进程承接。
- 新增 TS 后端接口：`GET /api/workflow-ai/model-profiles`、`POST /api/workflow-ai/model-profiles/test`、`POST /api/workflow-ai/plan`。
- 前端只通过 `fetch` 调用 TS 后端，不再在浏览器侧直接使用 Vercel AI SDK 组装 OpenAI-compatible provider。
- Vercel AI SDK 与 OpenAI-compatible provider 现在只在 TS 后端执行，系统默认模型配置与系统 API Key 也只保存在 TS 后端。
- 本地开发需要同时启动前端 `pnpm dev` 与 AI 服务 `pnpm dev:server`；Vite 通过 `/api/workflow-ai/*` 代理到 TS 服务。
- Python 后端继续保留算法分析类接口，例如 `/analyze/xgboost-shap`，不再承接工作流 AI 编排请求。

## 2026-03-27 Node 统一算法出口补充

- 前端算法节点不再直接请求 Python `backend/main.py`，统一改为调用 TS/Node 后端接口：
  - `POST /api/analysis/multiple-linear-regression`
  - `POST /api/analysis/random-forest-feature-importance`
  - `POST /api/analysis/lasso`
  - `POST /api/analysis/xgboost-shap`
- TS/Node 后端新增算法代理层，收到 `/api/analysis/*` 请求后会转发到 Python 算法服务：
  - `/api/analysis/multiple-linear-regression` -> Python `/analyze/multiple-linear-regression`
  - `/api/analysis/random-forest-feature-importance` -> Python `/analyze/random-forest-feature-importance`
  - `/api/analysis/lasso` -> Python `/analyze/lasso`
  - `/api/analysis/xgboost-shap` -> Python `/analyze/xgboost-shap`
- 浏览器侧现在只需要配置 Node API 地址；`VITE_API_BASE_URL` 的语义统一为“前端访问 Node 的基础地址”。
- Python 算法服务地址通过 Node 运行环境变量 `PYTHON_ANALYSIS_API_BASE_URL` 注入，默认值为 `http://127.0.0.1:8000`。
- 推荐生产部署拓扑为“浏览器 -> Node -> Python”，不要再让浏览器直接跨域访问 Python。
