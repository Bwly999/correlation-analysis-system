# 多因子相关性分析 - 工作流与节点系统说明文档

本项目采用类似 **n8n** 的声明式工作流引擎，通过高度标准化的节点协议实现功能的无限扩展。

---

## 1. 系统核心功能特性

### 1.1 节点管理
- **在线重命名**: 直接在画布上双击节点名称，或点击悬浮工具栏的“铅笔”图标即可快速修改节点名称。名称在画布与配置弹窗间实时同步。
- **快速建流 (+)**: 鼠标悬停在节点右侧或连线中间，点击出现的 "+" 号可进入快速连接模式。在该模式下，只需点击右侧节点库，新节点将自动添加并完成连线。
- **视图复位**: 左下角的“准星”图标支持一键校准视口，使所有节点居中显示。

### 1.2 数据获取 (Triggers)
- **多格式支持**: 本地文件导入节点支持 **CSV、Excel (.xlsx/.xls) 和 JSON**。系统具备智能识别功能，上传后自动解析。
- **运行时输入**: 针对文件选取、时间范围等参数，系统支持在执行前通过 `RuntimeInputModal` 弹窗进行即时收集。

### 1.3 调试与执行
- **单节点独立调试**: 在节点设置界面的左侧开启“模拟”开关，即可手动输入 JSON 数据作为 Mock 输入。此时运行节点将不再触发上游，极大提升了中间节点的调试效率。
- **数据深度分析**: 点击数据展示区的“放大”图标，可打开深度分析窗口。除了查看完整 JSON，还集成 **ECharts** 自动生成多因子分布趋势图。
- **全局停止**: 运行时下方会出现红色停止按钮，支持立即中断整个执行链路。

---

## 2. 开发新节点指南

### 2.1 节点协议 (`NodeDefinition`)
在 `src/nodes/definitions/` 下创建定义文件：

```typescript
export const myNode: NodeDefinition = {
  name: 'my-node',           // 唯一标识
  displayName: '我的节点',    // UI 显示名
  icon: 'settings-2',        // Lucide 图标名
  category: 'action',        // trigger | action | model
  properties: [ ... ]        // 参数定义
  execute: async (input, config) => { ... } // 业务逻辑
};
```

### 2.2 参数类型详解 (`NodeProperty`)
- `'string' / 'number' / 'boolean'`: 基础类型。
- `'options'`: 下拉单选。
- `'multi-options'`: 下拉多选（支持从上游因子中动态提取）。
- `'collection'`: **集合类型**。支持嵌套子属性，可用于实现“多组聚合”等复杂配置。
- `'tree'`: 3 层级树形选择器。
- `'file' / 'datetime-range'`: 常配合 `isRuntimeInput: true` 使用，用于运行时收集数据。

---

## 3. 性能优化规范
- **markRaw**: 所有节点的 `output` 结果必须通过 `markRaw` 处理，防止超大型 JSON 数据触发 Vue 的深度响应式追踪，从而保证界面在大数据量下依然丝滑。
- **智能截断**: 系统默认仅渲染数据的前 5 条作为预览，完整数据仅在用户点击“查看完整”时按需加载。
