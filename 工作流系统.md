# 工作流系统

本文档描述当前仓库中“工作流编辑器 + 节点系统 + 执行引擎 + 结果协议”的真实实现状态，作为工作流相关设计与开发的单一事实来源。

适用范围：
- 工作流画布与节点连接规则
- 节点定义方式与输入输出协议
- 执行引擎、运行时输入、历史快照与持久化
- 结果预览、分析弹窗与 viewer 注册
- 新增或重构节点时应遵守的约束

当前文档基于代码实现同步更新，参考核心文件：
- `src/stores/workflowStore.ts`
- `src/nodes/registry.ts`
- `src/nodes/types.ts`
- `src/nodes/result.ts`
- `src/utils/storage/types.ts`
- `src/components/workflow/resultView.ts`
- `src/components/workflow/viewers/registry.ts`

---

## 1. 系统定位

这是一个多因子相关性分析工作流系统，交互形态接近 n8n：
- 用户在画布中拖拽、连接节点
- Trigger 节点提供数据源或运行时入口
- Action 节点负责清洗、聚合、合并、筛选、体检等中间处理
- Terminal 节点负责产出最终分析、图表或导出文件

系统当前已经完成两层标准化：
- 节点定义标准化：所有节点通过 `NodeDefinition` 声明式注册
- 数据结果标准化：节点运行结果统一使用 `NodeResult` 协议

---

## 2. 当前节点地图

### 2.1 Trigger 节点

1. `file-import` / 本地文件导入
   - 读取本地 CSV、Excel、JSON
   - 支持自动识别格式
   - 可选自动数字转换、排除字段
   - 输出标准 `table` 结果

2. `manual-json-import` / 手动输入数据
   - 直接输入 JSON 文本
   - 适合快速验证节点链路、构造最小样例
   - 常用于开发调试、回归测试、手工试算
   - 输出标准 `table` 或 `json` 结果

3. `neighbor-system` / 看板数据对接
   - 通过宿主侧令牌接入外部看板系统
   - 支持按时间、方案、SN、任务令等模式拉取数据
   - 支持树形因子选择
   - 输出标准 `table` 结果，`meta.metadata` 中会附带来源上下文

### 2.2 Action 节点

1. `data-cleaning` / 数据清洗
   - 缺失值处理
   - 异常值处理
   - 数值缩放
   - 分类编码
   - 输出 `table`

2. `data-filter` / 数据筛选
   - 多条件组合筛选
   - 支持 `all` / `any`
   - 输出 `table`

3. `data-aggregation` / 数据聚合
   - 行内多列合并
   - 分组统计
   - 滚动窗口聚合
   - 输出 `table`

4. `data-merge` / 数据合并
   - 当前唯一核心多输入节点
   - `inputMode = 'multiple'`
   - 支持纵向追加、横向关联、分组集合三种模式
   - 输出 `table` 或 `tableCollection`

5. `data-profiling` / 数据体检
   - 从实现上属于 `action`
   - 可对上游表格做字段画像、质量评估、风险判断
   - 输出 `report`
   - 当工作流没有 terminal 节点时，也可作为叶子节点在全局运行中被执行

### 2.3 Terminal 节点

1. `xgboost-shap` / Xgboost + SHAP
   - 调用后端分析接口
   - 输出 `report`

2. `lasso` / Lasso 回归
   - 输出 `report`

3. `pearson` / Pearson 相关分析
   - 输出 `report`

4. `spearman` / Spearman 秩相关分析
   - 输出 `report`

5. `kendall` / Kendall 秩相关分析
   - 输出 `report`

6. `chart-display` / 图表展示
   - 支持散点图、柱状图、箱线图
   - 支持 `table` 与 `tableCollection`
   - 输出 `chart`

7. `data-export` / 数据导出
   - 支持 CSV、Excel、JSON
   - 输出 `file`

---

## 3. 节点流与连接规则

连接规则由 `workflowStore.ts` 中的 `CONNECTION_RULES` 和节点输入限制共同决定。

### 3.1 类别级连接规则

- Trigger -> Action / Terminal
- Action -> Action / Terminal
- Terminal -> 无

也就是说：
- Terminal 节点不能作为上游继续连出
- 不能回连到 Trigger
- 当前没有单独的“Model”类别，分析与展示终点统一归到 `terminal`

### 3.2 输入数量规则

- 默认节点是单输入
- `data-merge` 是多输入节点
- 多输入节点是否允许无限输入，由 `maxInputs` 控制
- 校验在 `validateConnection()` 中完成

### 3.3 全局运行目标选择规则

全局运行时，系统会优先执行所有 terminal 节点：
- 若存在 terminal，终点就是所有 terminal
- 若不存在 terminal，则退化为执行所有叶子节点（没有下游的节点）

这保证了：
- 标准分析流会自然跑到终点节点
- 纯中间节点调试流也能直接运行

---

## 4. 节点定义协议

所有节点都通过 `src/nodes/types.ts` 中的 `NodeDefinition` 声明。

```ts
export interface NodeDefinition<TInput = any, TConfig = Record<string, any>, TOutput = any> {
  name: string
  displayName: string
  icon: string
  category: 'trigger' | 'action' | 'terminal'
  description: string
  properties: NodeProperty[]
  inputMode?: 'single' | 'multiple'
  minInputs?: number
  maxInputs?: number | null
  execute: (input: TInput, config: TConfig) => Promise<TOutput> | TOutput
}
```

字段含义：
- `name`：节点唯一标识，用于注册与执行
- `displayName`：画布与节点库中的中文名称
- `icon`：Lucide 图标名
- `category`：节点类别，决定连接权限与全局运行行为
- `properties`：配置字段定义
- `inputMode`：`single` 或 `multiple`
- `minInputs` / `maxInputs`：多输入节点约束
- `execute()`：节点核心业务逻辑

### 4.1 当前支持的配置字段类型

`NodeProperty.type` 当前支持：
- `string`
- `textarea`
- `number`
- `options`
- `multi-options`
- `boolean`
- `collection`
- `tree`
- `file`
- `datetime-range`
- `json`
- `tags`
- `select-button`

### 4.2 运行时配置能力

节点属性还可以声明：
- `required`：是否必填
- `default`：默认值
- `isRuntimeInput`：是否在运行时通过弹窗收集
- `displayIf(config)`：条件显示
- `resolveOptions(context)`：动态选项
- `useUpstreamFactors`：从上游数据中提取字段选项

当前运行时输入主要集中在 Trigger 节点，例如：
- 文件选择
- 查询日期范围
- 方案、物料、SN、任务令等参数

---

## 5. 标准结果协议：NodeResult

当前执行链已切换到纯 `NodeResult` 协议，不再向下游节点传递旧版 `{ data: [...] }` alias。

### 5.1 基本结构

定义位于 `src/nodes/result.ts`：

```ts
export interface NodeResult<T = unknown> {
  kind: DataKind
  payload: T
  schema?: NodeSchema
  meta?: Record<string, unknown>
  preview?: NodePreviewSpec
  lineage?: Record<string, unknown>
}
```

### 5.2 当前使用中的 `kind`

系统当前实际主用以下结果类型：
- `table`
- `tableCollection`
- `report`
- `chart`
- `file`
- `json`

另外协议预留了：
- `scalar`
- `matrix`
- `timeseries`
- `custom`

### 5.3 结果辅助能力

`normalizeNodeResult()` 会补齐：
- `table` 结果的 `schema`
- `table` 结果的 `meta.rowCount`
- 缺失时的默认 `preview.viewer`

结果创建 helper：
- `createTableResult()`
- `createTableCollectionResult()`
- `createReportResult()`
- `createChartResult()`
- `createFileResult()`
- `createJsonResult()`

### 5.4 结果读取 helper

执行层与 UI 主要通过以下 helper 读取结果：
- `extractTableRows()`
- `extractTableCollectionGroups()`
- `getResultRows()`
- `getResultGroups()`
- `getResultReport()`
- `getResultChartOption()`
- `getResultFileInfo()`

注意：
- 运行时输入解析已经不再接受 legacy `{ data: [...] }` wrapper
- UI 读侧仍保留少量 legacy 输出兼容，主要用于旧数据或旧快照展示

---

## 6. 多输入协议

当前多输入节点统一使用以下结构：

```ts
export interface MultipleNodeExecutionItem<TResult = NodeResult | null> {
  sourceNodeId: string
  sourceNodeLabel: string
  edgeId?: string
  order?: number
  result: TResult
}

export interface MultipleNodeExecutionInput<TResult = NodeResult | null> {
  inputs?: Array<MultipleNodeExecutionItem<TResult>>
}
```

关键点：
- 每个输入项都保留来源节点 id 与标签
- `result` 是已经标准化后的 `NodeResult`
- `workflowStore` 不再下发旧版 `payload` alias
- `data-merge` 已经按纯 `result` 消费上游数据

这为后续扩展提供了明确边界：
- 多输入节点可以直接读取 `result.kind`、`meta`、`schema`、`lineage`
- 后续如果增加矩阵、时序、标量等结果类型，不必再叠加一层历史兼容桥

---

## 7. 执行引擎设计

执行引擎位于 `src/stores/workflowStore.ts`。

### 7.1 单节点执行

`executeNode(nodeId, forceUpdate, executionScope)` 的主要流程：
1. 找到节点并补齐默认配置
2. 如果是 Trigger，先检查是否缺少必填运行时输入
3. 若开启“手动模拟输入”，优先使用 `manualInput`
4. 若节点已缓存输出且未强制刷新，则直接复用
5. 递归执行所有上游节点
6. 根据 `inputMode` 组装单输入或多输入协议
7. 调用节点 `execute()`
8. 结果经 `normalizeNodeResult()` 后写入节点输出

### 7.2 全局执行

`runGlobal()` 的主要流程：
1. 选出 terminal 节点，若无则退化为叶子节点
2. 清理未冻结节点的运行态与输出缓存
3. 收集本轮执行范围内所有缺失运行时输入的 Trigger
4. 逐个执行目标节点
5. 运行结束后写入历史快照

### 7.3 运行时输入队列

对于必填但缺失的 Trigger 运行时参数：
- 系统会设置 `pendingExecution`
- UI 通过 `RuntimeInputModal` 提示用户补录
- 在全局运行场景下，支持多 Trigger 顺序补录与继续执行

### 7.4 手动模拟输入

节点支持调试模式：
- `useManualInput = true`
- `manualInput` 为字符串形式 JSON
- 执行时直接把该 JSON 作为节点输入

这适合：
- 调试中间节点
- 复现复杂边界输入
- 不依赖上游直接验证节点逻辑

### 7.5 冻结输出

节点支持“冻结输出”（`isPinned`）：
- 被冻结后，节点在后续运行中优先复用已有输出
- 工作流保存时会保留被冻结节点的输出
- 非冻结节点保存时只保留结构，不保留运行态输出

### 7.6 停止机制

全局停止依赖 `isStopping`：
- 运行中的递归执行会主动检查中止标记
- 停止后会解除 `pendingExecution`
- 全局运行状态会被回收为非运行中

---

## 8. 工作流持久化与历史快照

### 8.1 持久化模型

持久化类型位于 `src/utils/storage/types.ts`。

当前工作流保存结构：
- `SavedWorkflow`
  - `id`
  - `name`
  - `updatedAt`
  - `nodes: WorkflowNodeSnapshot[]`
  - `edges: Edge[]`

注意：
- 持久化节点使用 `WorkflowNodeSnapshot`
- 保存前统一经过 `serializeWorkflowNodes()`
- 节点状态会重置为 `idle`
- 只有冻结节点会保留输出

### 8.2 历史记录模型

历史记录使用 `ExecutionRecord`：
- `id`
- `workflowId`
- `workflowName`
- `startTime`
- `duration`
- `status`
- `nodes: WorkflowNodeSnapshot[]`
- `edges: Edge[]`

### 8.3 历史快照截断策略

为了避免历史记录膨胀，当前实现会对超大输出做快照截断：
- `table` 超过 100 行时，仅保留前 10 行
- `tableCollection` 中组内数据过大时，对每组做截断
- `report.meta.sourceData` 过大时，仅保留前 10 行
- 截断后会在 `meta.historyTruncated` 中标记
- 原始行数会写入 `meta.originalRowCount`

### 8.4 历史模式

进入历史模式后：
- 当前画布切换为某次历史快照
- 原始编辑态工作流会暂存
- UI 进入只读语义
- 可随时退出并恢复到编辑态

---

## 9. 结果展示与分析弹窗

### 9.1 DataDisplayPanel

`DataDisplayPanel.vue` 是统一的数据预览面板，职责：
- 标准化读取 `NodeResult`
- 对表格、分组集合、报告、图表、文件做摘要预览
- 对大数据量做智能截断展示
- 支持打开详情弹窗
- 支持在调试模式下切换手动输入编辑器

### 9.2 Viewer 注册

当前异步 viewer 注册位于 `src/components/workflow/viewers/registry.ts`：
- `chart-viewer` -> `ChartViewer.vue`
- `file-viewer` -> `ExportViewer.vue`
- `report-viewer` -> `ReportViewer.vue`

表格和 JSON 的轻量预览仍由 `DataDisplayPanel` 自身处理。

### 9.3 结果标准化入口

UI 读侧统一通过 `src/components/workflow/resultView.ts`：
- `normalizeWorkflowResult()`
- `getResultViewerKey()`
- `getResultRows()`
- `getResultGroups()`
- `getResultReport()`
- `getResultChartOption()`
- `getResultFileInfo()`

当前状态：
- 优先消费标准 `NodeResult`
- 对旧结果结构仍保留有限兼容，以便老数据回放

---

## 10. UI 结构

### 10.1 Header

`WorkflowHeader.vue` 负责：
- 当前工作流信息
- 保存、导入、导出入口
- 工作流管理入口
- 历史与状态相关操作

### 10.2 Sidebar

`NodeSidebar.vue` 负责：
- 节点库分类展示
- 搜索与筛选
- 快速建流入口
- 从“待连接”状态直接追加节点

### 10.3 Canvas

`WorkflowCanvas.vue` 负责：
- Vue Flow 画布
- 节点与连线渲染
- 画布拖拽与缩放
- 视图复位
- 节点配置、运行时输入、数据分析等弹窗联动

### 10.4 Footer / LogPanel

底部日志区负责：
- 实时展示执行过程日志
- 展示运行结束、停止、错误等状态
- 配合全局执行做调试与问题定位

---

## 11. 性能与切包策略

当前已落地的优化包括：

### 11.1 运行态性能

- 节点输出写入前统一经过 `markRaw`
- 大对象避免进入 Vue 深层响应式跟踪
- 历史快照会主动截断超大结果

### 11.2 懒加载

以下组件已改为异步加载：
- `DataDisplayPanel.vue` 中的 `MonacoEditor`
- `PropertyField.vue` 中的 `MonacoEditor`
- `viewers/registry.ts` 中的各类结果 viewer

### 11.3 Vite manualChunks

当前构建切包已显式拆分：
- `vendor-monaco`
- `vendor-charts`
- `vendor-primevue`
- `vendor-vue-flow`
- `vendor-data`
- `vendor-vue`
- `vendor-ui`
- `vendor-export-canvas`
- `vendor-export-pdf`
- `vendor`

说明：
- 主入口包已经显著瘦身
- 仍然较大的 chunk 主要来自 Monaco 与 PDF/导出相关重依赖
- 这属于依赖体积问题，不再是业务主链路未切分的问题

---

## 12. 新增节点的实现约束

新增节点时，必须遵守以下约束：

### 12.1 输入输出约束

- 节点输出必须返回标准 `NodeResult`
- 单输入节点直接接收一个输入值
- 多输入节点使用 `{ inputs }` 协议
- 不要再为运行时执行新增 legacy `{ data: ... }` 输入桥

### 12.2 结果设计约束

推荐优先复用现有结果类型：
- 表格类 -> `table`
- 多组表格 -> `tableCollection`
- 分析报告 -> `report`
- 图表 -> `chart`
- 导出结果 -> `file`
- 其他结构化对象 -> `json`

同时尽量把补充信息放到：
- `schema`：字段结构
- `meta`：统计、来源、摘要、状态
- `preview`：viewer、标题、摘要
- `lineage`：字段来源链路

### 12.3 UI 适配约束

若新增结果类型或 viewer：
1. 先定义好 `NodeResult.kind`
2. 在 `preview.viewer` 中约定 viewer key
3. 在 `resultView.ts` 中补充读取 helper（如确有必要）
4. 在 `viewers/registry.ts` 中注册异步 viewer
5. 补单测与构建验证

### 12.4 测试约束

至少应覆盖：
- 节点 execute 逻辑
- 标准结果协议断言
- 多输入协议（如适用）
- store 执行链路回归（如影响工作流运行）
- 历史快照或 viewer 行为（如影响展示）

---

## 13. 当前兼容边界

当前兼容策略分两层：

1. 执行层
- 已经完成纯 `NodeResult` 迁移
- 不再接受旧版 `{ data: [...] }` 执行输入桥
- 多输入节点也不再消费旧 `payload` alias

2. 展示层
- 仍兼容部分旧输出字段，如 `viewType`、`report`、`chartOption`、`exportInfo`
- 主要目的是兼容旧历史记录或旧测试样例

结论：
- 新代码一律按标准结果协议开发
- 旧结构兼容只允许停留在 UI 读侧或测试侧，不应回流到执行链

---

## 14. 维护原则

当以下任一内容发生变化时，必须同步更新本文档：
- 节点列表、节点类别或默认连接方式变化
- `NodeDefinition` 协议变化
- `NodeResult` 协议变化
- 多输入协议变化
- 执行引擎、历史快照、持久化模型变化
- viewer 注册、预览逻辑、分析弹窗变化
- 构建切包与懒加载策略变化

建议把本文档视为“工作流系统说明书”，而不是零散备注；涉及工作流系统的开发、重构、评审，优先以本文档为入口。
