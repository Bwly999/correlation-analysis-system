# 多因子相关性分析 - 工作流与节点系统说明文档

本项目采用类似 **n8n** 的声明式工作流引擎，通过高度标准化的节点协议实现功能的无限扩展。本文档旨在帮助开发者理解系统架构，并指导如何快速开发和集成新节点。

---

## 1. 系统核心架构

系统由三个核心层次组成：

1.  **协议层 (`src/nodes/types.ts`)**: 定义了节点的元数据结构、参数类型和执行契约。
2.  **引擎层 (`src/stores/workflowStore.ts`)**: 负责工作流的拓扑管理、节点连接规则校验、状态持久化以及递归执行逻辑。
3.  **UI 表现层**:
    *   `WorkflowCanvas.vue`: 无边际画布，支持拖拽、连线和快速建流。
    *   `NodeConfigModal.vue`: 节点详情视图 (NDV)，自动根据协议渲染三栏式配置界面。
    *   `RuntimeInputModal.vue`: 运行时拦截器，在执行缺失关键数据时自动弹出。

---

## 2. 节点系统规范

每一个节点都是一个遵循 `NodeDefinition` 接口的 TypeScript 对象。系统通过 **声明式配置** 自动生成 UI，开发者只需关注业务逻辑。

### 2.1 节点分类 (Category)
系统强制执行单向数据流规范，节点分为三类：

| 分类 | 标识 (`category`) | 说明 | 连接规则 |
| :--- | :--- | :--- | :--- |
| **数据获取** | `trigger` | 流程的起点，负责产生原始数据（如文件、API）。 | 仅能连接 Action 或 Model。 |
| **数据处理** | `action` | 中间过程，负责数据清洗、聚合、转换等。 | 仅能连接 Action 或 Model。 |
| **算法模型** | `model` | 流程的终点，执行最终的算法分析。 | **禁止引出连线**。 |

---

## 3. 开发新节点指南

### 步骤 1：创建定义文件
在 `src/nodes/definitions/` 目录下创建新文件，例如 `myNewNode.ts`。

```typescript
import type { NodeDefinition } from '../types';

export const myNewNode: NodeDefinition = {
  name: 'my-custom-node',      // 唯一标识
  displayName: '我的自定义节点', // UI 显示名称
  icon: 'settings-2',          // 图标标识 (Lucide 图标名)
  category: 'action',          // 节点分类
  description: '这是节点的详细描述信息。',
  properties: [
    // 在此定义参数...
  ],
  execute: async (input, config) => {
    // 业务逻辑实现
    return { result: 'success' };
  }
};
```

### 步骤 2：在注册表中登记
打开 `src/nodes/registry.ts`，将新节点导入并加入 `nodeDefinitions` 数组。

```typescript
import { myNewNode } from './definitions/myNewNode';

export const nodeDefinitions: NodeDefinition[] = [
  // ...
  myNewNode
];
```

---

## 4. 参数配置详解 (`NodeProperty`)

`properties` 数组定义了节点在 UI 界面展示的表单项。

### 4.1 基础属性
| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `name` | `string` | 内部键名，在 `execute` 函数中通过 `config[name]` 获取。 |
| `displayName` | `string` | 在界面上展示给用户的标签名。 |
| `type` | `Enum` | 数据类型，决定渲染哪种 UI 组件（详见 4.2）。 |
| `default` | `any` | 默认值。 |
| `description`| `string` | 悬停在帮助图标上显示的提示文字。 |
| `required` | `boolean` | 是否必填，若为 true 且未填，运行会报错。 |
| `isRuntimeInput`| `boolean`| **核心特性**：若为 true，该参数将移至左侧“运行时输入”区，并在执行前弹窗询问（见第 5 节）。 |

### 4.2 数据类型 (`type`) 对应 UI 效果
*   `'string'`: 标准文本输入框 (`InputText`)。
*   `'number'`: 数字输入框 (`InputNumber`)。
*   `'boolean'`: 切换开关 (`ToggleSwitch`)。
*   `'options'`: 下拉选择框。需配合 `options: [{ name: '显示名', value: '值' }]` 使用。
*   `'datetime-range'`: 日期时间范围选择器。常用于 `isRuntimeInput: true` 的场景。
*   `'file'`: 文件上传/选取区域。常用于 `isRuntimeInput: true` 的场景。
*   `'tree'`: 3层结构树形选择器。常用于选择系统因子。

---

## 5. 运行时输入机制 (Runtime Inputs)

本项目针对数据分析场景，提出了 **“配置与输入分离”** 的概念。

*   **静态参数** (`isRuntimeInput: false`): 随工作流保存。例如：缺失值填充策略、采样频率、模型 Alpha 值。
*   **运行时参数** (`isRuntimeInput: true`): 每次运行或调试时可能变化的即时数据。例如：具体要分析的 CSV 文件、具体的回溯时间段。

**交互表现：**
1.  **NDV 视图**：这些参数会显示在配置弹窗的 **左侧面板**。
2.  **画布运行**：点击“开始运行”或单节点调试时，如果此类参数为空，系统会弹出 **RuntimeInputModal** 强制要求用户输入后才开始计算。

---

## 6. 执行逻辑实现 (`execute`)

`execute` 函数是节点的大脑：

```typescript
execute: async (input: any, config: any) => { ... }
```

*   **`input`**: 上游节点的输出数据。如果是 `trigger` 节点，`input` 为 `null`。
*   **`config`**: 当前节点的所有参数值（包括静态配置和运行时输入）。
*   **返回值**: 必须返回一个对象或数组，该值将作为 `output` 传递给下游节点，并在右侧面板实时展示。

---

## 7. 最佳实践建议

1.  **节点解耦**：保持节点功能单一。例如，不要在“数据获取”节点里做“数据清洗”。
2.  **异步处理**：由于算法可能耗时较长，务必使用 `async/await` 编写执行逻辑。
3.  **友好报错**：在 `execute` 内部使用 `throw new Error('具体原因')`，错误信息会自动捕获并以红色显示在底部日志和节点状态上。
4.  **图标规范**：推荐使用 Lucide 图标库中的语义化图标，确保工作流的直观性。
