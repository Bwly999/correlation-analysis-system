# 多因子相关性分析系统 - Agent 指南

本仓库是一个多因子相关性分析系统，包含 n8n 风格的工作流编辑器、声明式节点系统和标准化结果协议。

## 1. 工作流系统文档入口

与“工作流系统”直接相关的内容，一律以 `./工作流系统.md` 为准，包括但不限于：
- 当前节点地图与节点流
- 节点分类、连接规则、输入数量规则
- `NodeDefinition` / `NodeProperty` 设计
- `NodeResult` 协议、多输入协议与历史兼容边界
- 执行引擎、运行时输入、冻结输出、历史模式
- 持久化快照、历史快照截断、viewer 注册与预览逻辑
- 构建切包与懒加载相关的工作流前端实现

要求：
- 若工作流系统实现发生变化，先更新 `./工作流系统.md`
- 本文件只保留约束与入口，不重复维护完整工作流细节
- 当需要理解节点系统或执行链路时，优先阅读 `./工作流系统.md`

## 2. 项目技术栈

- 核心框架：Vue 3（Composition API）+ TypeScript
- 工作流引擎：Vue Flow
- UI 组件库：PrimeVue v4
- 样式方案：Tailwind CSS v4
- 状态管理：Pinia
- 可视化分析：ECharts

## 3. 开发与测试命令

```bash
pnpm install
pnpm dev
pnpm test:unit
pnpm build
```

补充说明：
- 提交前至少应跑与改动相关的测试
- 若改动影响工作流链路、节点协议、viewer 或持久化，默认需要补跑 `pnpm test:unit` 与 `pnpm build`

## 4. 开发约束

### 4.1 文案与交互

- 所有用户可见文案必须使用中文
- 大模型写入或修改中文内容时必须特别注意编码正确性，严禁产生乱码；若发现乱码，必须先修复后再提交
- 工作流、节点、弹窗、提示语、占位符都应保持中文一致性
- 若新增工作流相关能力，请同步检查 `工作流系统.md` 是否需要更新

### 4.2 UI / UX 约束

- 视觉方向保持“极简现代 SaaS”
- 主品牌色使用 `Slate-900`（#0f172a）
- 功能强调色使用 `Blue-600`（#2563eb）
- 严禁使用紫色 / 靛蓝色作为主视觉强调
- 画布、节点、连接点等基础规范如有疑问，直接查 `./工作流系统.md`

### 4.3 工作流实现约束

- 新节点默认遵循当前标准结果协议，不新增旧版执行输入桥
- 新 viewer、新结果类型、新多输入节点，必须与 `./工作流系统.md` 中描述的协议保持一致
- 任何影响连接规则、执行顺序、历史模式、持久化快照的变更，都必须同步更新 `./工作流系统.md`

## 5. 提交规范

- Git commit message 必须使用中文
- 准备提交时必须同步更新 `CHANGELOG.md`
- 尽量保持原子化提交，一个 commit 只做一类事情

## 6. 连接规则摘要

这里只保留摘要，完整说明见 `./工作流系统.md`：
- Trigger -> Action / Terminal
- Action -> Action / Terminal
- Terminal 不允许继续输出连线
- 单输入为默认规则，多输入能力由节点定义显式声明

## 7. 推荐工作方式

处理工作流相关任务时，建议按以下顺序进行：
1. 先阅读 `./工作流系统.md`
2. 再查看对应实现文件
3. 修改代码
4. 同步更新 `./工作流系统.md`
5. 最后更新 `CHANGELOG.md`（若准备提交）

如果任务明确涉及节点流、节点协议或工作流架构，但没有同步更新 `./工作流系统.md`，应视为未完成。
