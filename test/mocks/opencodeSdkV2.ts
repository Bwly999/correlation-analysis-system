export const createOpencodeServer = () => {
  throw new Error('createOpencodeServer must be mocked in tests')
}

export const createOpencodeClient = () => {
  throw new Error('createOpencodeClient must be mocked in tests')
}
