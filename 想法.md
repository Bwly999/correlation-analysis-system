1. 算法类型节点的修改需求，将现在的单个算法节点里面包含各种算法的写法改为，不同算法拆分为多个独立的节点实现
2. 完成后端python的对接, 相关系分析python代码在backend\algorithm\robust_insight_tool.py
3. 将数据展示弹窗的功能进一步完善，查看现在的实现是如何做的，后续根据当前节点的type来自动获取对应的数据展示组件
4. 一键运行实现，运行完后自动跳出输出节点的数据展示弹窗，如果有多个则让用户选择一个
5. 停止功能修复
6. 数据清洗功能进一步完善
7. 进入界面后询问加载哪个工作流或者创建工作流，如果没有已有的工作流则只显示创建工作流
8. 支持工作流运行历史查看，使用S3桶存储数据或者参照一下n8n的实现
9. 补全测试
10. 完成节点和工作流系统重构，便于后续垃圾模型维护和新增功能
11. 修复数据源无法直接连接算法模型节点的bug
12. UI精致计划。目前菜单栏和右侧节点栏还是不行，比不上下面的终端栏
13. 完成相邻系统对接节点，树状选择因子，节点输入是时间、方案或者SN集合

不对不对，我要求的这个算法的前端展示效果是：结果弹窗中有两个显示模式，一个是纯前端分图表展示(展示蜂群图、柱状图(这
个如果是在前端渲染的话，柱子的颜色要与后端SHAP生成的柱状图颜色一致，是玫红色)、所有因子X与Y的影响曲线图，好在前端
渲染的就在前端渲染，搞不了的就用后端生成的图片)、一个是后端的那个完整大图展示，2个显示模式都支持导出功能，一个使用
pdf导出，一个图片直接导出。现在的实现的只有前端的分图表展示，并且展示的是整合报表，甚至整合报表的图片都是错的，当
前显示的是一个单因子的曲线影响图，好好分析一下全流程中是哪里出错了，并完整可靠的完成我这个完整的需求，完成后自动化
测试验证所有功能是否符合预期。

check一下工作流运行是不是会完全重新运行，只有冻结节点时才跳过
数据存储抽象，支持后续快速扩展多种存储方式，如服务器

多数据源、多起始节点，数据流合并节点(append、obj merge)，箱线图图表节点。

验证多启动节点时，如何输入启动参数

异常"未找到分析模型"的限制

当前这些节点数据转换和使用方式是否足够灵活，从而支持后续各种灵活的节点或者数据预览功能的开发？不够的话，这个系统该如何设计扩展？

右上角新增"新建"按钮，并完善"保存按钮的功能", 在网页退出、切换工作流、新建工作流等会让当前工作流信息丢失的情况下提示是否需要保存当前工作流, 保存成功后需要出一个弹出Message来提示保存的成功与否
为当前项目新增一个系统、完善的帮助界面+节点自带使用帮助说明，利好用户的使用体验
在工作流管理中心复制工作流的时候可以弹出一个弹窗来设置新工作流的名称，名称要有默认值

逐个完成以下新增的数据处理节点的需求：

- 字段选择节点，该节点主要用于在算法或者图表算法前筛选想要的字段。配置参数里支持包含和不包含两种模式，然后通过一个可搜索(支持正则搜索)的下来多选框来选择字段。
- 排序节点，支持设置按多个优先级来排序(越前面优先级越高)，支持升序和倒序
- 数据量限制节点，用于截断数据量，支持保留最前面的n个或者最后面的n个
- 数据合并节点，多输入节点，指定前面每个连接节点的合并key和这个key合并后的新名称，然后将不同节点的数据按key字段来进行obj级别的merge, 注意无论数据有没有key match到，合并后的obj的字段都是前面连接节点的并集
- 新增字段选择节点，该节点主要用于在算法或者图表算法前筛选想要的字段。配置参数里支持包含和不包含两种模式，然后通过一个可搜索的下来多选框来选择字段。
- 新增JS代码执行节点，用于灵活转换数据

- [x] 设计一个大的功能点，工作流结果看板，在一个单独的弹窗中可以展示任意多个节点的运行结果，默认展示的节点选择所有终止节点(需要其他节点在节点选择下拉多选框中手动选择)，该功能主要用于更全面的查看不同节点的数据输出效果，从而进行系统性的数据分析，更多细节构思中，Codex也帮忙想想(包括功能点、操作交互)

工作流修改了但是未保存界面上要有提示
PropertyField中的添加来源配置这种的按钮要有边框，不然用户容易忽略这个按钮
检查一下导出原始数据是否可以用于模拟输入
工作量运行看板在标准网格显示模式下有bug，只有一个图表展示节点时，卡片高度很小，无法展示具体内容，切换到自由栅格模式可以正常展示，修复这个标准网格模式下的显示bug

# 未完成的想法：

# 完整实现相邻系统对接这个节点。

## 我先给你看板已有的api系统作为前置条件

### api大全

api1: 查询当前用户已开通权限的产品列表
api2: 查询给定产品的阶段方案信息
api3: 查询指定产品在指定场景下的所有因子信息集合，因子信息中包含每个因子的名称、key、对应工序
api4: 通过给定的产品名、Sn List、要获取数据的因子List来获取系统的因子数据
api5: 通过时间段+物料类型+工序List来查询Sn List
api6：通过阶段方案List+任务令类型来查询Sn List
api7: 通过任务令List来查询Sn List
api8: 查询给定产品的所有场景

### api细节说明

1. 所有api都需要token来鉴权
2. 因子数据查询的真正api实际上就是api4, api5、6、7的查询方案都是为了获取Sn List，然后再去调用api4来获取数据

## 规划并完整完成以下需求：

### 1.节点名称修改

节点名称修改为"看板数据对接"

### 2.节点配置参数完善

节点配置参数有个
2.1 产品名称，一个下拉框，只能选择一个，下拉框数据从api1获取
2.2 因子全集，3层树状多选组件，因子全集很多，该组件需要支持搜索功能(搜索时去匹配3层的name拼接起来的字段)，数据使用上面选择的产品名称+固定的因子场景("全场景/全场景")调用api3来获取

### 3. 节点启动参数完善

节点启动参数支持4类：
3.1 按时间查询，最小维度到天，需要选择时间和物料类型，对应api5(工序List从选择的因子中自动提取);
3.2 按方案查询, 这个一个2层的树状选择，第一层是阶段(如V3、V4、VN1), 第二层是当前阶段下的具体方案(如A、B、C), 对应api6;
3.3 按SN查询，输入Sn List(通过换行符、或者','来拆分)，对应api5
3.4 按任务令查询，输入任务令 List(通过换行符、或者','来拆分)，对应api7

## 实现细节说明

1. 具体的已有的api系统中详细实现、细节和格式我无法给出，这一层具体的对接需要在内网完成，所以你需要抽象一层中间层，你来调用中间层，我后续是对接中间层，我们之间不要耦合

## 杂项



设计一个大的功能点，让系统可以支持通过LLM大模型驱动节点创建、工作流搭建，大模型api协议主要支持openai兼容协议(通过vercel ai sdk来实现)。

要有用户的概念，尤其是在数据保存相关的场景上，尤其是服务器版本的数据保存实现

完成node后端，支持数据保存

后端node运行工作流
节点插件化、LLM驱动节点创建、工作流搭建
为每个工作流新增历史版本查看回退功能?不加了
检查看板对接节点是否OK

## 功能点：

3. 新增所有字段之间的单调性分析节点, 如皮尔逊、斯普尔曼这种算法来计算所有字段两两之间的相关性，最后输出一个完整的热力图
4. 考虑将当前的单独的皮尔逊、斯普尔曼与上面说的单调性分析节点合并

- 新增节点模板功能，提供几个系统自带的模版，方便用户快速上手
