# 工作流节点说明

本文档专门描述工作流中的节点体系、节点职责，以及“如何创建一个新节点”的规则与规范。

如果需要了解执行引擎、结果协议、多输入协议、历史快照与 viewer 链路，请返回 `./工作流系统.md`。

参考核心文件：
- `src/nodes/registry.ts`
- `src/nodes/types.ts`
- `src/nodes/result.ts`
- `src/nodes/definitions/`
- `src/nodes/__tests__/`

---

## 1. 节点体系总览

当前节点分为三类：
- Trigger：流程入口，负责提供原始数据或运行时参数入口
- Action：中间处理，负责变换、清洗、筛选、聚合、合并、体检等处理
- Terminal：流程终点，负责输出分析结果、图表或导出文件

连接规则摘要：
- Trigger -> Action / Terminal
- Action -> Action / Terminal
- Terminal 不允许继续输出连线

输入规则摘要：
- 默认节点是单输入
- 多输入节点必须显式声明 `inputMode = 'multiple'`
- 当前核心多输入节点为 `data-merge`

---

## 2. 当前节点清单

### 2.1 Trigger 节点

1. `file-import` / 本地文件导入
   - 读取 CSV、Excel、JSON
   - 常用于从本地文件启动分析流程
   - 输出 `table`

2. `manual-json-import` / 手动输入数据
   - 直接输入 JSON 文本
   - 常用于调试、构造最小测试样例
   - 输出 `table` 或 `json`

3. `neighbor-system` / 看板数据对接
   - 从外部看板系统拉取业务数据
   - 支持时间、方案、SN、任务令等多种模式
   - 输出 `table`

### 2.2 Action 节点

1. `data-cleaning` / 数据清洗
   - 负责缺失值处理、异常值处理、缩放与编码
   - 输出 `table`

2. `data-filter` / 数据筛选
   - 负责多条件过滤与规则组合
   - 输出 `table`

3. `field-selection` / 字段选择
   - 在进入算法或图表前保留或排除指定字段
   - 支持普通搜索和正则搜索选择字段
   - 输出 `table`

4. `sort` / 排序
   - 按多个优先级规则排序
   - 支持升序和倒序
   - 输出 `table`

5. `data-limit` / 数据量限制
   - 截断表格数据量
   - 支持保留最前面或最后面的 n 条
   - 输出 `table`

6. `data-aggregation` / 数据聚合
   - 负责行内合并、分组统计、滚动窗口聚合
   - 输出 `table`

7. `data-merge` / 数据合并
   - 当前核心多输入节点
   - 支持纵向追加、横向关联、分组集合三种模式
   - 横向关联模式按“统一键名称 + 每个来源一个来源键字段”做对象级宽表合并
   - 输出 `table` 或 `tableCollection`

8. `js-transform` / JS代码执行
   - 单输入同步代码转换节点
   - 代码里只暴露 `rows`
   - 必须返回数组对象列表
   - 固定输出 `table`

9. `data-profiling` / 数据体检
   - 对上游表格做字段画像、质量评估与风险提示
   - 实现类别是 `action`
   - 输出 `report`

### 2.3 Terminal 节点

1. `xgboost-shap` / XGBoost + SHAP
   - 调用后端分析接口
   - 输出 `report`

2. `lasso` / Lasso 回归
   - 调用后端执行真实 Lasso 回归
   - 输出 `report`

3. `pearson` / Pearson 相关分析
   - 通过 `X 字段` / `Y 字段` 配置只计算交叉相关
   - 结果支持按不同 `Y` 字段切换相关性排行
   - 输出 `report`

4. `spearman` / Spearman 秩相关分析
   - 通过 `X 字段` / `Y 字段` 配置只计算交叉相关
   - 结果支持按不同 `Y` 字段切换相关性排行
   - 输出 `report`

5. `kendall` / Kendall 秩相关分析
   - 通过 `X 字段` / `Y 字段` 配置只计算交叉相关
   - 结果支持按不同 `Y` 字段切换相关性排行
   - 输出 `report`

6. `chart-display` / 图表展示
   - 输出 `chart`

7. `data-export` / 数据导出
   - 输出 `file`

---

## 3. 节点定义结构

所有节点都通过 `NodeDefinition` 声明。

```ts
export interface NodeDefinition<TInput = any, TConfig = Record<string, any>, TOutput = any> {
  name: string
  displayName: string
  icon: string
  category: 'trigger' | 'action' | 'terminal'
  description: string
  help?: NodeHelpDoc
  assistantHints?: NodeAssistantHints
  properties: NodeProperty[]
  inputMode?: 'single' | 'multiple'
  minInputs?: number
  maxInputs?: number | null
  execute: (input: TInput, config: TConfig) => Promise<TOutput> | TOutput
}
```

关键字段约束：
- `name` 必须全局唯一，使用稳定的 kebab-case，不要随意改名
- `displayName` 必须是中文，面向最终用户
- `icon` 必须选择已有 Lucide 图标，并保证节点库中可识别
- `category` 必须与节点真实职责一致，不能为了连线方便错放类别
- `help` 是用户帮助的唯一数据源，至少补齐 `summary`、`whenToUse`、`inputGuide`、`outputGuide`
- `assistantHints` 是后续 LLM 自动建节点 / 自动建流的结构化提示层，新增节点必须补齐最小集
- `properties` 必须完整声明，不允许靠组件内硬编码补关键配置
- `execute()` 必须返回可标准化到 `NodeResult` 的结果

---

## 4. NodeProperty 设计规则

当前常用属性类型包括：
- `string`
- `textarea`
- `number`
- `options`
- `multi-options`
- `boolean`
- `collection`
- `tree`
- `file`
- `datetime-range`
- `json`
- `tags`
- `select-button`

设计规范：
- 所有用户可见标签、提示、占位符必须使用中文
- 优先通过 `default` 提供可运行的默认配置
- 用 `required` 明确关键必填项，不靠运行时报错去补语义
- 用 `displayIf` 管理条件显示，不要在 UI 组件里写特判分支
- 需要动态选项时优先使用 `resolveOptions(context)` 或 `useUpstreamFactors`
- 对 `multi-options`，只有确实需要在候选为空时允许用户手工补值的场景，才开启 `forceInput`
- 需要运行前补录的字段才设置 `isRuntimeInput`，不要滥用
- 复杂配置优先用 `collection` 组织，避免堆叠大量平铺字段

---

## 5. 新节点创建规则

这是最重要的部分。新增节点时，必须遵守以下规则。

### 5.1 先判断节点类别

创建前先回答三个问题：
- 它是流程入口、中间处理还是流程终点？
- 它需要单输入还是多输入？
- 它的输出更像表格、报告、图表还是导出文件？

分类原则：
- 能独立产生源数据或运行时入口的，归为 Trigger
- 依赖上游数据继续加工的，归为 Action
- 作为最终产物展示、分析或导出的，归为 Terminal

禁止：
- 为了绕过连接限制，把本应是 Terminal 的节点做成 Action
- 为了减少实现成本，把本应是多输入的节点硬塞成单输入

### 5.2 必须走声明式注册

新增节点时：
1. 在 `src/nodes/definitions/` 下新增定义文件
2. 在 `src/nodes/registry.ts` 中注册
3. 为节点补齐图标、显示名、描述、属性定义
4. 确保节点库可以正常显示与搜索

禁止：
- 在画布组件中硬编码节点类型
- 在弹窗组件中用分支把某个新节点“特殊接进去”

### 5.3 输出必须遵循标准结果协议

新节点输出必须统一走 `NodeResult`：
- 表格类 -> `table`
- 多组表格 -> `tableCollection`
- 分析结论 -> `report`
- 图表 -> `chart`
- 导出结果 -> `file`
- 其他结构化对象 -> `json`

补充规范：
- 不允许新增旧版 `{ data: ... }` 执行输出桥
- 尽量补齐 `schema`、`meta`、`preview`
- 需要字段来源追踪时写入 `lineage`

### 5.4 输入协议必须清晰

- 单输入节点直接消费上游输入
- 多输入节点必须消费 `{ inputs }` 协议
- 多输入节点应统一读取 `inputs[].result`
- 不允许新增旧版 `payload` alias 兼容写法回流到执行链

### 5.5 属性设计必须可维护

- 配置设计优先让用户“直接可理解”，不是让实现“勉强可用”
- 同一类参数命名要稳定，例如 `targetColumn`、`groupBy`、`conditions`
- 不要在一个节点里混入两套互斥心智模型，必要时拆节点或用模式切换
- 如果某节点配置复杂到必须靠大量说明文字才能理解，优先考虑重构属性结构

### 5.6 节点帮助与 LLM 提示规范

所有节点都应补齐帮助信息，并遵守以下规则：

- `help.summary`：一句话说明节点用途，供帮助中心分类索引与节点帮助头部复用
- `help.whenToUse`：说明适用场景，帮助用户判断是否应该选这个节点
- `help.inputGuide`：说明输入要求、前置节点或连接约束
- `help.outputGuide`：说明输出结果形态与可衔接的下一步
- `help.parameterGuide` / `help.nextSteps` / `help.commonIssues`：按节点复杂度补充

`assistantHints` 用于未来自动创建节点和工作流，建议至少补齐：

- `useCases`
- `keywords`
- `workflowRoles`
- `recommendedPrevNodes`
- `recommendedNextNodes`

维护约束：

- 帮助中心中的节点摘要统一复用 `NodeDefinition.help.summary`
- 节点配置弹窗中的帮助详情统一复用 `NodeDefinition.help`
- 不允许在工作流组件中维护第二套节点帮助映射表
- 新增节点时，如果缺少 `help` 最小集，应视为节点定义未完成

### 5.7 节点文案规范

- 节点名称、描述、属性标签、帮助文案、错误提示必须是中文
- 文案要偏工具化、分析化，避免过度营销或口语化
- 不允许出现乱码、中英文混杂无规则、英文占位符直接外露

### 5.8 预览与 viewer 规范

新增节点时要考虑结果如何被预览：
- 常规表格、分组表格、JSON 优先复用现有结果 viewer 体系
- 若需要专用 viewer，先确定 `preview.viewer`
- 再到 `src/components/workflow/viewers/registry.ts` 中注册异步 viewer
- 不要把专用预览逻辑散落到多个工作流组件里
- 新增或调整 viewer 时，要同时确认节点详情弹窗与工作流结果看板都能正确复用该 viewer

### 5.9 历史与持久化兼容规范

新增节点时要确认：
- 节点输出是否可以安全进入历史快照
- 大结果是否需要截断
- 是否会给 `SavedWorkflow` / `ExecutionRecord` 带来异常体积
- 节点在历史模式下是否仍能被正确只读展示

### 5.10 测试规范

新增或重构节点至少应覆盖：
- 节点 `execute()` 的核心逻辑
- 标准结果协议断言
- 多输入协议断言（如适用）
- 节点帮助最小集断言（至少保证 `help.summary`、`whenToUse`、`inputGuide`、`outputGuide` 可用）
- 与 `workflowStore` 的关键执行链回归（如影响运行时）
- viewer 或预览行为验证（如新增专用展示）

优先落点：
- `src/nodes/__tests__/`
- `src/stores/__tests__/`

---

## 6. 推荐的新节点落地流程

建议按以下顺序开发新节点：
1. 先在 `工作流节点说明.md` 中补充节点定位与规则
2. 设计 `NodeDefinition`、属性和输入输出类型
3. 实现 `execute()`，确保返回标准 `NodeResult`
4. 注册节点并接通节点库显示
5. 补齐 `help` 与 `assistantHints`
6. 必要时补 viewer / 预览链路
7. 补节点测试与工作流回归测试
8. 如果影响系统级协议，再同步更新 `工作流系统.md`

---

## 7. 什么时候更新本文档

当以下内容变化时，应同步更新本文档：
- 节点新增、删除、改名、改类别
- 节点职责、输入模式、输出类型变化
- `NodeDefinition` / `NodeProperty` 设计变化
- 节点帮助字段、`assistantHints` 约束、节点创建规范、测试规范、viewer 规范变化

如果只涉及执行链、结果协议、历史快照、viewer 注册体系等系统级规则，则优先更新 `工作流系统.md`。
## 2026-03-25 补充说明

- 原独立“字段合并”节点已移除，其能力收敛到 `data-merge` 的“横向关联”模式。
- `data-merge` 在横向关联模式下的配置协议为：
  - 节点级 `unifiedKeyName` 用于定义合并后统一保留的主键字段名称。
  - `keyMappings` 用于为每个上游来源指定一个来源键字段 `mergeKey`。
- 横向关联执行时会移除各来源原始 key 字段，并在输出结果中仅保留 `unifiedKeyName` 这一列作为统一主键。

## 2026-03-22 补充说明

- 节点自动编排首期只允许从现有节点库中创建节点实例，不允许动态生成新的可执行节点定义。
- LLM 编排优先消费 NodeDefinition.help 与 ssistantHints，因此新增节点时应继续补齐这两类信息。
- AI 计划在应用前会校验节点类型、连接规则、输入模式与引用合法性，节点定义中的类别、输入限制和默认配置必须保持准确。

## 2026-03-22 补充说明

- 节点自动编排首期只允许从现有节点库中创建节点实例，不允许动态生成新的可执行节点定义。
- LLM 编排优先消费 `NodeDefinition.help` 与 `assistantHints`，因此新增节点时应继续补齐这两类信息。
- AI 计划在应用前会校验节点类型、连接规则、输入模式与引用合法性，节点定义中的类别、输入限制和默认配置必须保持准确。
